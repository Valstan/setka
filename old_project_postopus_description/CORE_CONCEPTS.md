# Ключевые концепции и паттерны

> **Назначение:** Извлеченные из обоих проектов универсальные паттерны, подходы и концептуальные решения, которые можно применить в новой архитектуре независимо от конкретной реализации.

---

## 🎯 Философия проектов

### Принцип "Качество контента превыше автоматизации"

**Суть:** Лучше опубликовать меньше, но качественнее

**Проявление:**
- 80-90% постов отфильтровываются
- Множественные проверки на каждом уровне
- Приоритет просмотрам и engagement
- Сохранение атрибуции источников

**Альтернативный подход:**
Публиковать всё подряд для "активности" группы

**Почему наш лучше:**
- Репутация сообщества
- Доверие аудитории
- Меньше жалоб и отписок
- Качественная лента

### Принцип "Региональность - это контекст, а не фильтр"

**Суть:** Один и тот же контент может быть релевантен для одного региона и бесполезен для другого

**Примеры:**
```
Пост: "Открылась новая школа в селе Калинино"

Регион Малмыж: ✅ Релевантно (Калинино рядом)
Регион Вятские Поляны: ❌ Не релевантно (другой район)
```

**Реализация:**
- Списки ключевых слов для каждого региона
- Географические связи между населенными пунктами
- Ручная настройка источников

**Вывод для новой системы:**
Нужна географическая база данных с отношениями между местами и умная система определения релевантности.

### Принцип "Дедупликация на всех уровнях"

**Проблема:** Один контент может прийти из 5-10 источников с вариациями

**Решение:** Многоуровневая дедупликация

**Уровни:**
1. **Структурная** - одинаковый пост (owner_id + post_id)
2. **Медийная** - одинаковые фото/видео (photo_id, video_id)
3. **Текстовая** - семантически идентичный текст ("рафинад")
4. **Историческая** - уже публиковали похожее ранее

**Ключевая идея:**
> Дедупликация - это не точное совпадение, а семантическая близость

**Для новой системы:**
Использовать embeddings и векторный поиск для семантической дедупликации.

---

## 🏗️ Архитектурные паттерны

### Паттерн "Pipeline фильтрации"

**Концепция:** Данные проходят через цепочку фильтров, каждый из которых может отбросить элемент

```
Входные данные
    │
    ▼
[Фильтр 1] → отброшено 30%
    │
    ▼
[Фильтр 2] → отброшено 40%
    │
    ▼
[Фильтр 3] → отброшено 15%
    │
    ▼
Результат (15% от исходных)
```

**Преимущества:**
- Модульность - легко добавлять/убирать фильтры
- Прозрачность - видно, где отсеивается
- Оптимизация - дорогие фильтры в конце
- Тестируемость - каждый фильтр изолирован

**Порядок имеет значение:**
1. Сначала быстрые проверки (отпечатки, даты)
2. Затем средние (текстовый анализ)
3. В конце дорогие (API запросы, AI)

**Применение в новой системе:**
```python
# Псевдокод концепции
pipeline = Pipeline([
    FastDuplicateFilter(),
    DateFilter(),
    RegionalRelevanceFilter(),
    TextQualityFilter(),
    AIClassificationFilter(),  # Дорогой, но точный
    FinalValidation()
])

result = pipeline.process(raw_posts)
```

### Паттерн "Отпечаток для дедупликации"

**Концепция:** Для быстрой проверки создается уникальный "отпечаток" объекта

**Виды отпечатков в проекте:**

**1. Структурный отпечаток (lip)**
```python
lip = f"{post['owner_id']}_{post['id']}"
# Пример: "-12345678_9012"
```

**2. Медиа отпечаток (hash)**
```python
hash = [photo['id'] for photo in post['attachments'] if photo['type'] == 'photo']
# Пример: [457239017, 457239018]
```

**3. Текстовый отпечаток (рафинад)**
```python
rafinad = text.lower().replace(' ', '').replace('\n', '')...
# "завтравмалмыжепройдетконцерт"
```

**Характеристики хорошего отпечатка:**
- ✅ Быстро вычисляется
- ✅ Компактный (для хранения)
- ✅ Уникальный (минимум коллизий)
- ✅ Устойчив к незначительным изменениям (для текста)

**Расширение для новой системы:**
- Перцептивные хеши для изображений
- Векторные embeddings для текста
- Аудио fingerprints для видео
- Временные отпечатки (время + контент)

### Паттерн "Умная агрегация"

**Проблема:** Много маленьких элементов → спам  
**Решение:** Объединить похожее в один элемент

**Примеры в проекте:**

**1. Агрегация новостей**
```
5 новостей по отдельности → Спам
1 пост с 5 новостями → Дайджест ✅
```

**2. Агрегация объявлений (безфото)**
```
20 объявлений за день → Один пост со списком
```

**Правила агрегации:**
- Ограничение по размеру (текст < N символов)
- Ограничение по количеству (медиа < 10 штук)
- Приоритет по важности (просмотры, дата)
- Сохранение атрибуции каждого элемента

**Концепция для новой системы:**
```
Критерии объединения:
1. Временная близость (в течение часа)
2. Тематическая близость (AI классификация)
3. Географическая близость (один регион)
4. Типологическая близость (все новости/все события)

Алгоритм:
- Кластеризация похожих элементов
- Выбор "якоря" (самый важный)
- Добавление остальных как дополнения
- Формирование единого элемента
```

### Паттерн "Глобальный контекст (session)"

**Концепция:** Единый объект с конфигурацией и рабочими данными

**Что хранится:**
```python
session = {
    # Конфигурация
    'name_base': 'mi',
    'name_session': 'novost',
    'post_group_vk': -12345,
    
    # Подключения
    'vk_app': <VK API>,
    'MONGO_BASE': <MongoDB>,
    
    # Рабочие данные
    'work': {
        'novost': {'lip': [...], 'hash': [...]},
        'bezfoto': {'lip': [...]}
    },
    
    # Настройки
    'zagolovok': {...},
    'heshteg': {...},
    'black_id': [...]
}
```

**Преимущества:**
- Единая точка доступа к данным
- Легко передавать между функциями
- Можно сохранить/восстановить состояние

**Недостатки:**
- Глобальное состояние (антипаттерн для больших систем)
- Сложно тестировать
- Не масштабируется

**Эволюция для новой системы:**
```python
# Вместо глобального session использовать:

1. Dependency Injection
class NewsProcessor:
    def __init__(self, config: Config, db: Database, api: VKApi):
        self.config = config
        self.db = db
        self.api = api

2. Context managers
with ProcessingContext(region='mi', type='novost') as ctx:
    results = process_posts(posts, ctx)

3. Immutable configs
config = Config.load('mi')  # Read-only
state = State()  # Mutable, но локальный
```

---

## 🔄 Паттерны работы с данными

### Паттерн "Ротация истории"

**Проблема:** История постов растет бесконечно → замедление

**Решение:** Автоматическая ротация

```python
while len(history) > MAX_SIZE:
    history.pop(0)  # Удалить самый старый
```

**Параметры:**
- MAX_SIZE = 30 для большинства типов
- MAX_SIZE = 100 для важных (novost)
- MAX_SIZE = 1000 для аналитики

**Альтернативы:**
- Хранить всё → растет БД, замедляется
- Хранить с TTL → сложнее контролировать
- Ротация → простота + контроль

**Улучшение для новой системы:**
```python
# Умная ротация с весами
class SmartHistory:
    def add(self, item, importance=1.0):
        # Важные элементы живут дольше
        item.ttl = BASE_TTL * importance
        self.items.append(item)
        self._cleanup()
    
    def _cleanup(self):
        # Удалить истекшие
        now = time.time()
        self.items = [i for i in self.items if i.ttl > now]
```

### Паттерн "Двухуровневая конфигурация"

**Концепция:** Глобальные настройки + региональные переопределения

```
┌─────────────────────────┐
│   Глобальный конфиг     │
│   • Общие правила       │
│   • Токены API          │
│   • Базовые фильтры     │
└───────────┬─────────────┘
            │
            ▼
┌─────────────────────────┐
│  Региональный конфиг    │
│  • Переопределения      │
│  • Локальные источники  │
│  • Специфичные правила  │
└─────────────────────────┘
```

**Загрузка:**
```python
# 1. Загрузить глобальный
config = load_config('global')

# 2. Применить региональный (merge)
regional = load_config('mi')
config.update(regional)

# 3. Результат: региональный переопределяет глобальный
```

**Преимущества:**
- DRY (Don't Repeat Yourself)
- Легко добавлять регионы
- Централизованные обновления

**Недостатки:**
- Неочевидность (откуда пришло значение?)
- Конфликты при merge
- Сложность тестирования

**Улучшение:**
```python
# Явная иерархия
config = Config(
    global_defaults=GlobalConfig.load(),
    regional_overrides=RegionalConfig.load('mi'),
    runtime_params={'date': today}
)

# Прозрачность
config.get('black_id', trace=True)
# > Value from: regional_overrides.black_id
```

### Паттерн "Ленивая загрузка конфигурации"

**Концепция:** Загружать данные только когда нужны

```python
# Плохо: загрузить все сразу
session['work'] = {
    'novost': load_table('novost'),
    'reklama': load_table('reklama'),
    'kultura': load_table('kultura'),
    ...  # 10+ таблиц
}

# Хорошо: загрузить только нужное
if session['name_session'] in session['zagolovki'].keys():
    session['work']['novost'] = load_table('novost')
```

**Применимо когда:**
- Много возможных конфигов
- Дорогая загрузка
- Используется только часть

---

## 🛡️ Паттерны надежности

### Паттерн "Retry с экспоненциальной задержкой"

**Концепция:** При ошибке повторить несколько раз с увеличивающейся задержкой

```python
for attempt in range(3):
    try:
        result = api_call()
        break
    except Exception:
        sleep(10 * (2 ** attempt))  # 10, 20, 40 секунд
```

**Зачем задержка:**
- Временные сбои (сеть, перегрузка)
- Лимиты API (rate limiting)
- Не перегружать при массовых сбоях

**Улучшение:**
```python
@retry(max_attempts=3, backoff=exponential, jitter=True)
async def api_call():
    ...
```

### Паттерн "Тихая деградация"

**Концепция:** При ошибке продолжить работу с уменьшенной функциональностью

**Примеры:**

**1. Fallback для источников**
```python
# Основной источник недоступен
try:
    posts = fetch_from_vk_api()
except:
    posts = fetch_from_cache()  # Старые, но работает
```

**2. Fallback для медиа**
```python
# Фото не загружается
if 'attachments' not in post or not load_photos(post):
    # Опубликовать только текст
    del post['attachments']
```

**3. Fallback для интеграций**
```python
# TGStat API недоступен
try:
    stats = get_stats_from_api()
except:
    stats = "Статистика временно недоступна"
```

**Философия:**
> Лучше работать частично, чем не работать вообще

### Паттерн "Логирование критичных событий"

**Что логировать:**

**Ошибки:**
```python
except Exception as exc:
    send_error(__name__, exc, traceback)
    # Отправить в Telegram админу
```

**Важные действия:**
```python
# Публикация
log(f"Published post {post_id} to {group_id}")

# Изменение конфига
log(f"Config updated: {changes}")
```

**Метрики:**
```python
# Статистика работы
log(f"Processed {len(posts)} posts, "
    f"published {len(result)}, "
    f"filtered {filtered_count}")
```

**Куда:**
- Telegram (для критичного)
- Syslog (для отладки)
- MongoDB (для аналитики)

---

## 🎨 UX паттерны

### Паттерн "Кнопки вместо команд"

**Плохо:**
```
Пользователь: /weather kirov malmyzh
Бот: Неверный формат. Используй: /weather <область> <город>
```

**Хорошо:**
```
Пользователь: [нажимает кнопку "Погода"]
Бот: Выбери область [Кировская] [Татарстан] [Удмуртия]
Пользователь: [Кировская]
Бот: Выбери город [Малмыж] [Калинино] ...
```

**Преимущества:**
- Нет синтаксиса для запоминания
- Меньше ошибок
- Быстрее для пользователя
- Интуитивно понятно

**Когда не подходит:**
- Много опций (100+ городов)
- Уникальный ввод (адрес, телефон)
- Свободный текст (отзыв, вопрос)

### Паттерн "Всегда отвечай пользователю"

**Концепция:** Бот никогда не "молчит"

```python
@dp.message()  # Catch-all
async def handle_any_message(message):
    await message.answer("Спасибо за сообщение! ...")
```

**Почему важно:**
- Пользователь понимает, что его услышали
- Не создается впечатление "сломанного" бота
- Можно собрать обратную связь

**Варианты ответов:**
- Подтверждение получения
- Перенаправление к админу
- Предложение альтернатив
- Возврат в главное меню

### Паттерн "Логирование для обратной связи"

**Концепция:** Каждое действие пользователя → админу

```python
# В каждом handler
await bot.send_message(ADMIN_ID, format_log(message))
```

**Зачем:**
- Понимание использования
- Быстрая реакция на вопросы
- Обнаружение проблем
- Идеи для улучшений

**Важно:**
- Структурированный формат
- Фильтрация спама (не логировать боты)
- Privacy (скрывать чувствительные данные)

---

## 🔌 Паттерны интеграций

### Паттерн "Общее хранилище (MongoDB)"

**Концепция:** Несколько сервисов используют одну БД

```
┌──────────┐        ┌─────────┐
│ Postopus │───────▶│         │
└──────────┘        │ MongoDB │
                    │         │
┌──────────┐        │         │
│ Telebot  │◀───────│         │
└──────────┘        └─────────┘
```

**Правила:**
- Postopus - read/write
- Telebot - read-only
- Разделение коллекций
- Никаких блокировок

**Преимущества:**
- Нет дублирования данных
- Единый source of truth
- Слабая связанность сервисов

**Риски:**
- Конкуренция за ресурсы
- Схема должна быть согласована
- Миграции сложнее

### Паттерн "Гибридные источники"

**Концепция:** Комбинировать кэш и API для баланса скорости/актуальности

```python
# Текст из API (всегда актуален)
text = api.get_post_text(post_id)

# Фото из кэша (быстро)
photos = cache.get_photos(post_id)

# Комбинация
result = {'text': text, 'photos': photos}
```

**Когда применять:**
- Часто меняющиеся + редко меняющиеся данные
- Дорогие + дешевые операции
- Актуальность vs скорость

**Пример из проекта:**
- Афиша: текст из VK API, фото из MongoDB

### Паттерн "Пул токенов"

**Проблема:** API токены имеют лимиты и могут истекать

**Решение:** Пул из нескольких токенов

```python
tokens = ['TOKEN1', 'TOKEN2', 'TOKEN3']
random.shuffle(tokens)

for token in tokens:
    try:
        api = VKApi(token)
        return api  # Нашли рабочий
    except:
        continue

raise Exception("All tokens failed")
```

**Стратегии:**
- Round-robin - поочередно
- Random - случайно (лучше для равномерности)
- Weighted - по весам (приоритет более надежным)
- Health-based - только работающие

---

## 📊 Метрики и оценка

### Метрики качества фильтрации

**Precision (Точность):**
```
Сколько из опубликованного действительно хорошее?
= Хорошие посты / Все опубликованные
```

**Recall (Полнота):**
```
Сколько хорошего мы не пропустили?
= Хорошие опубликованные / Все хорошие
```

**F1-Score:**
```
Баланс между precision и recall
= 2 * (Precision * Recall) / (Precision + Recall)
```

**В проекте (оценочно):**
- Precision: ~95% (редко публикуется мусор)
- Recall: ~90% (иногда пропускается хорошее)
- F1: ~92.5%

### Метрики эффективности

**Скорость обработки:**
- 1 запуск: 10-30 секунд
- Обработка 20 постов: 15-20 секунд
- Публикация: 2-5 секунд

**Нагрузка:**
- API запросы: 50-100 в час
- DB запросы: 10-20 в час
- Публикации: 10-15 в день

**Отсев:**
- Входящий поток: 20 постов
- После фильтров: 2-5 постов (75-90% отсев)
- Финальная публикация: 1 пост (агрегированный)

---

**Продолжение:** См. LESSONS_LEARNED.md для практических выводов →

