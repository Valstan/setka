# Архитектура POSTOPUS: Система автоматического курирования контента

> **Контекст для понимания:** Это описание архитектуры действующей системы, которая решала задачу автоматического отбора и публикации региональных новостей из VK. Документ объясняет КОНЦЕПЦИЮ и ЛОГИКУ, а не детали реализации.

---

## 🎯 Суть проекта

### Проблема
13 региональных VK-сообществ нуждались в постоянном наполнении качественным локальным контентом из 100+ различных источников. Ручная модерация требовала 4-6 часов ежедневной работы.

### Решение
Автоматическая система, которая:
1. Мониторит источники VK контента
2. Фильтрует и классифицирует посты
3. Удаляет дубликаты и нерелевантный контент
4. Агрегирует новости в единый дайджест
5. Публикует в целевые сообщества

### Результат
- Снижение ручной работы с 6 часов до 30 минут в день
- 100% покрытие региональных новостей
- Минимизация дубликатов (< 1%)
- Стабильная работа 3+ года

---

## 🏗️ Общая архитектура

### Концептуальная модель

```
┌─────────────────────────────────────────────────────────────┐
│                    ВХОДЯЩИЙ ПОТОК                           │
│  100+ VK источников (группы, паблики, личные страницы)      │
└──────────────────────┬──────────────────────────────────────┘
                       │
                       ▼
┌─────────────────────────────────────────────────────────────┐
│               ПАРСЕР + ФИЛЬТРАЦИЯ                           │
│  • Загрузка постов через VK API                             │
│  • Многоуровневая фильтрация                                │
│  • Дедупликация через "отпечатки"                           │
│  • Классификация по типу контента                           │
└──────────────────────┬──────────────────────────────────────┘
                       │
                       ▼
┌─────────────────────────────────────────────────────────────┐
│              ОБРАБОТКА И АГРЕГАЦИЯ                          │
│  • Очистка текста от спама                                  │
│  • Извлечение медиа-вложений                                │
│  • Добавление атрибуции источника                           │
│  • Агрегация нескольких новостей в один пост                │
└──────────────────────┬──────────────────────────────────────┘
                       │
                       ▼
┌─────────────────────────────────────────────────────────────┐
│              ПУБЛИКАЦИЯ                                     │
│  • Выбор VK токена                                          │
│  • Формирование финального текста                           │
│  • Публикация в целевое сообщество                          │
│  • Сохранение истории в MongoDB                             │
└─────────────────────────────────────────────────────────────┘
```

### Ключевые компоненты

**1. Управляющий слой (`start.py`, `start_paket.py`)**
- Точка входа системы
- Инициализация сессии
- Выбор режима работы (регион + тип контента)

**2. Диспетчер (`bin/control/control.py`)**
- Маршрутизация по типу контента
- Выбор соответствующего обработчика
- Оркестрация процесса

**3. Парсер (`bin/control/parser.py`)** ⭐ ЯДРО СИСТЕМЫ
- Загрузка постов из источников
- Применение всех фильтров
- Сортировка по приоритету

**4. Система фильтров (`bin/sort/`)**
- Фильтр по дате (старые посты)
- Фильтр по медиа (дубликаты фото/видео)
- Фильтр по тексту (дубликаты контента)

**5. Публикатор (`bin/rw/posting_post.py`)**
- Агрегация постов в дайджест
- Формирование финального текста
- Публикация через VK API

**6. Хранилище (`MongoDB`)**
- История опубликованного
- Конфигурации регионов
- Кэш обработанных постов

---

## 🔍 Детальная логика парсера (ЯДРО)

### Многоуровневая фильтрация

Парсер - это сердце системы. Он применяет фильтры последовательно, каждый следующий фильтр работает только с прошедшими предыдущие.

#### **Уровень 1: Быстрая отсечка**
```
Цель: Максимально быстро отбросить очевидно непригодные посты

Фильтры:
1. Проверка "отпечатка" (lip) - уже обрабатывали этот пост?
2. Проверка даты - пост не старше N часов?
3. Черный список ID - источник не в ban-list?
```

**Концепция "отпечатка" (lip):**
- Уникальный идентификатор поста: `owner_id + post_id`
- Хранится в MongoDB для каждого типа контента
- Мгновенная проверка дубликатов
- Автоматическая ротация (хранится последние N записей)

#### **Уровень 2: Структурная проверка**
```
Цель: Проверить наличие требуемых элементов

Фильтры:
1. Распаковка репостов - достать оригинальный контент
2. Повторная проверка отпечатка оригинала
3. Проверка наличия медиа (для тем типа "кино", "музыка")
4. Проверка длины текста (для "приколов" < 100 символов)
```

**Распаковка репостов:**
Если пост является репостом, система извлекает оригинальный контент через `clear_copy_history()`. Это критично, так как один и тот же контент может прийти как репост из разных источников.

#### **Уровень 3: Контекстные фильтры**
```
Цель: Применить специфичные для региона/темы правила

Примеры:
• Для новостей: фильтр по региональным ключевым словам
• Для "соседей": обязательно наличие хештега #Новости
• Для областных: проверка на авторство (только от администраторов группы)
• Для конкретных групп: исключение ссылок на их сайты
```

**Региональная специфика:**
Система понимает, что пост из группы "ВолейболвУржуме" релевантен для Уржума, но нужно проверить, что это не просто человек написал в открытую группу (проверка `owner_id != from_id`).

#### **Уровень 4: Дедупликация контента**
```
Цель: Избежать публикации семантически идентичного контента

Механизм:
1. Текст превращается в "рафинад" (text_to_rafinad)
   - Удаляются спецсимволы, пробелы, знаки
   - Приводится к нижнему регистру
   - Остается только суть текста

2. Берется "сердцевина" текста (20-70% длины)
   - Начало и конец могут различаться
   - Но суть должна быть уникальной

3. Сравнение с:
   - Уже опубликованными постами (последние 100)
   - Постами в очереди
   - Постами в "безфото" (объявления)
```

**Пример дедупликации:**
```
Оригинал 1: "🔥 В Малмыже пройдет концерт! 25 октября в ДК."
Оригинал 2: "25 октября в Малмыже концерт в ДК! Приходите все!"

После рафинада:
1: "малмыжепройдетконцерт25октябрявдк"
2: "25октябрявмалмыжеконцертвдкприходитевсе"

Сердцевина (20-70%):
1: "детконцерт25октября"
2: "рявмалмыжеконцертвд"

Результат: Дубликаты обнаружены, второй пост отброшен.
```

#### **Уровень 5: Фильтр медиа-дубликатов**
```
Цель: Не публиковать посты с уже использованными фото/видео

Проверки:
• Хеши фото (photo_id из VK)
• Хеши видео (video_id из VK)
• Хранение в MongoDB
```

#### **Уровень 6: Черный список слов**
```
Цель: Отфильтровать нежелательный контент

Проверка на:
• Запрещенные слова и фразы
• Спам-паттерны
• Рекламные конструкции
```

### Обработка постов без медиа ("безфото")

**Особая логика:**

Посты без фото/видео обрабатываются отдельно:
1. Если нет вложений ИЛИ нет просмотров → в категорию "безфото"
2. Более жесткая очистка текста (регулярные выражения)
3. Проверка длины: 30-250 символов
4. Накопление в отдельной коллекции
5. Периодическая публикация как дайджест объявлений

**Философия:** Короткие объявления без фото - это полезно, но не как отдельные посты. Лучше собрать 10-20 таких объявлений и опубликовать списком.

---

## 📦 Система сессий

### Концепция глобальной сессии

Вся система работает через один глобальный словарь `session`, который содержит:

**Конфигурацию:**
```python
session = {
    'name_base': 'mi',           # Текущий регион
    'name_session': 'novost',    # Тип контента
    'post_group_vk': -12345,     # Куда публиковать
    'timestamp_now': 1234567890,  # Текущее время
    'vk_app': <VK API объект>,   # Подключение к VK
    'MONGO_BASE': <MongoDB DB>,   # База данных
    ...
}
```

**Рабочие данные:**
```python
session['work'] = {
    'novost': {
        'lip': [список отпечатков],
        'hash': [список хешей медиа],
        ...
    },
    'bezfoto': {
        'lip': [объявления],
        ...
    }
}
```

**Региональные конфиги:**
```python
session['mi_words'] = ['Малмыж', 'Малмыжский']  # Ключевые слова региона
session['zagolovok'] = {
    'novost': '📰 НОВОСТИ\n',
    'kultura': '🎭 КУЛЬТУРА\n',
    ...
}
```

### Загрузка конфигурации

**Двухуровневая система:**

1. **Глобальный конфиг** (`config` база в MongoDB)
   - Общие настройки
   - Списки токенов
   - Правила фильтрации
   
2. **Региональный конфиг** (база региона в MongoDB)
   - Специфика региона
   - Источники контента
   - Целевые группы

**Пример запуска:**
```bash
python3 start.py mi_novost
#                ^^^^^^^^^^
#                |    └─ Тип контента (session)
#                └────── Регион (base)
```

Система разбирает аргумент и загружает:
- Сначала глобальный config
- Затем региональный config для "mi"
- Затем рабочую таблицу "novost"

---

## 🎨 Паттерны и подходы

### 1. Приоритизация по просмотрам

**Идея:** Больше просмотров = интереснее контент

После всех фильтров посты сортируются по количеству просмотров:
```python
result_posts.sort(key=lambda x: x['views']['count'], reverse=True)
```

Публикуется пост с максимальным engagement.

### 2. Агрегация новостей

**Проблема:** 5-10 новостей в день → спам в ленте  
**Решение:** Объединить несколько новостей в один пост

**Логика агрегации:**
```
1. Берется первая новость (самая просматриваемая)
2. Добавляется ее текст и медиа
3. Если есть место:
   - Добавляется вторая новость
   - Проверка: текст < MAX_SIZE и медиа < 10?
   - Добавляются еще новости
4. Получается дайджест из 2-5 новостей
```

**Пример результата:**
```
📰 НОВОСТИ

Завтра в Малмыже пройдет концерт...
https://vk.com/wall123_456 (Малмыж Инфо)

Открылась новая детская площадка...
https://vk.com/wall789_012 (Малмыж Сегодня)

#НовостиМалмыж
```

### 3. Атрибуция источников

**Философия:** Важно указывать первоисточник

Для каждого поста добавляется ссылка на оригинал и название источника:
```
{текст новости}
@wall12345_6789 (Название Источника)
```

Название источника:
1. Сначала ищется в конфиге (предзаданные источники)
2. Если не найдено → запрос к VK API за названием группы
3. Обрезается до 40 символов

**Исключение:** Если источник в черном списке (`bad_name_group`) - ссылка не ставится.

### 4. Режим "рулетка" для addons

**Проблема:** Есть 10+ типов доп.контента (кино, музыка, юмор и т.д.), но публиковать нужно только 1 в день

**Решение:** "Рулетка"
```
1. Есть массив тем: ['kino', 'music', 'prikol', 'photo', ...]
2. Случайное перемешивание
3. Попытка найти контент для первой темы
4. Если не нашли → следующая тема
5. До 5 попыток
6. Что первое найдется - то и публикуем
```

Это создает естественное разнообразие контента.

### 5. Умная работа с токенами

**Проблема:** VK токены могут истекать, иметь лимиты

**Решение:** Пул токенов
```python
session['names_tokens_read_vk'] = ['TOKEN1', 'TOKEN2', 'TOKEN3']

# При запуске
random.shuffle(session['names_tokens_read_vk'])
for token_name in session['names_tokens_read_vk']:
    if try_connect(token):
        break  # Нашли рабочий токен
```

Разные токены для разных операций:
- `names_tokens_read_vk` - чтение контента
- `names_tokens_post_vk` - публикация
- `names_tokens_dran_vk` - специально для региона "dran"

---

## 🔄 Типичные сценарии работы

### Сценарий 1: Публикация новостей (mi_novost)

```
1. Запуск: start.py mi_novost
2. Загрузка конфига regional 'mi' + session 'novost'
3. Чтение 20 последних постов из источников региона mi
4. Применение фильтров (80-90% постов отбрасывается)
5. Остается 2-5 подходящих постов
6. Сортировка по просмотрам
7. Агрегация в один пост-дайджест
8. Публикация в группу mi
9. Сохранение истории в MongoDB
```

### Сценарий 2: Репост рекламы (mi_repost_reklama)

```
1. Запуск: start.py mi_repost_reklama
2. Обработчик repost_reklama()
3. Чтение из специальной группы-источника рекламы
4. Простая проверка на дубликаты
5. Прямой репост (не копирование!) в целевую группу
```

**Почему репост, а не копирование?**
- Сохраняется оригинальное оформление
- Виден автор рекламы
- Можно связаться с рекламодателем

### Сценарий 3: Массовая рассылка (copy_to_all_setka)

```
1. Запуск: copy_to_all_setka.py (каждые 30 минут)
2. Чтение ОДНОГО последнего поста из ленты-источника
3. Проверка: пост не старше 30 минут?
4. Проверка текста: слово "репост" → режим репоста
5. Цикл по всем 13 региональным группам:
   - Публикация/репост
   - Задержка 15-30 секунд (защита от бана)
```

**Использование:** Срочные объявления, общие новости, поздравления

---

## 💾 Работа с MongoDB

### Структура базы данных

```
postopus (database)
  ├── config (collection) - глобальная конфигурация
  │   └── document: {title: 'config', ...настройки...}
  │
  ├── mi (collection) - регион Малмыж
  │   ├── document: {title: 'config', ...региональные настройки...}
  │   ├── document: {title: 'novost', lip: [...], hash: [...]}
  │   ├── document: {title: 'reklama', lip: [...]}
  │   └── document: {title: 'afisha', ...данные афиши...}
  │
  ├── dran (collection) - регион Дран
  │   └── ...аналогично...
  │
  └── ... (другие регионы)
```

### Паттерн работы

**Загрузка:**
```python
# Загрузить таблицу
table = load_table('novost')
# Возвращает: {'lip': [...], 'hash': [...], ...}
```

**Работа:**
```python
# Добавить новый отпечаток
session['work']['novost']['lip'].append(new_lip)
```

**Сохранение:**
```python
# Сохранить изменения
save_table('novost')
# Автоматически:
# - Обрезает списки до нужного размера
# - Делает upsert в MongoDB
```

### Ротация данных

Критически важно: списки `lip` и `hash` не должны расти бесконечно.

**Механизм:**
```python
while len(table['lip']) > MAX_SIZE:
    del table['lip'][0]  # Удалить самый старый
```

По умолчанию: MAX_SIZE = 30

**Почему 30?**
- Достаточно для отсечки дубликатов за 2-3 дня
- Не перегружает память
- Не замедляет проверки

---

## 🎯 Специализированные модули

### repost_me - "Меня репостнули"

**Идея:** Когда другие группы репостят твой контент

**Логика:**
1. Мониторинг репостов постов своей группы
2. Если кто-то репостнул → репостнуть их репост обратно
3. Взаимная поддержка сообществ

### sosed - "Соседние сообщества"

**Идея:** Новости из соседних регионов, но только релевантные

**Фильтр:** Обязательно наличие хештега `#Новости`

**Почему?** У соседних сообществ могут быть объявления, реклама, развлечения - это не нужно. Нужны только новости.

### oblast_novost - "Областные новости"

**Идея:** Новости всей Кировской области, но с региональной фильтрацией

**Механизм:**
```
1. Берутся посты из областных групп
2. Проверка на региональные ключевые слова
3. Если есть "Малмыж", "Малмыжский" → берем
4. Если нет → пропускаем
```

Это позволяет не пропустить важные областные новости, касающиеся региона.

---

## 🛡️ Защита от проблем

### 1. Защита от бана VK

**Проблема:** Слишком частые запросы → бан токена

**Решения:**
- Случайные задержки между постами (15-30 сек)
- Ротация токенов
- Лимиты на количество постов в день

### 2. Защита от спама

**Многоуровневая:**
- Черный список групп и пользователей
- Черный список слов и фраз
- Регулярные выражения для очистки
- Проверка длины текста
- Требование минимальных просмотров

### 3. Защита от падения

**Обработка ошибок:**
```python
for i in range(3):  # 3 попытки
    try:
        # Опасная операция
        break
    except:
        sleep(10)  # Ждем и повторяем
```

**Уведомления:**
- Ошибки отправляются в Telegram
- Логирование критичных моментов

### 4. Защита от дубликатов

**Многоуровневая:**
1. Отпечатки (lip) - быстрая проверка
2. Хеши медиа - дубликаты фото/видео
3. Рафинад текста - семантические дубликаты
4. Проверка истории публикаций

---

## 📊 Метрики и качество

### Показатели успеха системы

**Эффективность фильтрации:**
- Из 20 постов источников → 2-5 подходящих (~80-90% отсев)
- Дубликаты: < 1% от публикаций
- False negatives: ~5% (пропущенные хорошие посты)

**Операционные:**
- Время обработки: 10-30 секунд на запуск
- Частота запусков: каждый час (через cron)
- Uptime: 99.9% (падения крайне редки)

**Качество контента:**
- Релевантность: ~95% (по обратной связи от админов)
- Читабельность: высокая (автоматическая очистка)
- Разнообразие: 10+ типов контента

---

## 🔮 Архитектурные ограничения

### Что работает хорошо

✅ Фильтрация дубликатов - отлично  
✅ Работа с VK API - стабильно  
✅ MongoDB для хранения - быстро и надежно  
✅ Cron для планирования - просто и эффективно  

### Что можно было сделать лучше

⚠️ **Отсутствие AI/ML** - все фильтры rule-based  
⚠️ **Нет веб-интерфейса** - управление только через код  
⚠️ **Нет аналитики** - непонятно, какой контент работает лучше  
⚠️ **Жесткая конфигурация** - изменения требуют правки в БД  
⚠️ **Монолитная архитектура** - сложно масштабировать  
⚠️ **Один источник (VK)** - зависимость от одной платформы  

### Узкие места

🔴 **Производительность VK API**
- Лимиты запросов
- Необходимость задержек
- Зависимость от стабильности VK

🔴 **Качество очистки текста**
- Regex не всегда справляется
- Некоторый спам проскакивает
- Нет контекстного понимания

🔴 **Региональная специфика**
- Каждый регион требует ручной настройки
- Сложно добавлять новые регионы
- Много дублирования конфигов

---

## 💡 Ключевые инсайты для новой системы

### Философские

1. **Фильтрация - это искусство баланса**
   - Слишком строго → пропускаем хорошее
   - Слишком мягко → пропускаем мусор
   - Нужна гибкая настройка

2. **Дедупликация критична**
   - Пользователи ненавидят повторы
   - Но контент может слегка отличаться
   - Нужна семантическая дедупликация

3. **Контекст важнее контента**
   - Один и тот же пост может быть релевантен для одного региона и нет для другого
   - Источник важен (репутация)
   - Время публикации имеет значение

### Технические

1. **Глобальная сессия - антипаттерн**
   - Хорошо для маленькой системы
   - Плохо для масштабирования
   - Нужно dependency injection

2. **MongoDB как конфиг - работает**
   - Быстро читается
   - Легко обновляется
   - Но нужен UI для управления

3. **VK API - надежный, но ограниченный**
   - Стабильный, но медленный
   - Лимиты запросов
   - Нужно кэширование

### Паттерны

1. **Pipeline фильтрации**
   - Каждый фильтр - отдельная функция
   - Композиция фильтров
   - Легко добавлять новые

2. **Отпечатки для дедупликации**
   - Быстро и эффективно
   - Легко расширяемо (разные типы отпечатков)
   - Работает для любых объектов

3. **Агрегация контента**
   - Объединение похожего контента
   - Снижение спама
   - Лучший UX для читателей

---

**Продолжение:** См. CORE_CONCEPTS.md для углубления в концепции →

