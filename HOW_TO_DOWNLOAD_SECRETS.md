# Как скачать файл с секретами

## 🔐 ВАЖНО!

Файл `SECRETS_BACKUP.txt` содержит **ВСЕ критичные секреты** проекта SETKA:
- VK API токены
- Telegram bot токены  
- Пароли баз данных
- API ключи
- Учетные данные

**Этот файл НЕ в Git** (защищен .gitignore) ✅

---

## 📥 Способы скачивания

### Способ 1: SCP (Рекомендуется)

```bash
# С вашего локального компьютера:
scp valstan@3931b3fe50ab.vps.myjino.ru:/home/valstan/SETKA/SECRETS_BACKUP.txt ~/Downloads/

# Затем переместите в безопасное место:
mv ~/Downloads/SECRETS_BACKUP.txt ~/Documents/Secure/SETKA_SECRETS.txt
```

### Способ 2: SFTP

```bash
# Подключиться по SFTP
sftp valstan@3931b3fe50ab.vps.myjino.ru

# В SFTP консоли:
cd /home/valstan/SETKA
get SECRETS_BACKUP.txt
exit
```

### Способ 3: Через cat (копировать вручную)

```bash
# На сервере:
cat /home/valstan/SETKA/SECRETS_BACKUP.txt

# Скопируйте вывод и сохраните локально
```

### Способ 4: Создать архив

```bash
# На сервере:
cd /home/valstan/SETKA
tar -czf secrets_backup_$(date +%Y%m%d).tar.gz SECRETS_BACKUP.txt config/config_secure.py

# Затем скачать:
scp valstan@3931b3fe50ab.vps.myjino.ru:/home/valstan/SETKA/secrets_backup_*.tar.gz ~/Downloads/

# На сервере удалить архив:
rm secrets_backup_*.tar.gz
```

---

## 🔒 Безопасное хранение

### После скачивания:

1. **Сразу удалите файл из Downloads:**
   ```bash
   mv ~/Downloads/SECRETS_BACKUP.txt ~/Documents/Secure/
   ```

2. **Создайте зашифрованный архив:**
   ```bash
   # MacOS/Linux:
   zip -e SETKA_SECRETS.zip SETKA_SECRETS.txt
   # Введите надежный пароль
   
   # Или с 7zip:
   7z a -p SETKA_SECRETS.7z SETKA_SECRETS.txt
   ```

3. **Храните в безопасном месте:**
   - Password manager (1Password, Bitwarden)
   - Зашифрованный USB
   - Облако с 2FA (Google Drive, Dropbox)
   - Не в открытом виде!

4. **Создайте резервную копию:**
   - На другом устройстве
   - В другом облаке
   - У надежного человека (зашифрованная)

---

## ⚠️ Что НИКОГДА не делать:

❌ **НЕ коммитить** в Git  
❌ **НЕ отправлять** по email  
❌ **НЕ публиковать** на форумах/чатах  
❌ **НЕ хранить** в облаке без шифрования  
❌ **НЕ оставлять** в Downloads/Desktop  

---

## 🔄 Ротация секретов

Если секреты **скомпрометированы** (случайно опубликованы):

### 1. Telegram Bot токены:
```
1. Откройте @BotFather в Telegram
2. /mybots → Выберите бота → API Token → Revoke current token
3. Скопируйте новый токен
4. Обновите config/config_secure.py
5. Обновите SECRETS_BACKUP.txt
```

### 2. VK токены:
```
1. Откройте https://vk.com/apps?act=manage
2. Удалите старый токен
3. Создайте новый с нужными правами
4. Обновите config/config_secure.py
5. Обновите SECRETS_BACKUP.txt
```

### 3. PostgreSQL пароль:
```bash
sudo -u postgres psql
ALTER USER setka_user WITH PASSWORD 'NewSecurePassword2025!';
\q

# Обновите везде новый пароль
```

### 4. Groq API:
```
1. Откройте https://console.groq.com
2. Удалите старый API key
3. Создайте новый
4. Обновите config/config_secure.py
```

---

## ✅ Чеклист безопасности

После скачивания файла:

- [ ] Файл скачан на локальный компьютер
- [ ] Файл перемещен из Downloads в безопасное место
- [ ] Создана зашифрованная резервная копия
- [ ] Создана вторая резервная копия (на другом носителе)
- [ ] Проверено что файл НЕ в облаке (без шифрования)
- [ ] Проверено что файл НЕ в Git (git status)
- [ ] Добавлено напоминание обновить при изменении секретов

---

## 📝 Обновление файла с секретами

Когда добавляете новые секреты:

```bash
# На сервере:
cd /home/valstan/SETKA
nano SECRETS_BACKUP.txt

# Добавьте новые секреты
# Сохраните (Ctrl+O, Enter, Ctrl+X)

# Скачайте обновленный файл
scp valstan@3931b3fe50ab.vps.myjino.ru:/home/valstan/SETKA/SECRETS_BACKUP.txt ~/Secure/
```

---

## 🎯 Местоположение файлов с секретами

**На сервере (не в Git):**
```
/home/valstan/SETKA/
├── SECRETS_BACKUP.txt          ← Резервная копия всех секретов
├── config/config_secure.py     ← Рабочий конфиг (импортируется кодом)
└── .env                        ← Переменные окружения (опционально)
```

**В Git (публично):**
```
.gitignore                      ← Защищает секреты
.env.example                    ← Шаблон для .env
config/__init__.py              ← Только структура
README.md                       ← Без секретов
```

---

## 🔍 Проверка безопасности

**Убедитесь что секреты не в Git:**

```bash
cd /home/valstan/SETKA

# Проверить .gitignore
git check-ignore -v SECRETS_BACKUP.txt
git check-ignore -v config/config_secure.py

# Проверить что не staged
git status | grep -i secret

# Поиск секретов в tracked files
git grep "vk1.a." || echo "VK tokens not found ✅"
git grep "489021673\|5945194659" || echo "TG tokens not found ✅"
git grep "SetkaSecure2025" || echo "DB password not found ✅"
```

---

**Создано:** 9 октября 2025  
**Статус:** Файл защищен, готов к скачиванию  

📥 **Скачайте файл прямо сейчас и сохраните в безопасном месте!**

