# Зависимости MCP-обёртки VK-шлюза SARAFAN (consumer-side).
# Ставятся в окружении ПОТРЕБИТЕЛЯ, НЕ в SARAFAN (там mcp не нужен —
# server.py никогда не импортируется рантаймом/тестами SARAFAN).
mcp>=1.2.0
httpx>=0.27.0
