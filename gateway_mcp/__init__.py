"""MCP-обёртка VK-шлюза SARAFAN (consumer-side).

Пустой пакет-маркер. ``client`` зависит только от httpx (тестируется без MCP SDK);
``server`` дополнительно требует пакет ``mcp`` (см. requirements.txt). Импорт
этого пакета НЕ тянет ``server`` — чтобы тесты/линт работали без установленного
``mcp`` в окружении SARAFAN.
"""
