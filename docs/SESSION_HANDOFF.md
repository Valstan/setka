# Session Handoff

> Sticky-note для непрерывности между сессиями разработки SETKA. Перезаписывается через [`/close_session`](../.claude/commands/close_session.md) — историю смотри через `git log --follow -- docs/SESSION_HANDOFF.md`.

**Status:** IDLE (все нитки сессии смержены+задеплоены; активной код-задачи нет)
**Updated:** 2026-07-16
**Branch:** main
**Last release in prod:** код HEAD **`430244d`** (#354). Прод трогали этой сессией:
env-правки (`COLLECTION_AUDIT_REGION_CODES=` пусто → аудит по всем регионам;
`CLASSIFIER_PENDING_MAX=200`; бэкап `/etc/setka/setka.env.bak-20260716`),
деплой #351/#352/#353/#354, restart web×4 / worker×2. Health 200, все active.

---

## Текущая нитка

_Нет активной код-нитки._ Сессия 16.07 — мультирегион классификатора по заказу
владельца («нейронка видит только Малмыж»). Всё смержено и задеплоено:

- **#351** — снапшот 12 выученных правил захвачен в git; `/publisher` UI →
  `create_with_policy` (БД-роутер токенов).
- **Корневая причина моно-региона:** обкаточный гейт `COLLECTION_AUDIT_REGION_CODES=mi`
  в прод-env (источник `/pending` = аудит сбора) — снят; волна 12:40 дала аудит
  по 16 регионам (~1050 постов/волна).
- **#352** — регион-скоупный enforce (вердикт блокирует lip только в СВОЁМ
  регионе), fair round-robin батч блоками по регионам, преамбула постулатов
  «наш район = region_code поста», диагностика `GET /api/classifier-review/health`.
- **#353** — панель «Здоровье рутины» на `/classifier` (coverage %, backlog
  по регионам, вердикты/24ч).
- **#354** — толерантный per-item приём вердиктов (422 на весь батч из-за
  одного перелимита выбрасывал целый прогон рутины — найден в прод-логе);
  `DEFAULT_LIMIT` скрипта рутины 40→200 (рутина слала явный `?limit=40`,
  серверный потолок сам по себе не работал).
- Удалена осиротевшая `~/.claude/scheduled-tasks/setka-hitl-classifier/`
  (ingest-ключ открытым текстом) на дневной машине.
- **Письмо мозгу** `mailbox/to-brain/2026-07-16-lenient-ingest-and-pilot-gate-leftover.md` (#009).

## Следующий шаг

Активной код-задачи нет. Кандидаты:

1. **🟡 Watch (1-2 дня): успевает ли рутина за мультирегиональным потоком** —
   смотреть панель «Здоровье рутины» на `/classifier` (или
   `service.health_stats` на боксе). На закрытии сессии: collected 1686 /
   classified 206 / coverage 12%. С фиксом 422 + батчем 200 должно расти к
   80-90%. Заодно проверить аномалию каденса: вердикты приходили раз в ~2ч
   по 40, хотя рутина cron-ится каждый час в :22 — историю прогонов смотреть
   владельцу на claude.ai.
2. **Owner-gated:** перевыпуск community-токенов (сейчас ВСЁ публикуется через
   fallback МАМА — единственная точка отказа, см. PENDING); ротация
   `CLASSIFIER_INGEST_KEY`; чистка `VK_TOKEN_*` из env; вечерняя машина —
   удалить `~/.claude/scheduled-tasks/setka-hitl-classifier/` если есть.
3. **🟢 Enforce v2 / Claude API** — снимет лимиты рутины совсем.

## Контекст

- **План:** нет активного.
- **Связанные коммиты сессии:** `20e7893` (#351), `a67c12c` (#352),
  `b67d14e` (#353), `430244d` (#354).
- **Прод:** все сервисы active, health 200, HEAD `430244d` = main. Классификатор:
  все 16 регионов в потоке, enforce регион-скоупный, батч ≤200.
  ⚠️ Все community-токены отвечают error 10 (заморожены с баном VALSTAN) —
  каждый wall.post уходит в fallback МАМА (единственный кандидат каскада).
- **Открытых PR:** только закрывающий handoff-PR (doc-only, авто-merge).

## Открытые вопросы для пользователя

- Каденс рутины: почему вердикты раз в ~2ч при часовом cron — глянуть историю
  прогонов на claude.ai (там же расход токенов).
- Перевыпуск community-токенов: когда появится живой админ вместо VALSTAN?

## Не забыть (low-priority)

- 🟡 Watch coverage классификатора (см. «Следующий шаг» п.1) + PENDING-запись.
- 🟡 МАМА — единственный publish-токен (PENDING).
- 🟢 env cleanup токенов/ключей шлюза (owner-confirm).
- 🛠 git push на этой машине: HTTP/2+schannel флапает — обход `git -c http.version=HTTP/1.1 push`.

---

> Если читаешь это в начале новой сессии — обнови через `/close_session` в конце. **Не аккумулируй history тут** — она в `git log --follow -- docs/SESSION_HANDOFF.md`.
