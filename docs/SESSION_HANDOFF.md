# Session Handoff

> Sticky-note для непрерывности между сессиями разработки SETKA. Перезаписывается через [`/close_session`](../.claude/commands/close_session.md) — историю смотри через `git log --follow -- docs/SESSION_HANDOFF.md`.

**Status:** IDLE
**Updated:** 2026-06-07
**Branch:** main
**Last release in prod:** прод на `20b532c` — все 7 PR сессии задеплоены. 3/3 active, health 200, миграция 033 применена.

---

## Текущая нитка

_Нет — за сессию 2026-06-07 закрыто и задеплоено 7 задач (см. ниже). Открытая стартовая позиция._

Сделано в сессии (по жалобе владельца + data-driven находки):
1. **[#173](https://github.com/Valstan/setka/pull/173) `55bc1e4`** — copy-setka: сеть размножения постов встала ~28.05. Источник `copy_by_setka` — частная группа (is_closed=2); код брал случайный parse-токен, попадал на не-участника (VITA) → VK error 15 → 0 репостов. Фикс: перебор токенов до читаемого (VALSTAN — участник). Подтверждено живьём.
2. **[#174](https://github.com/Valstan/setka/pull/174) (squashed)** — copy-setka: добор captcha'д-хвоста через тики + интервал между репостами. Успех 10/16→14/16, kirov_obl снова доставляется; недоставленные пишутся в pending (`WorkTable.hash`), не теряются.
3. **[#175](https://github.com/Valstan/setka/pull/175) `7c32455`** — /tokens: реальные счётчики Главные/Вспомогательные (были зашиты в 0). `compute_token_stats` + `GET /api/tokens/stats`. Прод: main:2/aux:17/broken:5/total:24.
4. **[#176](https://github.com/Valstan/setka/pull/176) `4e76141`** — AI-дедуп новостей: probe показал, что нейро-embeddings не влезают в VPS (1.5ГБ/1 ядро/swap=0) → **отложено владельцем до апгрейда VPS**. Зафиксировано в PENDING (RAM-потолок, спящий SimHash, seam).
5. **[#177](https://github.com/Valstan/setka/pull/177) `68585ca`** — рост подписчиков: учёт сужен с ~840 сообществ до 16 главных ИНФО-групп регионов (×50 экономия VK API). Миграция 033 (DROP community_member_snapshots → region_member_snapshots). 16 регионов засеяны.
6. **[#178](https://github.com/Valstan/setka/pull/178) `132e173`** — деплой: `scripts/wait_for_health.py` (поллинг health вместо одиночного curl) — убирает ложный health-000 на тонком VPS. `/reliz` Шаг 8/9 переведён на поллер.
7. **[#179](https://github.com/Valstan/setka/pull/179) `20b532c`** — DM-роутер ЛС был молча сломан с деплоя (#164): UPSERT `index_where=Model.col=="x"` рендерился bind-параметром → Postgres не матчил частичный индекс → каждое входящее ЛС терялось. Фикс `text("origin = 'inbound_dm'")`. **412 потерянных ЛС восстановлено** (367 → уведомления, 45 → кабинет).

## Следующий шаг

Активной нитки нет. Кандидатные стартовые точки (приоритет — за владельцем):

1. **Владельцу: разобрать инбокс** — после фикса #179 в `/ad-cabinet` + `/notifications` появились **412 восстановленных ЛС** (367 не-рекламных, 45 реклама). Пользовательский шаг (триаж/ответы).
2. **Браузер-верификация `/subscriber-growth`** — кривые роста главных групп появятся с **2026-06-08** (нужен второй суточный снимок 04:00 MSK; сейчас 1 точка/регион).
3. **R4-R5 in-app переписка ЛС** — теперь есть живые данные в очереди (см. п.1).
4. **Освежение следующего региона** по [`docs/REGION_REFRESH_LOG.md`](REGION_REFRESH_LOG.md) (берём верх таблицы приоритета).
5. **Smoke tuzha** (discovery итерация 3, PENDING ⏳) — пользовательский шаг.

## Контекст

- **План:** нет активного файла-плана; roadmap'ы — в `PENDING_FOLLOWUPS.md`.
- **Связанные коммиты сессии:** `55bc1e4` `#173`, `#174`(squash), `7c32455` `#175`, `4e76141` `#176`, `68585ca` `#177`, `132e173` `#178`, `20b532c` `#179`.
- **Прод:** HEAD `20b532c`, 3/3 active, health 200, миграция 033 применена. Деплои сессии: copy-setka (worker), web (counters), миграция 033 (web/worker/beat), DM-fix (worker).
- **Открытых PR:** нет (все 7 смержены; handoff-PR этой сессии — отдельно).
- **Brain mailbox:** прочитано письмо 2026-06-07 (`batch-pooled-027-inbox-monitoring`, low/suggest, «ответа не жду» — Мозг разложил находки в G30/R5/R6/G19). Отправлены 2 находки: `mailbox/to-brain/2026-06-07-ai-dedup-probe-vps-blocked.md` (#176), `2026-06-07-sqlalchemy-partial-index-onconflict-gotcha.md` (#179).

## Открытые вопросы для пользователя

- Следующая нитка: триаж восстановленных ЛС, освежение региона, R4-R5, tuzha smoke — или иное?

## Не забыть (low-priority)

- ⏳ **copy-setka pending-добор:** `bal` + `tatarstan_obl` были недоставлены на тике 09:37 (#174) — проверить по логу воркера, что добрались на следующих тиках (`grep 'добор pending\|разослан частично\|доставлен всем' celery-worker.log`).
- 🟢 **`/subscriber-growth` кривые** — смотреть с 2026-06-08 (второй снимок).
- ⏸ **AI-дедуп новостей** — отложен до апгрейда VPS (нужен ≥4ГБ RAM для local embeddings); путь и спящий SimHash зафиксированы в PENDING.
- ⚠️ **VPS тонкий** (1.5ГБ / 1 ядро / swap=0): при деплое web ждать готовности поллером (`scripts/wait_for_health.py`, #178), не рестартить вслепую; рестарт 3 сервисов разом даёт долгий старт web.

---

> Если читаешь это в начале новой сессии — обнови через `/close_session` в конце. **Не аккумулируй history тут** — она в `git log --follow -- docs/SESSION_HANDOFF.md`.
