# Session Handoff

> Sticky-note для непрерывности между сессиями разработки SETKA. Перезаписывается через [`/close_session`](../.claude/commands/close_session.md) — историю смотри через `git log --follow -- docs/SESSION_HANDOFF.md`.

**Status:** IDLE (все нитки сессии смержены+задеплоены; активной код-задачи нет)
**Updated:** 2026-07-14
**Branch:** main
**Last release in prod:** код HEAD **`97e000f`** (#349). Прод трогали этой сессией:
миграция 060 (`collected_post_audit.media`), деплой правок базовых правил (#346) +
кнопки правки правил (#349), restart web×3 / worker×2. Прод-DML (под подтверждением
владельца): rule id=18 переформулировано под медиа-зрение, 10 поглощённых/дублей
правил → `retired` (12 действующих осталось). Health 200, все сервисы active.

---

## Текущая нитка

_Нет активной код-нитки._ Сессия 14.07 (вечер) целиком про HITL-классификатор:
дистилляция переведена в dev-чат, дан «медиа-зрение», почищен слой правил. Всё
смержено и задеплоено. Владелец выполнил свою часть (см. ниже). Открытые нитки owner-gated.

Сделано этой сессией (всё смержено + задеплоено):
- **#345** — `/distill`: облачная рутина-дистиллятор упразднена, дистилляция
  Корпуса из dev-чата по запросу. Скилл + журнал `docs/ops/DISTILL_LOG.md` +
  напоминание в `/start` (шаг 3.2, > 7 дней). Термин «Корпус».
- **#346** — первая дистилляция: 178 коррекций → 5 правил + фикс противоречий
  базовых правил (`config/classification_postulates.md` правила 2 и 8).
- **#347** — медиа-зрение: посты без текста читаются по фото/PDF (аудит сбора
  снимает вложения → `collected_post_audit.media`, миграция 060; media-прокси
  `/api/classifier/media`; вердикт с `media_summary`; бейдж «📷 ИИ увидел» в ленте).
- **#349** — кнопка ✏️ правки текста у утверждённых правил в панели (был только 🗑).
- **Прод-DML (owner-confirm):** rule 18 → «пост без текста: читаемые фото/PDF →
  классифицировать по содержимому, иначе delete»; retired 10 правил (1,2,4,9,10,
  11,14,15,16,17) как дубли/поглощённые новыми 18-22. Осталось 12 approved.
- **Владелец выполнил (claude.ai):** обновил промпт рутины-классификатора
  (media-шаг), удалил рутину-дистиллятор, утвердил 5 черновиков правил.
- **Письмо мозгу** `mailbox/to-brain/2026-07-14-cloud-routine-patterns.md` (рефлекс #009).

## Следующий шаг

Активной код-задачи нет. Кандидаты (по приоритету):

1. **🟢 Захват снапшота выученных правил в git** (не срочно, при след. касании
   нитки классификатора): beat пересобрал `logs/classifier_learned_rules_snapshot.md`
   на проде после чистки — захватить в репо шагом dev-сессии (ops-док §«Снапшот
   выученных правил», `ssh setka "cat …snapshot.md" > config/…snapshot.md` + PR).
2. **🟢 Токен-бэклог (автономно):** вычистить `VK_TOKEN_*`/`GATEWAY_KEY_*` из env
   (БД-путь обкатан); `web/api/publisher.py` → policy.
3. **🟡 Ротация `CLASSIFIER_INGEST_KEY`** (засветился в чате 14.07, риск низкий).
4. **Owner-gated:** VK-шлюз v2 write, Радар-ID round-trip с trener, Этап 2 сайта.

## Контекст

- **План:** нет активного.
- **Связанные коммиты сессии:** `97e000f` (#349 кнопка правки), `d1a5a7e` (#347
  медиа-зрение), `8900242` (#346 дистилляция+фикс), `80aa551` (#345 /distill).
- **Прод:** все сервисы active, health 200, HEAD `97e000f`. 12 действующих правил
  классификатора; медиа-зрение работает для постов, собранных с 14.07 (старые — `media` пустой).
- **Открытых PR:** только закрывающий handoff-PR (doc-only, авто-merge).

## Открытые вопросы для пользователя

- **VK-шлюз v2 write** — какие VK-методы записи разрешить, per-key scope? Не решено.
- **Reklama-агрегаторы** — отказываться ли от ре-трансляции чужих агрегаторов целиком?

## Не забыть (low-priority)

- 🟢 Захват снапшота выученных правил в git (см. «Следующий шаг» п.1).
- 🟢 env cleanup токенов/ключей шлюза (БД-путь обкатан).
- 🟢 Прежние `sleeping` из deadcode (не удалять): `classifier/schema.py::has_merge_signal`,
  `radar_id/keys.py::keys_available`.
- 🛠 git push на этой машине: HTTP/2+schannel флапает — обход `git -c http.version=HTTP/1.1 push`.

---

> Если читаешь это в начале новой сессии — обнови через `/close_session` в конце. **Не аккумулируй history тут** — она в `git log --follow -- docs/SESSION_HANDOFF.md`.
