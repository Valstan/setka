# Session Handoff

> Sticky-note для непрерывности между сессиями разработки SETKA. Перезаписывается через [`/close_session`](../.claude/commands/close_session.md) — историю смотри через `git log --follow -- docs/SESSION_HANDOFF.md`.

**Status:** IDLE
**Updated:** 2026-06-06
**Branch:** main
**Last release in prod:** прод на `6abcd62` — директива brain «единый роутер входящих ЛС» полностью реализована и задеплоена round-trip'ом: Этап 1 (#164, миграция 032 + restart web/worker/beat) и Этап 2 (#165, restart web). 3/3 active, health 200, новые эндпоинты 200, бэкфилл миграции проверен (23 строки → ad_cabinet/new, ничего не уехало).

---

## Текущая нитка

_Нет — директива brain `2026-06-06-community-messages-router-and-inbox` закрыта целиком (R1–R5), задеплоена и проверена._ Открытая стартовая позиция.

В сессии 2026-06-06 сделано (2 PR + 2 деплоя + VK-probe):
1. **Этап 1 (R1–R3)** ([#164](https://github.com/Valstan/setka/pull/164)) — багфикс потери не-рекламных ЛС. Корень оказался архитектурным (markAsRead в коде нет): раздел «Уведомления» показывал живой VK unread-счётчик и ничего не хранил; ad-кабинет хранил только рекламу → не-рекламное ЛС жило лишь как VK-флаг unread и терялось. Фикс: `dm_scanner` persist'ит **каждое** входящее ЛС в `ad_requests` до классификации (миграция 032: `route`/`handling_status`/`handled_at`); «Уведомления» показывают не-рекламные ЛС из БД (блок «Входящие ЛС сообществ»); кнопки переноса в обе стороны; UPSERT переоткрывает диалог при новом входящем.
2. **VK-capability-probe (R4)** ([#165](https://github.com/Valstan/setka/pull/165), `scripts/probe_community_dm_capabilities.py`) — probe-before-build (#020): community-токен читает историю (`getHistory`) и отвечает (`messages.send`); `markAsUnread`-эквивалента нет. Capability зелёная.
3. **Этап 2 (R4–R5)** ([#165](https://github.com/Valstan/setka/pull/165)) — in-app ответ из «Уведомлений» (→`handling_status=done`) + тред переписки; нитка = строка `ad_requests` по `(community, peer)`, новое входящее переоткрывает её. Только фронт, бэкенд переиспользован.

## Следующий шаг

Активной нитки нет. Кандидатные стартовые точки (приоритет — за владельцем):

1. **Браузер-верификация владельцем** свежих фич `/notifications`: не-рекламное ЛС остаётся в «Входящих ЛС сообществ»; «Ответить» из приложения доходит; «Переписка» показывает тред; «Не реклама → в уведомления» / «Это реклама → в кабинет»; «Обработано». Первые живые не-рекламные ЛС появятся со скана X:05/35.
2. **График подписчиков — шаг 3 (UI)**: ждёт накопления снимков (`community_member_snapshots`, копятся 04:00 MSK) — проверить через неделю-две и строить мульти-line Chart.js.
3. **Фазы 4/5 рекламного кабинета**: ML-классификатор за интерфейсом `classifier.classify`; авто-правила/follow-up/аналитика воронки.
4. **AI-дедуп новостей** — отложен; путь без бюджета: локальные embeddings (CPU).

## Контекст

- **План:** нет активного файла-плана; roadmap'ы — в `PENDING_FOLLOWUPS.md`.
- **Связанные коммиты сессии (все на проде `6abcd62`):**
  - `e12fa3a`/[#164](https://github.com/Valstan/setka/pull/164) — Этап 1: единый роутер входящих ЛС (persist-before-classify + свой статус).
  - `6abcd62`/[#165](https://github.com/Valstan/setka/pull/165) — Этап 2: in-app ответ + тред переписки + probe-скрипт.
- **Прод:** HEAD `6abcd62`, 3/3 active, health 200. Миграция 032 применена. ~1031 тест зелёный на main.
- **Открытых PR:** doc-only handoff-PR этого `/close_session` (авто-merge). Кодовых открытых PR нет.
- **Brain:** ack (#164) + probe-result (#165) + finding-письмо (этот handoff) — все в `mailbox/to-brain/`, на origin.

## Открытые вопросы для пользователя

- Следующая нитка: браузер-верификация ЛС-роутера, график подписчиков (ждёт данных), фазы 4/5 кабинета, AI-дедуп, или иное?

## Не забыть (low-priority)

- 🟢 **Браузер-верификация ЛС-роутера** (`/notifications` блок «Входящие ЛС сообществ») — на проде нет живых не-рекламных ЛС до ближайшего скана X:05/35.
- 🟢 **«Какой именно вызов метит read»** — probe не воспроизвёл (на группе было 0 непрочитанных). Неважно (Этап 1 устойчив независимо), но при живом непрочитанном можно добить read-only через `scripts/probe_community_dm_capabilities.py`.
- ⚠️ **VPS периодически таймаутил SSH** в этой сессии (порт 49237), восстанавливался за ~10-20с — на прод не влияло, при деплое закладывай retry.

---

> Если читаешь это в начале новой сессии — обнови через `/close_session` в конце. **Не аккумулируй history тут** — она в `git log --follow -- docs/SESSION_HANDOFF.md`.
