# Session Handoff

> Sticky-note для непрерывности между сессиями разработки SETKA. Перезаписывается через [`/close_session`](../.claude/commands/close_session.md) — историю смотри через `git log --follow -- docs/SESSION_HANDOFF.md`.

**Status:** IDLE (обе нитки сессии доведены до конца; активной код-задачи нет)
**Updated:** 2026-07-19
**Branch:** main
**Last release in prod:** код HEAD **`2254297`** (#368). Прод этой сессией: миграции
**068**, **071** (батчи регионов), **072** (`oauth_clients.branding`); env
`SESSION_COOKIE_DOMAIN=.xn--80adkdyec4j.xn--p1ai`; nginx-блок
`/etc/nginx/conf.d/radar_feed.conf` (радар.вмалмыже.рф, вне git); restart web ×2.
Все сервисы active, health 200. `3ca320a` на main — письмо мозгу, деплоя не требует.

---

## Текущая нитка

_Нет активной код-нитки._ Сессия 18–19.07 закрыла две:

1. **Экспансия на Кировскую область — скелетная фаза ЗАВЕРШЕНА ЦЕЛИКОМ.** Батч №8
   (#365: Омутнинск 34 / Кирс 27 / Афанасьево 25) и финальный батч №9 (#366:
   Подосиновец 24 / Опарино 23 / Луза 21). Итого **27 новых районов** (миграции
   061-068, 071), пулы засеяны нейро-разбором сканов, все регионы `is_active=false`.
2. **Единый вход экосистемы — сторона setka закрыта.** #367 (домен
   радар.вмалмыже.рф + расшаренная кука `.вмалмыже.рф`) и #368 (страница входа
   узнаёт сервис: «Войти в <Сервис>» со значком/цветом из `oauth_clients.branding`,
   миграция 072; кнопка входа через ВК; фикс OIDC round-trip для radar-роли).
   Письмо мозгу (#369) — просьба разослать Сабантую/Гоньбе директиву подключиться
   OIDC-клиентами.

## Следующий шаг

Активной код-задачи нет. Кандидаты по приоритету:

1. **⏸ Активация районов — триггер владельца (с 20.07, т.е. со дня после этой сессии):**
   как только владелец начнёт создавать VK-группы «- ИНФО» (порциями 2-4/день с
   основного аккаунта после разбана VALSTAN) — dev-сессия активации:
   `UPDATE regions SET vk_group_id=-<id>, is_active=true`, прописать `neighbors`,
   community-токены `COMM_<id>` через `/tokens`, проверить вход в волны сбора +
   классификатор, smoke первой сводки. Детали — PENDING §«Экспансия».
2. **⏳ Единый вход — ждём соседей:** когда brain/проекты ответят с redirect_uri +
   брендингом → зарегистрировать клиентов (`scripts/register_oidc_client.py`),
   положить `branding` в БД, отдать секреты через root-файлы.
3. **🟡 Watch coverage классификатора** — `/api/classifier-review/health` (backlog,
   coverage_pct) после мультирегиона; и **🟡 МАМА — единственный publish-токен**
   (ждёт живого админа / разбана VALSTAN).

## Контекст

- **План:** нет активного.
- **Связанные коммиты сессии:** `3e401b4` (#365 батч 8), `0b7973c` (#366 батч 9 финал),
  `f386caf` (#367 домен радара + кука), `2254297` (#368 брендированный единый вход),
  `3ca320a` (#369 письмо мозгу).
- **Прод:** HEAD `2254297`, все сервисы active, health 200. Внешний smoke зелёный:
  `https://радар.вмалмыже.рф/` → 302 `/radar` → 302 `/login?next=/radar` («📡 Войти в
  Радар»); `вход.вмалмыже.рф/login` → «🌿 Войти в Сервисы Малмыжа»; с
  `next=/oidc/authorize?client_id=trener` → «⚽ Войти в ТРЕНЕР».
- **Открытых PR:** только закрывающий handoff-PR (doc-only, авто-merge).

## Открытые вопросы для пользователя

- Брендинг для Сабантуя/Гоньбы (название на кнопке, значок, цвет, слоган) — когда
  проекты подключатся; можно задать заранее, положу в БД при регистрации клиента.
- Перевыпуск community-токенов: когда появится живой админ вместо VALSTAN?

## Не забыть (low-priority)

- 🟡 Пробелы пулов батчей №8-9 (afanasyevo без admin/sport, verhnekame без detsad,
  oparino без reklama, sosed 0 по обеим тройкам) — добор локальным знанием.
- 🟡 Ротация `CLASSIFIER_INGEST_KEY` (засветился в чате 2026-07-14) при следующем касании.
- 🟢 env cleanup `VK_TOKEN_*` (owner-confirm); nginx-блок Радара — упростить ACME-location.
- 🛠 git push на этой машине: HTTP/2+schannel флапает — обход `git -c http.version=HTTP/1.1 push`.
- 🛠 Джино: клики по координатам в auth.jino.ru не срабатывали — работать через
  `read_page`/`ref_N` (form_input + click по ref).

---

> Если читаешь это в начале новой сессии — обнови через `/close_session` в конце. **Не аккумулируй history тут** — она в `git log --follow -- docs/SESSION_HANDOFF.md`.
