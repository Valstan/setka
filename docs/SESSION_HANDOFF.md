# Session Handoff

> Sticky-note для непрерывности между сессиями разработки SETKA. Перезаписывается через [`/close_session`](../.claude/commands/close_session.md) — историю смотри через `git log --follow -- docs/SESSION_HANDOFF.md`.

**Status:** ACTIVE (standing-нитки ждут владельца; новая развилка по аудио-модулю ждёт его выбора; mid-flight кода нет)
**Updated:** 2026-06-22
**Branch:** main
**Last release in prod:** прод HEAD `1925005` (#264, картинки рассылки). Все сервисы active, health 200 (probe 2026-06-22). **Прод-правка nginx вне git в силе:** `client_max_body_size 20m` на уровне `http`.

---

## Текущая нитка

Сессия 2026-06-22 точечная: (1) ответил brain на ad-CRM эмпирический чек-поинт — **закрыто** (PR [#266](https://github.com/Valstan/setka/pull/266) смержен); (2) исследовал по запросу владельца **модуль аудио → текст в Сарафане** — дошли до развилки архитектуры, **кода нет**, ждём выбор владельца.

Параллельно живы прежние standing-нитки (ждут наружу-проверок владельца): картинки «Сетевой рассылки» (#264), спот-чек 19 LLM-флагов, «Радиоточка» (VK-личка / `@ASupersharij`).

## Следующий шаг

1. **Аудио-модуль (если владелец вернётся к теме):** он выбирает архитектуру **B** (локальный помощник, лучшее качество GigaAM, без нагрузки на сервер — рекомендован под 2 машины) или **C** (в браузере WASM, ноль установки, качество Whisper-small) → расписать конкретный план. Все установленные факты (Telegram bot-to-bot запрещён; SaluteSpeech Freemium закрывается 15.07.2026; GigaAM v3 — лучший бесплатный русский self-host; трилемма «любые 2 из 3») — в [`PENDING_FOLLOWUPS.md`](PENDING_FOLLOWUPS.md) → 🟢 «Модуль аудио → текст». **Не передоказывать.**
2. **Standing browser-проверки владельцем** (по приоритету, см. `PENDING_FOLLOWUPS.md`): картинки рассылки (#264) вышли в посте?; спот-чек 19 drop-флагов (Малмыж, `digest_curation_runs`); «Радиоточка» — VK-личка (код полигону 137760500) / добавить `@ASupersharij` через `@malm_info_bot`.
3. **ad-CRM Фаза 2 (на стороне brain):** ушёл сигнал «инбокс тонет → нужен триаж/фильтр, не throughput» (#266). Нить **не открыта** — ждём ответ brain про Фазу 2.

## Контекст

- **План:** активного плана-файла нет.
- **Связанные коммиты сессии:** `3c0e8b9` docs(mailbox) — ответ brain по ad-CRM (#266, смержен).
- **Прод:** все сервисы active, health 200, прод HEAD `1925005` (probe 2026-06-22). main = origin/main, чисто.
- **Открытых PR:** этот handoff-PR (doc-only, авто-merge).

## Failed approaches (этой нитки)

- **Telegram-бот → `@smartspeech_sber_bot` для распознавания** — невозможно: Bot API запрещает боту писать боту. Автоматизация только через userbot (Telethon, личный аккаунт) = серая зона ToS / риск бана. Владелец отказался. **Не возвращаться к bot-route.**
- **«Веб-страница из Сарафана сама ставит и гоняет нативный код на любом компе»** — невозможно: песочница браузера запрещает сайту установку/исполнение нативного кода на клиенте. Реальный локальный compute через браузер = либо localhost-помощник (ставится 1×), либо WASM в самой вкладке. **Не обещать «автоустановку с любого компа».**

## Открытые вопросы для пользователя

- Аудио-модуль: берём **B** (локальный помощник, GigaAM) или **C** (в браузере, Whisper-small)? Или тема отложена.
- Standing: картинки рассылки вышли в посте? Что берём из проверок следующим?

## Не забыть (low-priority)

- 🟢 «Кругозор»: стат-замер охвата 1× vs 2× через ~неделю.
- 🟢 ad-CRM: ждём ответ brain про Фазу 2 (триаж инбокса).
- 🟢 Следующий dead-code прогон ~2026-07-14.

---

> Если читаешь это в начале новой сессии — обнови через `/close_session` в конце. **Не аккумулируй history тут** — она в `git log --follow -- docs/SESSION_HANDOFF.md`.
