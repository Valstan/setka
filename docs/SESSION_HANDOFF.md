# Session Handoff

> Sticky-note для непрерывности между сессиями разработки SETKA. Перезаписывается через [`/close_session`](../.claude/commands/close_session.md) — историю смотри через `git log --follow -- docs/SESSION_HANDOFF.md`.

**Status:** ACTIVE (mid-flight кода нет; нить «непрерывная нить клиента» — И1/И2 задеплоены, следующий срез И5)
**Updated:** 2026-06-25
**Branch:** main
**Last release in prod:** прод HEAD `d231501` (#282). Все сервисы active, health 200 (restart 2026-06-25 23:24 MSK). **Миграция 048 применена.** Прод-правка nginx вне git в силе: `client_max_body_size 20m`.

---

## Текущая нитка

**Непрерывная нить клиента** (запрос владельца 2026-06-25): вести клиента приём → общение → оплата →
публикация → подсчёт → контроль → напоминание о доплате, **не покидая кабинет, не теряя клиента, не
переключаясь между окнами**. Разбита на 5 инкрементов (дизайн — workflow understand→design→synthesize).
**Учёт в штуках** (решение владельца), расход = вышедшие платные, напоминание — **при перерасходе**.

За сессию 2026-06-25 задеплоены **три** среза (все merged + в проде):

1. **ad-CRM Раунд 4 — достижимость ЛС** ([#280](https://github.com/Valstan/setka/pull/280), `ef52245`).
   Probe-срез вскрыл: 79% входящих с `can_message=false` (VK не даёт писать) → бейдж «ЛС закрыта» +
   deeplink «Ответить из личного VK» + метрика воронки достижимости.
2. **И1 — рублёвый баланс нити** ([#281](https://github.com/Valstan/setka/pull/281), `0876d79`). Блок
   «Оплачено/Израсходовано/Осталось» в карточке + сигнал в списке + кнопка «Записать доплату». Починен
   баг рассинхрона `total_paid` (список `=='paid'` vs карточка `!='awaiting'`).
3. **И2 — штучный учёт + Telegram-напоминание о перерасходе**
   ([#282](https://github.com/Valstan/setka/pull/282), `d231501`, миграция 048). Баланс «Куплено N − Вышло
   M = Осталось K»; beat `alert-ad-overspent-daily` 11:00 MSK при перерасходе; поле «за N публ.» в оплате.

## Следующий шаг

Открытой кодовой нити нет (И1/И2 завершены и задеплоены). Кандидаты (по приоритету):

1. **И5 — «всё в карточке»** (завершает «не переключаясь между окнами»): планировать публикацию + видеть
   заявки клиента прямо из карточки клиента (overlay-модалка поверх `/ad` «Клиенты»), без ухода на вкладку
   «Планировщик». Бэкенд: `POST /clients/{id}/schedule-here` (прокси к `create_scheduled`, guard от
   двойного клика) + `GET /clients/{id}/requests`. UI: модалка + блок «Заявки клиента» в `renderClientDetails`.
2. **Браузер-проверка владельцем** накопленного: Раунд 4 (бейдж/deeplink ЛС), И1/И2 (баланс, «за N публ.»,
   перерасход красным, Telegram-напоминание). Чисто пользовательский шаг.
3. **«Кругозор» 1×/2×** — решение владельца ждёт с #270 (см. PENDING ⏳).

## Контекст

- **План:** активного плана-файла нет; дизайн нити — в workflow-выводе сессии (5 инкрементов, см.
  PENDING «🧵 Непрерывная нить клиента»).
- **Связанные коммиты сессии:** `ef52245` (#280 достижимость ЛС), `0876d79` (#281 И1 рублёвый баланс),
  `d231501` (#282 И2 штучный баланс + напоминание, миграция 048).
- **Прод:** все сервисы active, health 200, HEAD `d231501` = origin/main. Миграция 048 применена.
- **Открытых PR:** этот handoff-PR (doc-only + письмо-находка, авто-merge). #280/#281/#282 смержены и задеплоены.

## Failed approaches (этой нитки)

- **Материализовать баланс отдельной таблицей-источником правды** — отвергли по уроку `AdOrderItem`
  (миграция 030): его `quantity` ни с деньгами, ни с фактом публикаций не синхронизируется → третий
  расходящийся учётный журнал. **Баланс вычисляем** из `ad_payments`/`ad_publications`, добавили лишь
  `units_paid` (вход) и `spend_alerted_at` (дедуп). Append-only книгу (И4) держим опциональной — только
  если простого баланса не хватит.

## Открытые вопросы для пользователя

- И5 «всё в карточке» — строить следующим, или сначала браузер-проверка И1/И2?
- «Кругозор» — включаем 2× как эксперимент или оставляем 1×?

## Не забыть (low-priority)

- 📬 Письмо-находка про reachability-bottleneck в CRM ушло в `mailbox/to-brain/2026-06-25-crm-reachability-fallback.md` (в этом закрывающем PR).
- ⚠️ **Грабля деплоя (G92):** `ssh "systemctl restart"` может молча НЕ выполниться — проверять
  `ActiveEnterTimestamp` свежее времени pull, не только health 200. В этой сессии все 3 деплоя проверены.
- 🟢 Следующий ad-CRM срез — по brain-пингу «срез в неделю» (постоянная программа).
- 🟢 Следующий dead-code прогон ~2026-07-14.

---

> Если читаешь это в начале новой сессии — обнови через `/close_session` в конце. **Не аккумулируй history тут** — она в `git log --follow -- docs/SESSION_HANDOFF.md`.
