# Session Handoff

> Sticky-note для непрерывности между сессиями разработки SETKA. Перезаписывается через [`/close_session`](../.claude/commands/close_session.md) — историю смотри через `git log --follow -- docs/SESSION_HANDOFF.md`.

**Status:** IDLE (инцидент постинга закрыт и задеплоен; все нитки сессии смержены; активной код-задачи нет)
**Updated:** 2026-07-14
**Branch:** main
**Last release in prod:** код HEAD **`f7cab8a`** (#341). Прод трогали этой сессией: миграция 059 (gateway_keys), 6 env-ключей шлюза импортированы в БД, env-переключатели классификатора (enforce on + все регионы), рестарты web/worker/beat ×2. Health 200, все сервисы active.

---

## Текущая нитка

_Нет активной код-нитки._ Сессия 14.07 закрыла два инцидента + мандат мозга:
инцидент постинга (2-й бан VALSTAN → всё молчало), нейро-фильтрацию публикаций
и шлюз self-serve. Всё смержено и задеплоено. Открытые нитки owner-gated.

Сделано этой сессией (всё смержено + задеплоено):
- **#339** — VK-шлюз self-serve: БД `gateway_keys` единый источник (миграция 059,
  env — bootstrap-fallback), `scripts/issue_gateway_key.py` (issue/rotate/disable/
  import-env), агрегатный анти-бан бюджет `GATEWAY_GLOBAL_QUOTA_PER_MIN`. Миграция
  применена на проде, 6 ключей импортированы в БД, smoke KAZANSKAYA из БД → 200.
  Закрыл MANDATE-письмо brain 2026-07-12.
- **#340** — 🚨 инцидент постинга: 2-й бан VALSTAN → `wall.post` отвечал VK error 10
  (не 5!), а каскад ротировался только на 5/17/29 → резерв МАМА не пробовался,
  постинг молчал во всех регионах. Фикс: `_PUBLISH_ROTATE_CODES`/
  `_COMMUNITY_FALLBACK_CODES` теперь включают 10 (без auto-disable — 10 бывает
  транзиентным). Задеплоено.
- **#341** — нейро-фильтр публикаций (enforce, заказ владельца): вердикты
  классификатора delete/hold выключают пост из сводки (правка оператора главнее ИИ),
  на все регионы. + анти-обфускация в `is_hard_spam` (латиница-двойники «Оkaжу
  поmощь» + серые «услуги с документами») — режет текст, за который забанили.
  + дистиллятор → Opus 4.8 Low (ops-док). Env-переключатели на проде.
- **#343** — письмо мозгу про граблю VK error 10 (рефлекс #009).

## Следующий шаг

Активной код-задачи нет. Кандидаты (по приоритету):

1. **⏳ Проверить утром, что постинг ожил** (ночью циклов сводок нет): после
   #340 первая утренняя сводка должна уйти резервом МАМА.
   `ssh setka "grep -E 'Published|publish-token:|community-fallback-publish:' /home/valstan/SETKA/logs/celery-worker.log | tail"`.
   Если снова тишина — смотреть, не забанили ли и МАМА.
2. **🟡 За владельцем (не код):** (а) переключить модель рутины «Дистиллятор
   правил Сарафана» на Opus 4.8 Low (✏️ на claude.ai); (б) при следующем касании —
   ротировать `CLASSIFIER_INGEST_KEY` (засветился в чате 14.07, риск низкий).
3. **🟢 Токен-бэклог (автономно):** вычистить `VK_TOKEN_*`/`GATEWAY_KEY_*` из env
   (БД-путь обкатан; env — аварийный fallback); `web/api/publisher.py` → policy.
4. **Owner-gated:** VK-шлюз v2 write (guarded, per-key scope) — scope не решён.

## Контекст

- **План:** нет активного.
- **Связанные коммиты сессии:** `f7cab8a` (#341 enforce+обфускация), `99e25f3`
  (#340 каскад error 10), `cd7660b` (#339 шлюз self-serve), `920fba5` (#343 письмо
  мозгу), `3dbb977` (#342 PENDING).
- **Прод:** все сервисы active, health 200, HEAD `f7cab8a` (= #341). Enforce включён
  (`CLASSIFIER_ENFORCE_ENABLED=1`, `CLASSIFIER_REGION_CODES=` пусто = все регионы).
  Каскад error 10 задеплоен, но live-подтверждение постинга — за утренним циклом (см. п.1).
- **Открытых PR:** только закрывающий handoff-PR (doc-only, авто-merge).

## Открытые вопросы для пользователя

- **VK-шлюз v2 write** — какие VK-методы записи разрешить, per-key scope? Не решено.
- **Reklama-агрегаторы** — отказываться ли от ре-трансляции чужих агрегаторов целиком?
  (Инциденты банов — оба через reklama-тему из агрегаторов; is_hard_spam лечит симптом.)

## Не забыть (low-priority)

- ⏳ Утренняя проверка постинга (п.1 «Следующий шаг») — единственный незакрытый хвост инцидента.
- 🟡 Ротация `CLASSIFIER_INGEST_KEY` + переключение модели дистиллятора (в PENDING §HITL).
- 🟢 env cleanup токенов/ключей шлюза (БД-путь обкатан).
- 🟢 Прежние `sleeping` из deadcode (не удалять): `classifier/schema.py::has_merge_signal`,
  `radar_id/keys.py::keys_available`.
- 🛠 git push на этой машине: HTTP/2+schannel флапает — обход `git -c http.version=HTTP/1.1 push`.

---

> Если читаешь это в начале новой сессии — обнови через `/close_session` в конце. **Не аккумулируй history тут** — она в `git log --follow -- docs/SESSION_HANDOFF.md`.
