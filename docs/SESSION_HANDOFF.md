# Session Handoff

> Sticky-note для непрерывности между сессиями разработки SETKA. Перезаписывается через [`/close_session`](../.claude/commands/close_session.md) — историю смотри через `git log --follow -- docs/SESSION_HANDOFF.md`.

**Status:** IDLE
**Updated:** 2026-08-27
**Branch:** `main`
**Last release in prod:** `ecee066` (#534) — задеплоен 27.08 ~21:44 MSK, три сервиса
`active`, health 200, лендинг отдаёт 200. Прод и `main` совпадают.

---

## 🛑 Токен VITA — решение принято, задачи больше нет

**Владелец 2026-08-21: «не буду ротировать».** Живой бессрочный VK-токен остаётся в публичной
истории репозитория навсегда — принятый риск, а не хвост работы. **Не открывать заново и не
предлагать ротацию на `/start`.** Подавление отпечатка в [`.gitleaksignore`](../.gitleaksignore)
**не временное**; ⛔ зелёный CI не означает, что этот ключ безопасен. Радиус поражения — секция
`accepted-risk` в [`PENDING_FOLLOWUPS.md`](PENDING_FOLLOWUPS.md).

## Текущая нитка

_Нет — заказ владельца по лендингу закрыт и задеплоен, дозор D-047 закрыт. Открытая
стартовая позиция._

Сессия 27.08 сделала две вещи: выкатила доработку публичного лендинга
`/regions/links` (сортировка по охвату, прирост подписчиков, помесячная полоса, две
колонки, кликабельное имя сообщества) и закрыла наблюдательный хвост прошлой сессии —
живой трафик подтвердил посев привязок ключей шлюза.

## Следующий шаг

Активной нитки нет. Кандидаты по приоритету:

1. **🔴 Калинино (`-218991929`) не доходит до классификатора с 03.08** — единственный
   живой блокер. Причина не искалась ни разу. Заход: выяснить, на каком слое теряется —
   опрос сообщества → отбор до классификации → сама классификация. Стена живая
   (проверено read-only пробой 25.08), регион `mi` классифицируется ежедневно, строка
   Калинино по всем полям неотличима от работающих соседей.
2. **🟡 `@cache` не кэширует ничего** на эндпоинтах с зависимостью `db` (шесть мест:
   `web/api/regions.py:600,881`, `communities.py:200,236`, `posts.py:50,125`). Ключ
   строится из `str(kwargs)`, куда попадает `AsyncSession` с адресом объекта — новым на
   каждый запрос. Чинится одним `key_builder`, игнорирующим сессию. Найдено 27.08.
3. **Ходы владельца** (не код): тексты акции в `/templates`, `trusted=true` существующим
   клиентам CRM, привязка ключа `SABANTUY_MALMYZH` при подтверждении сообщества.

## Контекст

- **План:** нет активного плана; хвосты — в [`PENDING_FOLLOWUPS.md`](PENDING_FOLLOWUPS.md).
- **Коммиты сессии:**
  - `4a1a565` (#533) — **лендинг**: `modules/network_growth.py` (чистая арифметика
    прироста по `region_member_snapshots`, 35 юнит-тестов), поле `growth` в ответе
    `GET /api/regions/vk-links`, плитки + помесячная полоса + грид на две колонки в
    `region_links.html` / `region_links.js`, дефолтная сортировка «по подписчикам»,
    кликабельное имя вместо печатного адреса.
  - `ecee066` (#534) — доки по факту деплоя и закрытый дозор D-047.
- **Прод:** `ecee066`, три сервиса `active`, health 200, `/regions/links` → 200.
  Миграций в этой сессии не было (081–086 как были).
- **Открытых PR:** нет.
- **Живые приёмки на проде 27.08:** лендинг отдаёт 35 сообществ / 30 614 подписчиков,
  прирост +38 за сутки / +1931 за 30 дней / +5284 за всё время (с 7 июня),
  `stale_days=0`; в списке ровно наши 35 групп с адресами `*_info` / `*_info43`.
  D-047: GONBA 28 вызовов + CDK_KALININO 1 вызов, **все `200`**, ни одного 403.

## Открытые вопросы для пользователя

- 🎁 **Акция:** вставить тексты предложения в `/templates` (категория `ad_offer`) и
  разослать клиентам полуавто из карточек; промо-пакеты выдавать кнопкой «🎁 выдать
  акцию» во вкладке Кабинеты.
- 🟡 **`trusted=true` существующим клиентам CRM разом?** (сейчас первые три поста
  каждого идут через модерацию).
- 🟡 **SABANTUY_MALMYZH** — ключ шлюза сознательно не привязан: привязать при
  подтверждении сообщества владельцем.

## Не забыть (low-priority)

- 🌐 **VPN на машине владельца режет и прод, и GitHub — вплоть до полной невидимости.**
  27.08 полсессии `ssh sarafan` умирал на `banner exchange`, а позже отвалился и
  `api.github.com`. Диагностика по TCP тогда **врёт**: под `sing-tun` успешный connect
  приходит даже на заведомо закрытый порт, а ping отвечает за 0 мс. Разделяет одно —
  контроль-негатив (коннект в порт, который обязан быть закрыт). Само проходит; если
  прод «умер» — сперва проверь тоннель, а не бокс.
- 🛠 **01.09 09:15 MSK** — первый живой прогон `dormant_recheck_disabled` (~48 вызовов
  ВК, ждём ~2 оживших); отказ виден только по отсутствию лога — заглянуть после.
- 🛠 **04.12** — плашка «за полгода» на лендинге должна переключиться с «за всё время»
  сама (история снимков дорастёт до 180 дней). Проверить, что переключилась.
- 🛠 **Локальный `pytest`** — `--basetemp` в scratchpad обязателен; полный прогон
  **2829 passed** за ~10 мин.
- 🛠 **Не сочинять текст в командной строке** (канон): писать файл, отдавать путь.
- 🛠 **Миграции на проде — от `postgres`**, БД называется `setka`; `now()` в psql — MSK,
  `created_at` — наивный UTC (разъезд ровно 3 часа).
- 🛠 **Smoke-гейт деплоя** ложно краснеет (мерит после отбора) — техдолг в PENDING, при
  следующем `/reliz` не пугаться.

---

> Если читаешь это в начале новой сессии — обнови через `/close_session` в конце. **Не аккумулируй history тут** — она в `git log --follow -- docs/SESSION_HANDOFF.md`.
