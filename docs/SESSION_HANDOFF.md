# Session Handoff

> Sticky-note для непрерывности между сессиями разработки SETKA. Перезаписывается через [`/close_session`](../.claude/commands/close_session.md) — историю смотри через `git log --follow -- docs/SESSION_HANDOFF.md`.

**Status:** IDLE
**Updated:** 2026-06-04
**Branch:** main
**Last release in prod:** прод на `abb6efc` — **рекламный кабинет 2.0 закрыт целиком** (блоки B+A+C + хвост C). Миграция 027 применена, 3/3 сервиса active, health 200, `/ad-cabinet` и `/ad-crm` → 200.

---

## Текущая нитка

_Нет активной нитки — **рекламный кабинет 2.0 закрыт полностью** (B1/B2 планировщик, A реклама во входящих ЛС, C CRM + привязка планировщика к клиенту). Открытая стартовая позиция._

## Следующий шаг

Кандидатные стартовые точки (приоритет — за владельцем):

1. **Веб-дашборд управления/здоровья** — одобрен владельцем (mailbox brain `2026-06-04-web-dashboard-and-dedup-deferred.md`, `suggest`). Лёгкая админка поверх Postgres+Telegram: источники, статистика публикаций, статус воркеров/beat + heartbeat-watchdog (#018), ручной запуск/пауза. **Перед стартом** — отписать ack в `mailbox/to-brain/` (owner просил подтвердить, что берём в нитку). Синергия с уже сделанным `/ad-crm` (тот же UI-стек Bootstrap+vanilla JS поверх FastAPI).
2. **AI-дедуп новостей** — отложен владельцем, но путь без бюджета уточнён brain'ом: начинать с **локальных embedding-моделей** (`multilingual-e5`/`LaBSE`, CPU, без geo-блока, нулевой бюджет), порог косинусной близости эмпирически. Когда возьмёшься — отписать brain'у, какой эмбеддер выбрал (для tech-radar).
3. **Точечный добор источников регионов** через `/discover_communities` (длинный хвост СДК/библиотек) — рутинная подпитка пулов.

## Контекст

- **План:** отдельного файла-плана нет; закрытый roadmap Кабинета 2.0 — в [`PENDING_FOLLOWUPS.md`](PENDING_FOLLOWUPS.md) §«Кабинет 2.0 — roadmap» (все блоки ✅).
- **Связанные коммиты сессии:**
  - `abb6efc`/#143 — feat(ad-cabinet): хвост C — привязка `ad_scheduled_posts.client_id`/`price` из планировщика (авто-резолв клиента из заявки + бэкфилл строк + мягкое продвижение сделки в `scheduled`; UI поле цены + заметка о клиенте). +4 теста.
  - `7fb9f84`/#142 — feat(ad-cabinet): блок C UI — страница `/ad-crm` (воронка, карточки клиентов, оплаты/публикации inline, модалка «Завести клиента»), кнопка «В CRM» на карточке заявки. +1 тест (роуты).
  - `e072b12`/#141 — feat(ad-cabinet): блок C бэкенд — CRM (миграция 027 `ad_clients`/`ad_payments`/`ad_publications` + `ad_requests.client_id` + FK; API `/api/ad-crm`). +21 тест. _(смержен и задеплоен этой сессией)_
- **Прод:** HEAD `abb6efc`, 3/3 сервиса active, health 200. Миграция 027 применена (#141). Живой round-trip CRM (создать клиента → оплата → публикация → воронка → очистка) проверен на проде — путь зелёный, остатка в БД нет. 922 теста зелёные на main.
- **Открытых PR:** doc-only handoff-PR этого `/close_session` (авто-merge). Кодовых открытых PR нет.

## Открытые вопросы для пользователя

- Следующая нитка: **веб-дашборд** (требует ack в `mailbox/to-brain/` перед стартом) или что-то иное?

## Не забыть (low-priority)

- 🟢 **Браузер-верификации за владельцем** (агент в UI не ходит): `/ad-crm` — завести клиента, оплата/публикация, воронка; «В CRM» на `/ad-cabinet`; поле «Цена размещения» в планировщике; ранее не закрытые B1/B2 (отложенный пост → VK-«Отложенные»; план из предложки → уход оригинала). Логика под всеми кнопками доказана тестами + прод round-trip'ом.
- 🟢 **Веб-дашборд — ack в `mailbox/to-brain/`** обязателен перед взятием в нитку (owner-request brain).
- 🟢 **Хвост C (не сделан намеренно):** `AdPublication` сейчас заводится только вручную через карточку клиента; авто-создание записи о публикации, когда VK реально публикует отложенный пост (reconcile postponed→published), — отдельная будущая задача.

---

> Если читаешь это в начале новой сессии — обнови через `/close_session` в конце. **Не аккумулируй history тут** — она в `git log --follow -- docs/SESSION_HANDOFF.md`.
