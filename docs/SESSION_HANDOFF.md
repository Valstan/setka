# Session Handoff

> Sticky-note для непрерывности между сессиями разработки SETKA. Перезаписывается через [`/close_session`](../.claude/commands/close_session.md) — историю смотри через `git log --follow -- docs/SESSION_HANDOFF.md`.

**Status:** IDLE
**Updated:** 2026-07-27
**Branch:** main
**Last release in prod:** **`417d84e`** (#400) — задеплоено и проверено этой сессией.
Прод на два коммита позади `main` (#401, #402) — **оба doc-only, деплой не нужен**.

> `IDLE`: нитки сессии закрыты целиком, незавершённого кода нет. Остаток —
> три пункта подтверждённой владельцем очереди (ниже) и два owner-only хвоста.

---

## Текущая нитка

_Нет — все три темы сессии закрыты, открытая стартовая позиция._

Сессия 2026-07-27 (3 PR, #400–#402):

**1. Матрёшка «сводка в сводке» — закрыта фильтром** (заказ владельца по инциденту).
Опубликованная соседская сводка собиралась заново соседским обменом других
регионов: в одном посте оказались четыре вложенные шапки «Новости соседей … -
ИНФО». `utils/text_utils.is_neighbor_bulletin()` + шаг 5b в `_filter_post` режут
такой пост до публикации **всегда**, независимо от популярности. Одна точка
покрывает все три пути (районные, каскадные, соседский обмен) — все идут через
`_filter_post`. Задеплоено и проверено на проде боевым текстом инцидента.

**2. Батч №3 раскатки — северо-западный треугольник.** Опарино (30) + Луза (28) +
Подосиновец (24) созданы в VK под VALSTAN и активированы. Куст выбран замкнутым
треугольником: все трое граничат попарно и **ни один не соседствовал с активным
районом** — поодиночке соседский обмен у них не заработал бы вовсе. Активных
районов стало 26.

**3. Ре-триаж старения PENDING.** Девять секций стояли с тегом `fresh` при дате
месячной давности. Прочитаны все девять: работа в них давно закончена и
задеплоена, открытым висел чужой шаг. Восемь → `parked` с условием
расконсервации, dead-code-гигиена и программа ad-CRM → `recurring` (программы, а
не задачи). Пять браузер-хвостов сведены в «Пакет браузер-верификаций владельцем».
Кода не касались.

**Побочно:** «ГОНЬБА долбит шлюз» проверена замером и оказалась ложной тревогой —
25–30 вызовов/сутки при квоте 5000 (0.5%). Записано в PENDING как
`closed-as-noop`, писем никому не слали (решение владельца).

## Следующий шаг

Активной нитки нет. Очередь, **подтверждённая владельцем 2026-07-27** («это всё
нужно доделать»), — три пункта из пяти, два закрыты этой сессией:

1. **`/distill`** — дистилляция Корпуса. Последняя 2026-07-20, порог 7 дней
   наступил 2026-07-27, `/start` будет напоминать. Журнал —
   [`docs/ops/DISTILL_LOG.md`](ops/DISTILL_LOG.md).
2. **Судьба `tasks/monitoring_tasks.py`** — модуля нет в `include` Celery-app
   (`tasks/celery_app.py:171`), его четыре задачи (`scan_all_communities`,
   `scan_region`, `health_check`, `cleanup_old_data`) воркеру недоступны.
   Решить: включить в `include` (задачи оживут) или удалить как мёртвый слой.
   Найдено гейтом имён задач; 🟡 в PENDING, секция «Dead-code гигиена».
3. **Выхлоп новых источников** (висит с 2026-07-22): у `yaransk` / `kiknur` /
   `sanchursk` сравнить `collected_post_audit` до и после массового добора —
   выросло ли число собранных постов (особенно у Яранска: +13 источников,
   включая главный паблик района).

~~4. Следующая порция районов~~ — сделано этой сессией (батч №3).
~~5. Ре-триаж протухших секций~~ — сделано этой сессией (#402).

**Следующая порция районов** (когда вернёмся к раскатке): очередь считать **из
БД, а не из чек-листа** — список там устарел до добора 22.07. На 2026-07-27 верх:
Юрья 40, Котельнич 32, Слободской 28. Порядок —
[`docs/ops/region-activation-checklist.md`](ops/region-activation-checklist.md),
активация `scripts/activate_regions.py` (dry-run → `--apply` под гейтом #025).

## Контекст

- **План:** нет активного.
- **Связанные коммиты сессии:**
  - `417d84e` (#400) — фильтр соседских сводок + 11 тестов; **задеплоен** (`/reliz`, smoke OK).
  - `17a6716` (#401) — батч №3: журнал регионов, чек-лист, PENDING.
  - `50a4095` (#402) — ре-триаж старения PENDING (девять секций).
- **Прод:** `setka` / `celery-worker` / `celery-beat` active, `/api/health/full`
  200, HEAD `417d84e`. Отстаёт от `main` на #401 и #402 — обе doc-only.
- **Открытых PR:** нет (все три смержены).
- **Письма в мозг (ждут его pull):**
  - `mailbox/to-brain/2026-07-27-aging-tags-degrade-silently.md` (feedback, normal) —
    механика #033 деградирует молча, потому что статус ручной; плюс урок про
    «доля в ленте ≠ нагрузка».
  - С прошлой сессии: `2026-07-26-radar-token-500-fixed-uuid-mismatch.md`,
    `2026-07-26-captcha-batch-limit-and-gate-first-catch.md`.

## Открытые вопросы для пользователя

- **Два owner-only хвоста по новым группам батча №3** (без них публикация идёт
  каскадом на VALSTAN — работает, но без плеча сообщества):
  1. Ключи `COMM_<id>` — «Управление → Работа с API → Создать ключ» (права
     `photos, docs, messages, wall, manage, stories, market`), затем занести
     через `/tokens` как `COMM_240505553` (Опарино), `COMM_240505724` (Луза),
     `COMM_240505855` (Подосиновец).
  2. МАМА — подписаться на три группы и назначить редактором (под МАМОЙ в
     браузере не залогинены, поэтому не сделано).
- **Название в подвале.** Стоит «САРАФАН — автоматическая система
  медиаменеджмента»; альтернативы — «автоматическая редакция районных новостей» /
  «сбор и публикация местных новостей». Меняется одной строкой в
  `web/templates/base.html`.
- Тема `sosed` (два beat-слота почти без источников), мёртвые категории в пуле
  `arbazh` (это `DELETE` прод-данных), правило про сборы на личные карты — с
  прошлых сессий, остаётся.

## Не забыть (low-priority)

- 🟢 Каталог сервисов пополняется строкой в `config/services_catalog.py` — по мере
  того как проекты пришлют название / эмодзи / URL / описание.
- 🟡 Ротация `CLASSIFIER_INGEST_KEY` — только вместе с владельцем (шаг «правка
  env-секрета в облачной рутине» ассистенту недоступен).
- 🟢 Первый автозамер прав токенов пройдёт 1-го числа в 04:10; результат появится
  на `/tokens` в секции «Права токенов».
- 🛠 **`brain_matrica` не подтягивается** — `git pull --ff-only` там прерван:
  untracked `.agents/`, `.codex/`, `AGENTS.md` конфликтуют с входящими коммитами,
  репо отстал на 29 коммитов. Писать в чужой репо нельзя, поэтому mailbox-проверка
  на `/start` пропущена. Владельцу: `cd ../brain_matrica && rm -rf .agents .codex
  AGENTS.md && git pull --ff-only origin main`.
- 🛠 SSH на прод и `git push` на этой машине периодически флапают (exit 255 /
  connect timeout) — ретраить с бо́льшим `ConnectTimeout`, не диагностировать как
  отказ команды. Сработало и в этой сессии.

---

> Если читаешь это в начале новой сессии — обнови через `/close_session` в конце. **Не аккумулируй history тут** — она в `git log --follow -- docs/SESSION_HANDOFF.md`.
