# Session Handoff

> Sticky-note для непрерывности между сессиями разработки SETKA. Перезаписывается через [`/close_session`](../.claude/commands/close_session.md) — историю смотри через `git log --follow -- docs/SESSION_HANDOFF.md`.

**Status:** IDLE (все нитки сессии смержены+задеплоены; активной код-задачи нет)
**Updated:** 2026-07-14
**Branch:** main
**Last release in prod:** код HEAD **`d1a5a7e`** (#347). Прод трогали этой сессией:
миграция 060 (`collected_post_audit.media`), restart web+worker ×2, деплой правок
базовых правил классификатора (#346). Health 200, все сервисы active. Постинг
подтверждён живым (резерв МАМА публикует, инцидент #340 закрыт live).

---

## Текущая нитка

_Нет активной код-нитки._ Сессия 14.07 (вечер) целиком про HITL-классификатор:
петля дистилляции переведена в dev-чат + классификатору дано «медиа-зрение».
Всё смержено и задеплоено. Открытые нитки owner-gated.

Сделано этой сессией (всё смержено + задеплоено):
- **#345** — `/distill`: облачная рутина-дистиллятор упразднена (UI переключения
  модели не нашёлся), дистилляцию Корпуса делает Claude из dev-чата по запросу
  (~раз в неделю). Скилл + журнал `docs/ops/DISTILL_LOG.md` + напоминание в
  `/start` (шаг 3.2, > 7 дней). Термин «Корпус» = накапливаемые посты+вердикты+правки.
- **#346** — первая дистилляция из чата: 178 коррекций → 5 черновиков правил в
  панель (recorded:5). Плюс фикс противоречий в базовом файле правил
  (`config/classification_postulates.md` правила 2 и 8 — конфликт с выученными).
- **#347** — медиа-зрение классификатора: посты без текста больше не слепая зона.
  Аудит сбора снимает сводку вложений (`collected_post_audit.media`, миграция 060),
  рутина качает фото/PDF через media-прокси `/api/classifier/media` (egress облака
  пускает только наш хост), модель выносит вердикт + `media_summary`; лента
  показывает «📷 ИИ увидел: …».
- **Письмо мозгу** (`mailbox/to-brain/2026-07-14-cloud-routine-patterns.md`) — два
  переносимых паттерна работы с облачными рутинами (рефлекс #009).

## Следующий шаг

Активной код-задачи нет. Кандидаты (по приоритету):

1. **🟡 За владельцем (не код, claude.ai):** (а) обновить промпт рутины-классификатора
   «Классификатор текстов Сарафана» — добавился media-шаг (полная заготовка в
   `docs/ops/hitl-classifier-routine.md` §«Промпт рутины (заготовка, v3)»);
   (б) удалить рутину-дистиллятор «Дистиллятор правил Сарафана» (упразднена #345);
   (в) утвердить 5 черновиков правил в `/classifier` → панель «Правила обучения»,
   при этом черновик «пустой текст → delete» поправить на «пустой текст И медиа не
   распознано → delete»; отклонить устаревшие выученные правила 1, 4, 11, 16.
2. **🟢 Токен-бэклог (автономно):** вычистить `VK_TOKEN_*`/`GATEWAY_KEY_*` из env
   (БД-путь обкатан); `web/api/publisher.py` → policy.
3. **🟡 Ротация `CLASSIFIER_INGEST_KEY`** (засветился в чате 14.07, риск низкий) —
   при следующем касании нитки классификатора.
4. **Owner-gated:** VK-шлюз v2 write, Радар-ID round-trip с trener, Этап 2 сайта.

## Контекст

- **План:** нет активного.
- **Связанные коммиты сессии:** `d1a5a7e` (#347 медиа-зрение), `8900242` (#346
  дистилляция+фикс правил), `80aa551` (#345 /distill).
- **Прод:** все сервисы active, health 200, HEAD `d1a5a7e`. Миграция 060 применена.
  Медиа-зрение включится для постов, собранных с этого момента (старые — `media` пустой).
- **Открытых PR:** только закрывающий handoff-PR (doc-only, авто-merge).

## Открытые вопросы для пользователя

- **VK-шлюз v2 write** — какие VK-методы записи разрешить, per-key scope? Не решено.
- **Reklama-агрегаторы** — отказываться ли от ре-трансляции чужих агрегаторов целиком?

## Не забыть (low-priority)

- 🟡 Три owner-действия на claude.ai (см. «Следующий шаг» п.1) — промпт рутины,
  удаление дистиллятора, утверждение черновиков правил.
- 🟢 env cleanup токенов/ключей шлюза (БД-путь обкатан).
- 🟢 Прежние `sleeping` из deadcode (не удалять): `classifier/schema.py::has_merge_signal`,
  `radar_id/keys.py::keys_available`.
- 🛠 git push на этой машине: HTTP/2+schannel флапает — обход `git -c http.version=HTTP/1.1 push`.

---

> Если читаешь это в начале новой сессии — обнови через `/close_session` в конце. **Не аккумулируй history тут** — она в `git log --follow -- docs/SESSION_HANDOFF.md`.
