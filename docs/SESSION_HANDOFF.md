# Session Handoff

> Sticky-note для непрерывности между сессиями разработки SETKA. Перезаписывается через [`/close_session`](../.claude/commands/close_session.md) — историю смотри через `git log --follow -- docs/SESSION_HANDOFF.md`.

**Status:** IDLE
**Updated:** 2026-07-26
**Branch:** main
**Last release in prod:** **`0c0385c`** (#385) — задеплоено и проверено этой сессией.

> `IDLE`: разработческая нитка «единый вход + каталог сервисов» закрыта целиком,
> незавершённого кода нет. Остаток — ход **brain** (разослать директиву проектам)
> и данные **от проектов** (что писать в каталог); ассистенту тут делать нечего
> до их хода.

---

## Текущая нитка

_Нет — последняя задача закрыта, открытая стартовая позиция._

Сессия 2026-07-26 закрыла заказ владельца по единому входу экосистемы:
три PR, все задеплоены и проверены живьём (см. `PENDING_FOLLOWUPS.md` →
⏳ «Единый вход экосистемы + каталог сервисов»).

- **#383** — единый вход: сервисный поддомен `*.вмалмыже.рф` → центральный
  `вход.вмалмыже.рф/login?next=https://<родной-хост>/…` → возврат на родной
  домен авторизованным.
- **#384/#385** — Радар живёт на корне `радар.вмалмыже.рф` (без «/radar» в адресе).
- **#385** — публичный каталог `вход.вмалмыже.рф/services` + кнопка каталога в
  Радаре и на странице входа.

**Главное открытие дня:** 🔴-блокер «вход ВК сломан во всей экосистеме» оказался
**ложным**. Приложение VK ID 54666252 живо и настроено — просто VK ID-приложения
живут в кабинете `id.vk.ru/about/business/go`, а не в `dev.vk.ru`, где их искали
накануне. Владелец вошёл через ВК живьём.

## Следующий шаг

Активной нитки нет. Кандидатные стартовые точки (по убыванию готовности):

1. **Проверить выхлоп новых источников** (висит с 2026-07-22): у активных
   `yaransk` / `kiknur` / `sanchursk` сравнить число собранных постов в
   `collected_post_audit` до и после массового добора — вырос ли (особенно
   Яранск: +13 источников, включая главный паблик района).
2. **Миграция 073 `neighbors`** — `database/migrations/073_kirov_oblast_neighbors.sql`
   подготовлена и закоммичена, **не применена** (прод-`UPDATE` = человеческий
   гейт #025). Владельцу прочитать список соседей глазами и дать добро:
   `ssh setka "sudo -u postgres psql -d setka -f /home/valstan/SETKA/database/migrations/073_kirov_oblast_neighbors.sql"`.
3. **Ре-триаж протухшего backlog** (pool #033): секции PENDING с июньскими
   датами всё ещё помечены `fresh`, хотя им 30–45 дней; по существу в них
   остались только 🟢-хвосты «браузер-проверка владельцем» — свести в единый
   `parked`-пакет, снять теги с закрытых.
4. **Дистилляция Корпуса** — последняя была 2026-07-20; после 2026-07-27
   `/start` начнёт напоминать (порог 7 дней).

## Контекст

- **План:** нет активного.
- **Связанные коммиты сессии:**
  - `f95f21e` (#383) — единый вход, центральный `/login` + абсолютный `next`.
  - `c4b7139` (#384) — канонический редирект `/radar` на радар-хост.
  - `0c0385c` (#385) — Радар на корне поддомена + каталог `/services` + письма в мозг.
- **Прод:** `setka` / `celery-worker` / `celery-beat` active, `/api/health/full` 200,
  HEAD прода = `origin/main` = `0c0385c`. Расхождений нет.
- **Открытых PR:** нет (все три смержены).
- **Письма в мозг (ждут его pull):**
  - `mailbox/to-brain/2026-07-26-ecosystem-catalog-and-onboarding-standard.md`
    (report, high) — разослать проектам директиву и включить в постоянный онбординг.
  - `mailbox/to-brain/2026-07-26-two-findings-vkid-cabinet-and-sso-trust-boundary.md`
    (idea, suggest) — две находки: кабинет VK ID и граница доверия SSO-возврата.

## Открытые вопросы для пользователя

- **Тема `sosed`: упразднить или наполнить?** Два beat-слота в сутки гоняют её
  почти без источников (развилка описана в `PENDING_FOLLOWUPS.md`).
- **Мёртвые категории `music`/`kino`/`art`/`prikol`** в пуле `arbazh` (12 строк,
  ноль ссылок в коде) — чистить? Это `DELETE` прод-данных, нужен ваш ход.
- Правило про сборы на личные карты физлиц — вопрос с прошлой сессии, остаётся.
- Перевыпуск community-токенов из-под разблокированного VALSTAN — остаётся.
- **Ёмкость облачной рутины-классификатора** исчерпана (потолок ≈1600/сутки
  против притока ≈1550) — рычаг только у владельца на claude.ai.

## Не забыть (low-priority)

- 🟢 Каталог сервисов пополняется строкой в `config/services_catalog.py` — по мере
  того как проекты пришлют название / эмодзи / URL / описание.
- 🟢 Карточка Сабантуя в каталоге ведёт на `https://сабантуймалмыж.рф` — URL взят
  из `docs/ops/vk-id-setup-radar.md`, живьём не открывался.
- 🟡 Ротация `CLASSIFIER_INGEST_KEY` — только вместе с владельцем (иначе встанет
  поток вердиктов).
- 🟢 env cleanup `VK_TOKEN_*` (owner-confirm); nginx-блок Радара — упростить
  ACME-location.
- 🛠 git push на этой машине: HTTP/2+schannel флапает — зафиксировано
  `git config http.version HTTP/1.1` в `.git/config` этого worktree.
- 🛠 SSH на прод периодически отдаёт exit 255 (banner exchange) — это флап канала,
  ретраить с бо́льшим `ConnectTimeout`, не диагностировать как отказ команды.

---

> Если читаешь это в начале новой сессии — обнови через `/close_session` в конце. **Не аккумулируй history тут** — она в `git log --follow -- docs/SESSION_HANDOFF.md`.
