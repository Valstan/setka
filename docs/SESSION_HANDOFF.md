# Session Handoff

> Sticky-note для непрерывности между сессиями разработки SETKA. Перезаписывается через [`/close_session`](../.claude/commands/close_session.md) — историю смотри через `git log --follow -- docs/SESSION_HANDOFF.md`.

**Status:** IDLE (4 фичи сессии смержены; mid-flight кода нет)
**Updated:** 2026-06-30
**Branch:** main
**Last release in prod:** прод HEAD `e3bc416` (#291). Все сервисы active, health 200 (последний restart web+worker+beat 2026-06-29 ~23:47 MSK, G92-проверен). **Миграций в этой сессии не было.** #292 (MCP-обёртка) на проде НЕ нужен — артефакт исполняется у потребителей, рантайм SARAFAN его не импортирует.

---

## Текущая нитка

_Нет — последняя задача закрыта, открытая стартовая позиция._ За сессию закрыты 4 фичи
(все merged, 2 задеплоены): deliver-19, И5 «всё в карточке», VK-шлюз v2 (observability +
MCP-обёртка).

## Следующий шаг

Активной нити нет. Кандидатные стартовые точки (по приоритету):

1. **Ответ brain по LLM-курации.** Я отдал precision владельца **3/19 ≈ 16%**
   (`mailbox/to-brain/2026-06-29-curation-spotcheck-precision.md`); премиса «не-район = мусор»
   опровергнута. Развилка ждёт brain/владельца: **(A)** переоснастить рубрику под интент
   «разносол» + перемерить shadow, либо **(B)** свернуть LLM-фильтр. Enforcing текущей рубрики
   НЕ включаем. См. PENDING «🤖 LLM-курация».
2. **VK-шлюз v2 — запись в VK (guarded).** Security-чувствительно: перед постройкой нужны
   решения владельца по scope (какие write-методы? per-key write-allowlist? анти-бан). Начать с
   probe + дизайна. См. PENDING «🌐 VK-шлюз → v2-бэклог».
3. **VK-шлюз v2 — async «прочесать паблик»** — отложено до реальной потребности потребителя
   (probe-before-build): пока никто не просит тяжёлых запросов.
4. Прочее из PENDING: Кругозор (2×-эксперимент за владельцем), генератор обложек (ждёт фон от
   brain↔владельца), радар-бэклог.

## Контекст

- **План:** нет активного плана.
- **Связанные коммиты сессии:**
  - `85c66e1` (#289) — спот-чек 19 LLM-флагов, precision 3/19 → brain (doc-only).
  - `c7b7a89` (#290) — И5 «всё в карточке»: `schedule-here` из карточки + заявки клиента модалкой (задеплоено).
  - `e3bc416` (#291) — VK-шлюз v2: логирование 401/429 + ретеншн `gateway_requests` (задеплоено).
  - `507fd6d` (#292) — VK-шлюз v2: MCP-обёртка `gateway_mcp/` (деплой не нужен).
- **Прод:** все сервисы active, health 200, HEAD `e3bc416` (#291). Расхождение с main — только #292
  (MCP-обёртка, прод-деплой не требуется). Миграция 049 (прошлая сессия) применена.
- **Открытых PR:** этот handoff-PR (doc-only + письмо-находка, авто-merge). #289–292 смержены.

## Открытые вопросы для пользователя

- **LLM-курация:** дождаться ориентира brain по развилке (A переоснастить / B свернуть) или решить самим?
- **VK-шлюз запись:** когда будешь готов дать scope-решения — заходим в дизайн записи-в-VK.

## Не забыть (low-priority)

- 📬 Письма-находки этой сессии: `2026-06-29-curation-spotcheck-precision.md` (precision-число),
  `2026-06-30-mcp-wrapper-testable-core-pattern.md` (паттерн тестируемого ядра MCP-обёртки) — уедут
  в закрывающем PR.
- 🟢 **Браузер-проверки владельцем** (физические, не блокеры): И5 — кнопки «Запланировать публикацию» /
  «Входящие заявки» в карточке клиента (`/ad` → Клиенты); v2 — 401 от `(unknown)` уже виден на `/gateway-stats`.
- 🟢 **Раздать `GATEWAY_KEY_*` потребителям** + дать им `gateway_mcp/` (MCP-обёртка готова): малмыжские
  сайты / GONBA (brain #062).
- 🟢 Синтетическая тест-строка `(unknown)|auth|401` в `gateway_requests` на проде (от смоука v2) —
  безвредна, прунется ретеншном через 90 дней.
- 🟢 Следующий dead-code прогон ~2026-07-14.

---

> Если читаешь это в начале новой сессии — обнови через `/close_session` в конце. **Не аккумулируй history тут** — она в `git log --follow -- docs/SESSION_HANDOFF.md`.
