# Session Handoff

> Sticky-note для непрерывности между сессиями разработки SETKA. Перезаписывается через [`/close_session`](../.claude/commands/close_session.md) — историю смотри через `git log --follow -- docs/SESSION_HANDOFF.md`.

**Status:** ACTIVE
**Updated:** 2026-08-12
**Branch:** main
**Last release in prod:** `8344744` (#475) — слой отбора по меткам + политика деградации волн

---

## Текущая нитка

**Звено 5 цепочки — «сборка сводки по меткам нейросети» — построено наполовину.** Решающий слой
(`modules/classifier/selection.py`) готов, оттестирован и задеплоен, но **никем не вызывается**:
поведение публикации сегодня не изменилось ни на шаг. Осталась хирургия в парсере и отдельная
работа по рейтингу.

Сессия закрыла три звена из семи и вылечила инцидент безопасности: угон обоих Telegram-ботов,
перевод экосистемы на DeepSeek (D-024), headless-классификатор вместо облачной рутины, дистиллятор
правил (звено 7).

## Следующий шаг

**Порядок жёсткий — рейтинг ДО встраивания, иначе отбирать будет не из чего.**

1. **Метрики и рейтинг** (не начато). Миграция: колонки `views`/`likes`/`comments`/`reposts` +
   `published_at` в `collected_post_audit` (их там сейчас нет вообще). Таска обновления метрик по
   правилам владельца: **не трогать посты старше 72 часов** и **не трогать уже опубликованные** —
   они в рейтинге не участвуют. Затем отбор топ-N по рейтингу внутри разрешённых вердиктом.
2. **Встраивание отбора в парсер.** `modules/vk_monitor/advanced_parser.py`: принять
   `allowed_lips` + флаг `verdict_mode`. Классификация стадий `_filter_post` уже сделана:
   - **отключить** (редакционные, их заменяет нейронка): `hard_spam` (:474),
     `neighbor_bulletin` (:481), `advertisement` (:484), `blacklist_text` (:491),
     `region_words` (:498), `no_attachments` (:525), тематические (:532);
   - **сохранить** (механика): возраст 72ч (:440), дедуп по lip (:449), чёрный список
     сообществ (:462), дедуп по медиа (:537) и тексту (:551).
   Вызвать `selection.decide_mode` в [parsing_scheduler_tasks.py:292](../tasks/parsing_scheduler_tasks.py:292)
   и [cascaded_bulletin.py:456](../modules/cascaded_bulletin.py:456), режимы:
   `verdicts` → передать `allowed_lips`; `skip-wave` → выйти без публикации; `algorithmic-fallback`
   → нынешнее поведение. Гейт для включения — новый env-флаг, по умолчанию выключен.
3. **Посты без текста.** Решение принято за владельца и им не оспорено: ИИ-фильтр ставит им явный
   `hold` с пометкой «нет текста, вложения не читаются», чтобы они попадали в ленту оператору, а не
   исчезали молча. Сейчас `headless.classify_posts` их просто пропускает (`skipped_no_text`, 18-19
   на прогон) — под звеном 5 это означало бы тихое исчезновение афиш-картинок из лент.

## Контекст

- **План:** цепочка из 7 звеньев в формулировке владельца (2026-08-12) — см. секцию D-024 в
  [`PENDING_FOLLOWUPS.md`](PENDING_FOLLOWUPS.md). Мандат brain `2026-08-10-conveyor-go-...`.
- **Связанные коммиты сессии:**
  - `843583c` (#468) — маскирование секретов в логах + сторож целостности Telegram-ботов
  - `589cf0b` (#469) — письмо brain об угоне ботов (три переносимые находки)
  - `6b0b88d` (#470) — движок экосистемы DeepSeek, Groq убран целиком (D-024)
  - `debdc63` (#471) — headless-классификация вместо облачной рутины
  - `c5ebb5b` (#472) — фикс реестра SQLAlchemy в скрипте сверки
  - `89197da` (#473) — переключение на headless + разбор объёма классификации
  - `bb54194` (#474) — дистиллятор правил (звено 7)
  - `8344744` (#475) — слой отбора по меткам + политика деградации волн
- **Прод:** HEAD `8344744` = `main`, все три сервиса active, health 200. Расхождения нет.
- **Прод-операции сессии (вне git):** ротация обоих Telegram-токенов (комната КАРМАНа, две строки
  `TELEGRAM_TOKEN_*` удалены из `setka.env`); `GROQ_API_KEY` удалён из env; пакет `groq` снесён из
  venv; `CLASSIFIER_HEADLESS_ENABLED=1`; логи вычищены от токенов, права `750`/`640`. Бэкапы:
  `setka.env.bak-20260812-{tg-rotation,groq-removed,headless-on}`.
- **Открытых PR:** нет.

## Failed approaches (этой нитки)

- **Считать качество headless по проценту совпадения с рутиной** — отвергнуто: 33% выглядят
  провалом, но половина расхождения это `hold` рутины (47% потока), от которых она уходила от
  решения. Процент совпадения с неэталоном не измеряет качество; смотреть надо матрицу переходов и
  читать расхождения глазами.
- **Маскировать токен регуляркой `\b(\d{6,12}):...`** — не срабатывает на главной форме утечки
  `/bot<токен>`: между `t` и цифрой нет границы слова. Поймано тестом, воспроизводившим утёкшую
  строку дословно. Урок общий: тест на маскирование обязан брать **фактическую строку из лога**.
- **Включить headless параллельно с облачной рутиной** — нельзя: запись идемпотентна по `lip`, оба
  источника разбирают одну очередь и воруют друг у друга посты, счёт идёт за обоих, сверка
  становится невозможной.
- **`pre-commit run --all-files` до `git add`** — не проверяет untracked-файлы. Дважды за сессию
  «зелёный локально» падал в CI на новом файле. Порядок: `git add` → прогон гейта.

## Открытые вопросы для пользователя

- **Облачная рутина «⚡ Классификатор текстов Сарафана» — выключена ли?** Headless работает по
  расписанию сам; пока рутина жива, они разбирают одну очередь на двоих.
- **`GROQ_API_KEY`** — отозвать в консоли Groq (скомпрометирован, всё ещё живой) и удалить из
  комнаты КАРМАНа через UI. Пока он в комнате, vault-клиент на каждом старте пишет
  `вне allowlist, проигнорированы: GROQ_API_KEY` — сигнал инцидента превращается в фон.
- **Аккаунт `Valstan` в бане ВК до 18.08.2026** из-за спама, который пропускают алгоритмические
  фильтры. Это и есть главный довод за звено 5.
- **Rolling discovery** — условие расконсервации наступило (нейро-фильтр есть), слот выключен
  сознательно; включение меняет расход токенов, решение владельца.
- **Базового файла постулатов не существует** — завести или перестать обещать его в докстрингах.

## Не забыть (low-priority)

- 🛠 **Ad-hoc скрипты начинают с `import database.models`** — иначе `models_extended` роняет реестр
  SQLAlchemy на `'Region' failed to locate a name`. Наступил на это второй раз за месяц.
- 🛠 **`git add` до `pre-commit run --all-files`** — untracked-файлы гейт не видит.
- 🛠 **Прод: `now()` в psql — MSK, а `created_at` пишется наивным UTC.** Ad-hoc запросы с окном
  `now() - interval` смещены на 3 часа; на окнах в сутки+ это шум, на окнах в часы — ложный ноль.
- 🛠 **Не-ASCII на прод — только файлом** (`scp`), никогда аргументом команды (G235).
- 🛠 **Локальный `pytest`** — `--basetemp=<scratchpad>/pt -p no:cacheprovider`; `PRE_COMMIT_HOME`
  тоже в scratchpad.
- 🟡 **Красный прогон gitleaks на подсадном секрете** — зелёный доказывает, что сканер запускается,
  но не что он ловит.
- 🟡 **`VK token is invalid or expired` для группы `-218877712`** — замечено в логе воркера, к работе
  сессии отношения не имеет, никто не смотрел.

---

> Если читаешь это в начале новой сессии — обнови через `/close_session` в конце. **Не аккумулируй history тут** — она в `git log --follow -- docs/SESSION_HANDOFF.md`.
