# Session Handoff

> Sticky-note для непрерывности между сессиями разработки SETKA. Перезаписывается через [`/close_session`](../.claude/commands/close_session.md) — историю смотри через `git log --follow -- docs/SESSION_HANDOFF.md`.

**Status:** IDLE
**Updated:** 2026-08-06
**Branch:** main
**Last release in prod:** `aaef53f` (#441) — resolve-profile: поддержка vk.ru и idXXX формата

---

## Текущая нитка

_Закрыта: переработка ad-CRM — список публикаций v2 + сканер + форма клиента._

Всё задеплоено на прод (5 PR за сессию):
- **Список публикаций** — группировка по сообществам, сканер по ключевым словам, диапазону дат и автору, статус Оплачен/Не оплачен, отчёт
- **Форма клиента** — авто-заполнение из VK-ссылки, новые поля (телефон, Telegram, email, адрес), можно без VK

## Следующий шаг

Активной нитки нет. Приоритет из PENDING:
1. **Миграция 076 выполнена** (пункт из прошлого handoff закрыт). Следующая — **076 уже на проде**.
2. **Сценарий B vault** (автозеркалирование секретов из CI)
3. **Пилот П4 DeepSeek vs Haiku** (MANDATE brain 2026-08-01)
4. **Gitleaks** — крупнейший незакрытый техдолг
5. **Регистрозависимые логины** — миграция с данными

## Контекст

- **План:** нет активного.
- **Связанные коммиты сессии (2026-08-06):**
  - `abb3929` (#437) — список публикаций v2: группировка, сканер, отчёт
  - `ccc6879` (#438) — fix: кэш api.js
  - `cc81115` (#439) — сканер: точная фраза, диапазон дат, поиск по автору
  - `f1f47db` (#440) — форма клиента: авто-заполнение из VK, новые поля, миграция 077
  - `aaef53f` (#441) — fix: resolve-profile vk.ru и idXXX
- **Прод:** все 3 сервиса active, HEAD `aaef53f`, `/api/health/full` 200. Миграции 076 и 077 применены.
- **Открытых PR:** нет.

## Открытые вопросы для пользователя

- **Пилот П4 (MANDATE brain).** DeepSeek vs Haiku на 100 постах, оффлайн. Не начат.
- **Судьба `sosed` темы** — два beat-слота в сутки почти без источников; нужно решение: добить или упразднить.
- **Ключи трёх молчащих потребителей** (`CDK_KALININO`, `DK_MALMYZH`, `SABANTUY_MALMYZH`) — активны с 13.07, ноль вызовов.

## Не забыть (low-priority)

- 🟡 **Gitleaks** — секрет-сканера в контуре нет вовсе, самый крупный незакрытый техдолг.
- 🟡 **Регистрозависимые логины** — `VALSTAN` и `valstan` разные аккаунты; миграция с `lower(login)` + гейт #025.
- 🟡 **Рекламная рубрика ре-транслирует чужие агрегаторы** → риск бана VK; отказ от ретрансляции при повторных банах.
- 🟡 **Ротация `CLASSIFIER_INGEST_KEY`** — делать только в присутствии владельца (одним ходом: ключ + restart + секрет рутины).
- 🟢 **Поштучные услуги лендинга** — сверить с реальностью.
- 🟢 **ТАКСИ** — карточка в каталоге.
- 🛠 **`pre-commit run --all-files` не видит untracked-файлы.** Правило: `git add -A` перед прогоном гейта.
- 🛠 **Локальный `pytest`** — нужен `--basetemp=<scratchpad>/pt -p no:cacheprovider`, таймаут 600000.

---

> Если читаешь это в начале новой сессии — обнови через `/close_session` в конце. **Не аккумулируй history тут** — она в `git log --follow -- docs/SESSION_HANDOFF.md`.
