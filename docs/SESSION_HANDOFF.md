# Session Handoff

> Sticky-note для непрерывности между сессиями разработки SETKA. Перезаписывается через [`/close_session`](../.claude/commands/close_session.md) — историю смотри через `git log --follow -- docs/SESSION_HANDOFF.md`.

**Status:** ACTIVE
**Updated:** 2026-08-28
**Branch:** `main`
**Last release in prod:** `28b2a27` (#546) — прод и `main` расходятся на один doc-only
коммит `c1cc88c` (#547, только доки). Миграции 087, 088, 089 применены и записаны в журнал.

---

## 🛑 Токен VITA — решение принято, задачи больше нет

**Владелец 2026-08-21: «не буду ротировать».** Живой бессрочный VK-токен остаётся в публичной
истории репозитория навсегда — принятый риск, а не хвост работы. **Не открывать заново и не
предлагать ротацию на `/start`.** Подавление отпечатка в [`.gitleaksignore`](../.gitleaksignore)
**не временное**; ⛔ зелёный CI не означает, что этот ключ безопасен. Радиус поражения — секция
`accepted-risk` в [`PENDING_FOLLOWUPS.md`](PENDING_FOLLOWUPS.md).

## Текущая нитка

**Модуль «Раскрутка»** — этапы 0 и 1 выкачены и приняты живьём, идёт **неделя наблюдения
сухого прогона**. Публикаций нет и не будет, пока владелец не снимет `dry_run` порычажно.
**Работы в нитке для агента сейчас нет** — она ждёт трёх решений владельца (пары / тексты /
включение канала), то есть по правилам файла это `parked`, а не активная стройка.

Сессия 28.08 занималась другим и **закрыла всё, что начинала**: инцидент со спамом в Telegram
и сплошной ре-триаж бэклога.

## Следующий шаг

Нитка раскрутки ждёт владельца, поэтому следующая работа берётся из бэклога. После ре-триажа
он честный — приоритет по убыванию:

1. **🔴 Калинино (`-218991929`) — единственный блокер с живым ущербом.** Сообщество без
   конвейера с 03.08. Ре-триаж сузил поиск: под фильтр волны оно **подходит**
   (`category='kultura'`, регион `mi` жив — 40 вердиктов за сутки), значит теряется **после**
   выборки — смотреть `wall.get` и дедуп по `lip`, а не классификатор.
   ⚠️ Довод старой записи «`checked_at` обновляется» **ничего не доказывает**: поле не пишется
   волной вообще, у всех 1518 активных максимум `2026-08-24`.
2. **Журнал миграций 080–086** — числятся pending, объекты на проде есть. Сверить каждую с
   прод-схемой и по совпавшим `scripts/migrate.py mark-applied <файл> --yes`.
   До этого **`migrate.py up` запускать нельзя** — прогонит семь миграций заново.
3. **Пачка мелких техдолгов одним заходом:** `key_builder` для 9 `@cache`-точек (сейчас ключ
   строится из всех аргументов, включая `AsyncSession` → кэш промахивается всегда),
   маскирование секрет-объекта в `record.msg`, smoke-гейт мерит после отбора (два ложных
   красных), ретеншен `ad_interactions`, имя района вместо `region_id` в очереди модерации.

## Контекст

- **План:** `C:\Users\Valstan\.claude\plans\clever-inventing-key.md` (этапы 0–6 раскрутки).
  Этапы 0 и 1 закрыты, 2–6 не начаты.
- **Коммиты сессии:** `dfde588` (#545) частичная доставка перестала считаться провалом ·
  `28b2a27` (#546) потолок повторов 3 попытки + миграция 089 · `c1cc88c` (#547) ре-триаж
  бэклога, 44 записи.
- **Прод:** три сервиса `active`, health 200, код `28b2a27`. Зеркало Гоньбы с 18:41 идёт
  зелёным (`nothing new to mirror`) — впервые с 27.08 09:10.
- **Открытых PR:** нет.
- **Тесты:** 2960 passed, 36 skipped.
- **Бэклог после ре-триажа:** ~79 открытых секций против ~105 до уборки; 41 пункт открыт
  реально, с проверенным фактами следующим шагом у каждого.

## Failed approaches (этой нитки)

- **Механика SEO-RPG (массовый `wall.post` в чужие группы)** — отвергнута до строчки кода.
  Причины в шапке [`config/promo.py`](../config/promo.py); ключевая — теми же токенами
  публикуются все 38 районов, и бан уже гасил сеть (G151). **Не возвращаться** без нового
  ресурса токенов.
- **`groups.edit` под user-токеном** — у user-токенов метода **нет** (`[3] Unknown method`),
  а групповой ключ его исполняет. **Не повторять рассуждение «групповой токен слабее»** —
  оно верно не для всех методов.
- **`groups.addLink`** — метод удалён у всех типов токенов, ссылки только руками.
- **Смотреть на один флаг `success` там, где отправка состоит из нескольких вызовов** —
  стоило двух суток спама в живом канале (#545). Правило теперь в коде: `delivered` (ушло
  хоть что-то) отдельно от `success` (ушло всё), ретрай смотрит на `delivered`.
- **Верить метке старения в `PENDING_FOLLOWUPS.md`** — 26% записей описывали уже сделанную
  работу. Ре-триаж гонять только с требованием доказательства на каждый вердикт.

## Открытые вопросы для пользователя

- 🖐 **Перевод 19 сообществ «группа» → «публичная страница»** — API не умеет, только руками,
  выдержка ≥30 дней. Список — во вкладке «Районы».
- 🔗 **Блок «Ссылки»** — проставлять руками, соседей и область (4–8 ссылок), не все 38.
- 🟡 **`trusted=true` существующим клиентам CRM разом?** На проде сейчас ни одного
  `trusted=true` из 9 клиентов — решение не применялось.
- 🟡 **SABANTUY_MALMYZH** — привязать ключ при подтверждении сообщества владельцем.
- 🟡 **Раскрутка, три вопроса недели наблюдения:** адекватны ли пары (спорная «Малмыж →
  Кумёны», второй хоп ~150 км), не читаются ли тексты как реклама, не пора ли включать канал.
- 🟡 **Прайс на лендинге** — четыре строки `SINGLE_SERVICES` (350/+200/300/700 ₽) публичны;
  соответствуют ли они тому, что реально продаётся?

## Не забыть (low-priority)

- ⚠️ **`migrate.py up` нельзя запускать не глядя** — 080–086 числятся pending при живых
  объектах. Подробности в PENDING.
- 🛠 **01.09 09:15 MSK** — первый живой прогон `dormant_recheck_disabled` (~48 вызовов ВК).
- 🛠 **04.12** — плашка «за полгода» на лендинге должна переключиться сама.
- 🛠 **Локальный `pytest`** — `--basetemp` в scratchpad обязателен, полный прогон ~2960 за
  ~8,5 мин. `pre-commit` — только с `PRE_COMMIT_HOME` в scratchpad.
- 🛠 **venv в этом worktree пересоздан 28.08** (`scripts/setup-dev.ps1` + доустановка
  `pytest pytest-asyncio pytest-cov pre-commit` — сам скрипт их не доставил).
- 🛠 **Контрольные суммы миграций считать НА ПРОДЕ.** Локальная копия лежит с CRLF, на сервере
  LF — sha256 не совпадает, и запись в `applied_migrations` соврала бы. Поймано на 089.
- 🛠 **Смоук-гейт даёт ложный минус** — мерит выход ПОСЛЕ отбора. 28.08 в 16:45 на `ur` дал
  «0 < 1» при полностью здоровом проде. Третий ложный красный подряд, техдолг заведён.
- 🛠 **Миграции на проде — от `postgres`**, БД `setka`; `now()` в psql — MSK, `created_at` — UTC.
- 🌐 **VPN `happ-tun` на машине владельца** режет `gh api` и `api.telegram.org` — за сессию
  дважды TLS-таймаут. Само проходит, повтор помогает.

---

> Если читаешь это в начале новой сессии — обнови через `/close_session` в конце. **Не аккумулируй history тут** — она в `git log --follow -- docs/SESSION_HANDOFF.md`.
