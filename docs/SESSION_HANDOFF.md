# Session Handoff

> Sticky-note для непрерывности между сессиями разработки SETKA. Перезаписывается через [`/close_session`](../.claude/commands/close_session.md) — историю смотри через `git log --follow -- docs/SESSION_HANDOFF.md`.

**Status:** IDLE
**Updated:** 2026-08-29
**Branch:** `main`
**Last release in prod:** `d14f91d` (#551) — прод и `main` идентичны. Миграций в сессии не было; журнал миграций сверен, `Pending to apply: 0`.

---

## 🛑 Токен VITA — решение принято, задачи больше нет

**Владелец 2026-08-21: «не буду ротировать».** Живой бессрочный VK-токен остаётся в публичной
истории репозитория навсегда — принятый риск, а не хвост работы. **Не открывать заново и не
предлагать ротацию на `/start`.** Подавление отпечатка в [`.gitleaksignore`](../.gitleaksignore)
**не временное**; ⛔ зелёный CI не означает, что этот ключ безопасен. Радиус поражения — секция
`accepted-risk` в [`PENDING_FOLLOWUPS.md`](PENDING_FOLLOWUPS.md).

## Текущая нитка

_Нет — всё, что начинала сессия 29.08, закрыто и выкачено на прод._ Модуль «Раскрутка»
по-прежнему `parked`: неделя наблюдения сухого прогона, ждёт трёх решений владельца
(пары / тексты / включение канала) — это не стройка агента.

Сессия 29.08 закрыла три пункта бэклога, все на проде:
блокер Калинино → найден **сетевой** баг фото-дедупа (`photo_<id>` без owner_id — ложные
дубли по всей сети, 6–7 тыс. дропов/день) → фикс #549; журнал миграций 080–086 сверен с
прод-схемой и записан `mark-applied` → #550; кэш-ключ на DI-объектах (9 мёртвых
`@cache`-точек) → #551.

## Следующий шаг

Кандидаты (по убыванию):

1. **Дозор фото-дедупа (#549)** — когда накопятся сутки волн: `posts_filtered_duplicate_foto`
   в `parsing_stats` должен упасть с 6–7 тыс./день; следующий свежий пост Калинино
   (`-218991929`) должен появиться в `collected_post_audit` (а с текстом — и в вердиктах).
   Одна SQL-проба, детали в секции Калинино в PENDING.
2. **Остаток «пачки мелких техдолгов»:** маскирование секрет-объекта в `record.msg` ·
   смоук-гейт мерит после отбора (три ложных красных) · ретеншен `ad_interactions` ·
   имя района вместо `region_id` в очереди модерации.
3. **Вопросы недели наблюдения раскрутки** — за владельцем (пары / тексты / включение).

## Контекст

- **План:** нет активного плана (план раскрутки `clever-inventing-key.md` — этапы 2–6 не начаты, parked).
- **Коммиты сессии:** `bf38508` (#549) фикс фото-фингерпринта + письмо мозгу ·
  `ac49cf1` (#550) журнал миграций, pending 0 · `d14f91d` (#551) кэш-ключ из стабильных значений.
- **Прод:** три сервиса `active`, health 200, код `d14f91d` (= main). Смоук mi/kultura после
  #549: `posts_parsed=21, would_publish=1` — заметно больше типичных 3–5 у старых волн.
- **Открытых PR:** нет.
- **Тесты:** 2975 passed, 36 skipped.

## Открытые вопросы для пользователя

- 🖐 Перевод 19 сообществ «группа» → «публичная страница» — руками, список во вкладке «Районы».
- 🔗 Блок «Ссылки» — проставить руками соседей и область (4–8 ссылок).
- 🟡 `trusted=true` существующим клиентам CRM разом? (на проде ни одного из 9).
- 🟡 SABANTUY_MALMYZH — привязать ключ при подтверждении сообщества владельцем.
- 🟡 Раскрутка: адекватны ли пары, не читаются ли тексты как реклама, не пора ли включать канал.
- 🟡 Прайс на лендинге — четыре строки `SINGLE_SERVICES` соответствуют продаваемому?

## Не забыть (low-priority)

- ⚠️ **`pre-commit run --all-files` слеп к untracked-файлам** — новый файл линтуется только
  после `git add`; CI на #551 поймал black-формат, который локальный прогон пропустил.
- 🛠 **01.09 09:15 MSK** — первый живой прогон `dormant_recheck_disabled` (~48 вызовов ВК).
- 🛠 **04.12** — плашка «за полгода» на лендинге должна переключиться сама.
- 🛠 Локальный `pytest` — `--basetemp` в scratchpad, полный прогон ~2975 за ~8,5 мин;
  `pre-commit` — только с `PRE_COMMIT_HOME` в scratchpad.
- 🛠 «Мёртвые волны» `scanned=0` при `success=t` кластерами (12/14/17.08) — наблюдение в
  секции Калинино в PENDING, отдельный заход при повторении.
- 🌐 VPN `happ-tun` на машине владельца режет SSH/gh — TLS-таймауты, повтор помогает.

---

> Если читаешь это в начале новой сессии — обнови через `/close_session` в конце. **Не аккумулируй history тут** — она в `git log --follow -- docs/SESSION_HANDOFF.md`.
