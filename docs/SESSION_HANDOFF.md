# Session Handoff

> Sticky-note для непрерывности между сессиями разработки SETKA. Перезаписывается через [`/close_session`](../.claude/commands/close_session.md) — историю смотри через `git log --follow -- docs/SESSION_HANDOFF.md`.

**Status:** ACTIVE
**Updated:** 2026-08-29
**Branch:** `main`
**Last release in prod:** `d6a7c09` (#561) — прод = main минус doc-only #562; воркер и beat перезапущены, health 200.

---

## 🛑 Токен VITA — решение принято, задачи больше нет

**Владелец 2026-08-21: «не буду ротировать».** Живой бессрочный VK-токен остаётся в публичной
истории репозитория насовсем — принятый риск, а не хвост работы. **Не открывать заново и не
предлагать ротацию на `/start`.** Радиус поражения — секция `accepted-risk` в
[`PENDING_FOLLOWUPS.md`](PENDING_FOLLOWUPS.md).

## Текущая нитка

**Ребрендинг сети ИНФО** — план утверждён владельцем и исполнен на этапы 0–4 за одну
сессию (PR #556–#562, журнал — [`docs/ops/rebranding-2026-08.md`](ops/rebranding-2026-08.md)):
проба возможностей (city/status — ловушка «ok без применения»), генератор графики
(контактный лист принят владельцем), автооформление **порции 1 (10 регионов)** со сверкой
живьём, **хедлайнер** (обоснование — замер Кирса: 437k vs 167 просмотров на одном контенте),
футер «Ленты соседей» + снятие мёртвого гейта соседского канала, **города всех 27 сообществ**
проставлены браузером и сверены API. Всё на проде.

Той же сессией (утро): **батч №8 раскатки** — Уни+Фалёнки+Богородское созданы и активированы
(#553), остаток области — Оричи и Орлов; журнал регионов перестал врать про активность (#554);
разбор находимости ВК с проверкой источников — плейбук (#555).

## Следующий шаг

1. **Порция 2 автооформления** (~12 full-регионов, mi…zuevka): бюджет user-вызовов VALSTAN
   обнуляется завтра →
   `ssh sarafan "sudo bash -c 'set -a; . /etc/setka/setka.env; set +a; cd /home/valstan/SETKA && ./venv/bin/python scripts/setup_groups.py --apply'"`
   (идемпотентно, применённых пропустит; гейт #025 — подтверждение владельца). При `error 129`
   следом `--repair` (иногда дважды — ретраи добивают).
2. **Дозор хедлайнера/футера**: через сутки волн глянуть стены пары регионов — выходят ли
   одиночные посты без шапки и футер «Ленты соседей»; охваты не судить раньше 7–10 дней.
3. Остальное по журналу ребрендинга: UGC-кнопки в `/notifications`, верификация Малмыжа
   (owner), beat-мониторинг `stats.get`.

## Контекст

- **План:** `C:\Users\valstan\.claude\plans\jazzy-pondering-puffin.md` (утверждён; этапы 5–7 не начаты).
- **Коммиты сессии:** `d9efcaa` (#553) батч №8 · `870f1bf` (#554) журнал регионов + снятие
  «перевода в страницу» · `c08db43` (#555) плейбук находимости · `e472e32` (#556) проба ·
  `b744512` (#557) branding · `1231d41` (#558) + `8a64537` (#559) setup-канал ·
  `864794b` (#560) хедлайнер · `d6a7c09` (#561) соседи · `25a61bb` (#562) журнал ребрендинга.
- **Прод:** три сервиса `active`, health 200, код `d6a7c09`; порция 1 оформления применена
  (сверено `groups.getById`), города 27/27 стоят.
- **Открытых PR:** нет.
- **Тесты:** полный прогон 2995 passed / 36 skipped (перед #558); дальше — целевые сюиты зелёные.

## Failed approaches (этой нитки)

- **Правка города/статуса через `groups.edit`** — пробовано трижды (city_id отдельно,
  country_id+city_id, status отдельно): API возвращает `ok=1` и **молча не применяет**
  (описание тем же вызовом применяется). Не повторять — только веб-интерфейс/браузер.
  Зафиксировано в [`group-setup-probe.md`](ops/group-setup-probe.md) и письме мозгу.
- **Слепой Enter в автоподсказке города** — первый пункт бывает чужой областью (Богородское →
  Московская обл.); выбирать только по скриншоту с «Кировская область».
- **`Down+Enter` для выбора города** — работает для уникальных имён, опасен для омонимов;
  конвейер переведён на визуальный клик.

## Открытые вопросы для пользователя

- 🖐 Верификация МАЛМЫЖ - ИНФО — подать заявку руками (критерии пройдены; кнопка в act=edit).
- 🖐 Ключи `COMM_<id>` батча №8 — выпущены ✅ (замечено при прогоне порции 1); МАМА редактором
  в трёх новых группах — проверить.
- 🔗 Блок «Ссылки» — проставить руками соседей и область (4–8 ссылок) — из прошлых сессий.
- 🟡 Прочее — секция ребрендинга и «Раскрутки» в PENDING.

## Не забыть (low-priority)

- ⚠️ **VK `error 129 Invalid photo` — транзиторный**: у одного региона падал аватар и
  проходила обложка, у соседнего наоборот; лечится ретраем (`--repair`).
- 🛠 **01.09 09:15 MSK** — первый живой прогон `dormant_recheck_disabled` (~48 вызовов ВК).
- 🛠 **04.12** — плашка «за полгода» на лендинге переключится сама.
- 🌐 VPN `happ-tun` на машине владельца режет SSH/gh — повтор помогает.
- 🛠 Точка «до» для замера отдачи ребрендинга — 2026-08-29 (~90 подписчиков суммарно у 25
  новых групп); окно оценки ≥2 недели.

---

> Если читаешь это в начале новой сессии — обнови через `/close_session` в конце. **Не аккумулируй history тут** — она в `git log --follow -- docs/SESSION_HANDOFF.md`.
