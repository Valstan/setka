# Session Handoff

> Sticky-note для непрерывности между сессиями разработки SETKA. Перезаписывается через [`/close_session`](../.claude/commands/close_session.md) — историю смотри через `git log --follow -- docs/SESSION_HANDOFF.md`.

**Status:** ACTIVE
**Updated:** 2026-09-22
**Branch:** `main`
**Last release in prod:** прод = `08b3743` (#759), main = то же. Четыре сервиса `active`,
health 200 за 1.0 с, smoke `OK` (220 из ВК, `would_publish=1`). Миграций в сессии не было.
**Выкачено девять PR за вечер** — отсюда и нитка ниже.

---

## 🛑 Токен VITA — решение принято, задачи больше нет

**Владелец 2026-08-21: «не буду ротировать».** Принятый риск, а не хвост работы. **Не открывать
заново и не предлагать ротацию на `/start`.** Радиус — секция `accepted-risk` в
[`PENDING_FOLLOWUPS.md`](PENDING_FOLLOWUPS.md).

## 🔴 Живое: ИИ-фильтр по-прежнему лежит (402)

DeepSeek отвечает `402 Payment Required` с 12:22 22.09, непрерывно. Сеть **публикует без
нейро-отбора**. **Оплата — ход владельца.** Всё в [P177](pending/P177-deepseek-402-i-oblachnaya-rutina.md).

Важно для сегодняшних правок: **срочность (`urgent`) на проде пока не работает и не может** —
без вердиктов множество срочных пусто, порядок публикации прежний. Проверять её можно только
после оплаты.

---

## Текущая нитка

**Дозор за девятью выкатами одного вечера.** Заказ владельца: «рефакторинг — почему не хватает
памяти, проверить на дублирование работы» плюс два прямых решения по ленте. Код закончен,
развёрнут и проверен на боксе; осталось **наблюдение за последствиями**, и они крупные.

Что изменилось в поведении сети:

1. **Окно кандидата 72 ч → 24 ч** (#752). Замер до выката: старше суток были 3.9 % публикаций,
   медиана возраста 4.5 ч. Но по новым районам Татарстана до 43 % (Верхний Услон 42.9,
   Менделеевск 36.4, Нижнекамск 25.8, Лаишево 18.2, Елабуга 16.7).
2. **Тема `addons` снята** (#757) — решение владельца по замеру: задуманному («добавка извне»)
   отвечали 8 % её публикаций. Это **−26 % объёма сети**. Подтверждено на проде: слот 22:20 не
   сработал.
3. **Память воркера** (#751): потолок RSS 350 МБ, тройка проверок разнесена с :15/:16/:17 на
   :15/:28/:57, заведён прибор `mem: task=`.
4. **Кэш стен** (#754/#755/#758/#759): работает половина «история своей стены» (2 КБ на запись),
   донорская выключена по замеру.
5. **Срочность и метрики в вердикте** (#753) — инертно до оплаты DeepSeek.

## Следующий шаг

1. **Первым делом — объём сети.** После снятия `addons` он обязан упасть примерно на четверть.
   Проверить, что упал именно на это, а не больше:

   ```sql
   SELECT wave_theme, count(*) FROM published_posts
   WHERE published_at > now() AT TIME ZONE 'UTC' - interval '2 days'
   GROUP BY 1 ORDER BY 2 DESC;
   ```

   Ориентир «до»: novost 2047, addons 1752, kultura 635, admin 624, reklama 566, union 509,
   sport 191, detsad 181 (за 7 дней). Если просело что-то кроме `addons` — причина в окне суток.
2. **Прибор памяти.** `grep 'mem: task=' logs/celery-worker.log` — кто поднимает потолок. За
   первый час назвал двоих: `check_recent_comments` +57 МБ, `check_unread_messages` +50 МБ.
   По этим показаниям решать, дробить ли тройку проверок по районам (в PR сознательно НЕ сделано).
3. **Дозоры в будильнике**: 26.09 — объём после снятия темы и после окна суток; 29.09 — память
   (`bash scripts/oom_probe.sh`). **Читать дозор 29.09 с поправкой на низкую базу**: убийства
   ядром шли ~2 раза в сутки до 14.09 и раз в четыре дня после PR #707, так что «ноль за неделю»
   сам по себе ничего не доказывает.
4. **Код-лекарство P178** — единственная невыполненная работа сессии: писать в
   `region_configs.bulletin_filters` только отличия от дефолта. Пока этого нет, первое же
   сохранение настроек района в UI закрепит текущие значения, и ловушка вернётся на других ключах.

## Контекст

- **План:** `C:\Users\valstan\.claude\plans\memoized-enchanting-pearl.md` — исполнен целиком,
  плюс четыре PR сверх него (появились из замеров по ходу).
- **Связанные коммиты сессии** (9 PR, все смержены и выкачены):
  - `8e1e260` #751 — память воркера: потолок RSS, разнос проверок, прибор, `scripts/oom_probe.sh`;
  - `4b97875` #752 — окно кандидата 24 ч, порог свежести 24→6 ч;
  - `e33aefb` #753 — метрики и возраст в промпт, `urgent`/`importance` в вердикте;
  - `200dfc1` #754 — кэш стен ВК;
  - `33cab76` #755 — **кэш своей стены не работал вовсе**, чинено хранением результата;
  - `3957d35` #756 — доки: анатомия `addons`, снятое закрепление у четырёх районов;
  - `42575a5` #757 — тема `addons` снята;
  - `f493377` #758 — донорская половина кэша выключена по замеру;
  - `08b3743` #759 — TTL истории 30 мин → 6 ч.
- **Правка данных на проде** (гейт #025, разрешение владельца получено): у `mi`, `vp`, `bal`,
  `kukmor` удалён ключ `max_post_age_hours` из `region_configs.bulletin_filters` (`UPDATE 4`).
  Закреплённых районов в сети — 0.
- **Открытых PR:** нет.
- **Письма мозгу этой сессии:** одно — [защитный потолок молча выключил то, что
  защищал](../mailbox/to-brain/2026-09-22-a-protective-cap-silently-disabled-the-thing-it-protected.md)
  (`kind: idea`, `compliance: suggest`, ответа не ждём).

## Failed approaches (этой нитки)

- 🔴 **Ставить защитный потолок, не проверив, что он не отсекает главный случай.** Потолок
  записи 256 КБ молча выключил кэш собственной стены района (~1 МБ) — то есть весь смысл фичи.
  Отказ писался в `debug` при прод-уровне INFO: **тесты зелёные, лог чист, работа не сделана**.
  Нашлось `redis-cli exists`, не чтением кода. **Правило:** защита, способная отключить
  защищаемое, обязана говорить это на прод-уровне логов.
- 🔴 **Кэшировать сырьё, когда нужен результат.** Снимок стены — мегабайт, список lip'ов из
  него — 2 КБ. Второе и в потолок проходит, и вызов снимает целиком.
- 🔴 **Ждать пользы от кэша донорских стен.** 2.3 % попаданий (11 против 474) при росте пика
  Redis 5.3 → 37.9 МБ. Причина структурная: одно ядро, районы по очереди, доноры у каждого свои.
  **Увеличение TTL не поможет** — поднимет не попадания, а резидентную память. Не соблазняться.
- 🔴 **Выключать «аварийный» путь, не посчитав, кто на нём держится.** Собирались снять запасной
  путь «читать весь пул района» — на нём целиком висела тема `addons` (1752 поста в неделю).
  Выключение погасило бы её молча. Считать ДО, а не после.
- 🔴 **Мягкий лимит времени задачи (`task_soft_time_limit`).** Корутины крутятся на ОДНОМ
  персистентном event loop (`utils/celery_asyncio`); `SoftTimeLimitExceeded` выбрасывает
  управление из `run_until_complete`, но корутину НЕ отменяет — она доигрывает во время
  следующей задачи, держа соединение из пула. Оставлен только жёсткий лимит; на это есть гейт.
- 🔴 **Рестарт воркера при непустой очереди.** Сделал при `llen celery = 16` — задача убита
  SIGTERM (вернулась по `acks_late`). Правило из прошлой сессии было процитировано мной же и
  тут же нарушено. Следующие выкаты делал проверкой очереди прямо в команде.
- 🔴 **Читать статус CI как `(.check_runs | length) == 0` → «готово».** Пустой список означает
  «проверок ещё нет», а не «все прошли». Ждать надо `length > 0 AND all(completed)`.
- 🔴 **Голый `./venv/bin/python -c` на проде** — не видит секретов (`DATABASE_URL` живёт в
  окружении systemd). Пайплайн проверять через `scripts/smoke_test.py`, БД — через
  `sudo -u postgres psql`.

## Заказ, пришедший в конце сессии (не начат)

**Вечерний сборник из непопавших в ленту** — [P182](pending/P182-vecherniy-sbornik-iz-nepopavshih-v-lentu.md).
Замер в тот же вечер: за сутки фильтры пропустили 3620 постов, **3141 не опубликован (87 %)**.
Дыру расширили сами двумя верными решениями — одна новость на пост (P168) и окно в сутки (#752):
что не попало в слот сегодня, не попадёт уже никогда.

Спецификация написана целиком (источник данных, место в расписании, роль DeepSeek, риски,
лимиты ВК). **Реализация не начата и не должна начинаться до оплаты DeepSeek** — без движка
тема выдаст механическую склейку, то есть ровно то, от чего владелец уходил. За владельцем —
имя рубрики (кандидаты в записи; «дайджест» отвергнут) и выбор одной из двух форм.

## Открытые вопросы для пользователя

- 🖐 **Имя рубрики вечернего сборника** и форма (P182) — вкусовое решение владельца.
- 🖐 **Оплата DeepSeek** — без неё сеть публикует без нейро-фильтра, и срочность из #753 мертва.
- 🖐 **Тонкие районы Татарстана** — владелец решил ждать дозора 26.09; если замолчат, лекарство
  есть и деплоя не требует (свой `max_post_age_hours` этим районам).
- 🖐 **Предложка** у Менделеевска, Нижнекамска, Верхнего Услона — включить руками.
- 🖐 **Блоки «Ссылки» и «Разделы»** у новых районов Татарстана — API их не пишет.
- 🖐 **Следующая порция оффера** — 9 ждут (висит с 12.09).
- 🖐 **P091 п.3:** «нет вердикта = не публиковать» — нужно слово владельца.
- 🖐 **Провайдеру:** vSwap и/или больше RAM. **ОРД VK:** первый контрагент. **ЮKassa.**

## Не забыть (low-priority)

- ⚠️ **Мандаты мозга не тронуты этой сессией:** [D-093 Сабантуй](../../brain_matrica/mailboxes/setka/from-brain/2026-09-22-d093-sabantuy-ingest-is-live-accept-grant-sabantuy-ingest-key-rubric-news-dry-run.md)
  (принять grant, строка конфига, сухой прогон — **отчёт к 29.09**) и
  [D-100 ревизия доступов](../../brain_matrica/mailboxes/setka/from-brain/2026-09-22-d100-remove-unnamed-and-duplicate-keys-disk-85-percent-three-dead-gateway-keys.md)
  (ключи `authorized_keys`, диск 85 %, мёртвые ключи шлюза — **отчёт к 06.10**). Оба `mandate`.
- ⚠️ **Диск бокса 84–85 %** — подтверждено замером, это половина D-100.
- ⚠️ **Три техдолга наблюдаемости не начаты:** [P172](pending/P172-kandidat-72-chasa-verdikt-24-chasa.md)
  (симптом 2 закрылся окном суток, 3 и 4 в силе), [P174](pending/P174-zakrytaya-stena-otdaet-nol-postov-bez-sleda.md),
  [P175](pending/P175-mertvye-pribory-last-checked-i-nulevye-parsing-stats.md).
  [P173](pending/P173-sborshchik-ne-govorit-pochemu-svodka-pusta.md) — владелец просил не трогать.
- ⚠️ **Три развилки по сторожу** — [P176](pending/P176-dashbord-storozha-utverzhdaet-polnoe-pokrytie.md).
- ⚠️ **Подосиновец** — источники выдохлись, кодом не чинится: нужен `/discover_communities` или
  решение, что район остаётся тонким.
- ⚠️ `reconcile_scheduled_publications` падает на FK `ad_scheduled_posts.created_by_user_id`.
- ⚠️ **Ротация `COMM_137760500`** (пакет P005, за владельцем).
- ⚠️ Дистилляция: проба 22.09 за 17 дней дала `count: 0` — сырья нет, молчим.
- ⚠️ `is_active = true` в `regions` даёт **57** строк: 55 районов + `kirov_obl` и `tatarstan_obl`.

---

> Если читаешь это в начале новой сессии — обнови через `/close_session` в конце. **Не аккумулируй history тут** — она в `git log --follow -- docs/SESSION_HANDOFF.md`.
