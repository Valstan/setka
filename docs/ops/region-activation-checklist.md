# Активация района: от скелета к живому региону

Чек-лист на раскатку «- ИНФО»-групп по Кировской области. Скелеты готовы —
районы лежат в БД неактивными, с засеянным пулом источников, брендингом и
localities; не хватает **только VK-группы**, куда публиковать сводки. Её создаёт
владелец руками, порциями **2–4 в день** (антиспам VK; решение зафиксировано при
батче №1 — через МАМУ не создаём, единственный publish-токен под риском
заморозки остановил бы все публикации).

Журнал раскатки — [`REGION_REFRESH_LOG.md`](../REGION_REFRESH_LOG.md).
Эталон, с которого списаны параметры ниже, — батч №1 (Яранск / Санчурск /
Кикнур, созданы 2026-07-21).

---

## Шаг 1. Владелец создаёт группу в VK

Параметры сняты с живых групп батча №1 — не по памяти, а через `groups.getById`.

| Поле | Значение |
|---|---|
| Тип | **Группа** (`type: group`), открытая |
| Название | `<РАЙОН> - ИНФО \| Афиша, новости, события` |
| Короткий адрес | `<код_района>_info43` |
| Тематика | Городское сообщество |
| Стена | Ограниченная (постит только администрация) |
| Обложка/аватар | как у соседних районов |

**Описание** — шаблон батча №1, меняются только склонения:

> Все важные новости **<Города>** и **<Района>** района в одном месте: события,
> администрация, культура, спорт, образование, афиша и частные объявления.
> Есть новость или знаете полезное местное сообщество? Напишите нам.

> ⚠️ **Капча на четвёртой группе.** Проверено на раскатке 2026-07-26: три группы подряд создаются
> без единой проверки, на четвёртой VK показывает «Подтвердите, что вы не робот». Ассистент капчи
> не проходит — планируйте **3 группы за заход**, остальные заводите сами или в другой день.
> Это уточняет прежнюю оценку «2–4/день»: верхняя граница техническая, а не административная.
> Подтверждено вторым замером 2026-07-27 — три группы снова прошли чисто.

### Порция 2026-07-26 — северо-восточный куст (сделана)

Выбраны не по размеру пула, а **связным кустом**: все четыре граничат друг с
другом, поэтому соседский обмен новостями (`share_neighbor_news`) заработает
сразу внутри группы, а не будет ждать активации соседей.

| Код | Название группы | Короткий адрес | id | Пул |
|---|---|---|---|---|
| `omutninsk` | ОМУТНИНСК - ИНФО \| Афиша, новости, события | `omutninsk_info43` | −240490491 | 43 |
| `belholunitsa` | БЕЛАЯ ХОЛУНИЦА - ИНФО \| Афиша, новости, события | `belholunitsa_info43` | −240490844 | 40 |
| `verhnekame` | КИРС - ИНФО \| Афиша, новости, события | `kirs_info43` | −240490945 | 33 |
| `afanasyevo` | АФАНАСЬЕВО - ИНФО \| Афиша, новости, события | `afanasyevo_info43` | −240491007 | 30 |

Все четыре активированы 2026-07-26; хвост — ключи `COMM_<id>` и МАМА редактором.

> У Кирса код района (`verhnekame`) не совпадает с названием города — короткий
> адрес человечнее взять по городу. Скрипт активации это умеет:
> `verhnekame=kirs_info43`. Программно используется только `vk_group_id`,
> короткий адрес ни на что не влияет.

### Порция 2026-07-27 — северо-западный куст (сделана)

Замкнутый треугольник: все трое граничат попарно, и **ни один не соседствовал
с активным районом** — поодиночке соседский обмен у них не заработал бы вовсе.
Подосиновец взят сразу, хотя пул у него меньше: соседей у него всего двое, и
без этой порции он остался бы изолированным надолго.

| Код | Название группы | Короткий адрес | id | Пул |
|---|---|---|---|---|
| `oparino` | ОПАРИНО - ИНФО \| Афиша, новости, события | `oparino_info43` | −240505553 | 30 |
| `luza` | ЛУЗА - ИНФО \| Афиша, новости, события | `luza_info43` | −240505724 | 28 |
| `podosinovets` | ПОДОСИНОВЕЦ - ИНФО \| Афиша, новости, события | `podosinovets_info43` | −240505855 | 24 |

> ✅ **Что на самом деле надо задавать руками — только описание и короткий адрес.**
> Диалог создания VK спрашивает название + тематику; всё остальное у новой группы
> уже совпадает с эталоном: `type: group`, открытая, `wall: 2` («публиковать —
> Администраторы и редакторы», «предлагать — Никто»). Проверено 2026-07-27 дважды:
> живые настройки `omutninsk_info43` через UI и `groups.getById` по трём новым
> группам. В раздел «Разделы» заходить не нужно.

### Порция 2026-08-19 — центральный куст (сделана)

Снова замкнутый треугольник, но, в отличие от батча №3, **не изолированный**: Слободской
граничит с уже активной Белой Холуницей, и тройка пришивается к северо-восточному кусту.
Взяты самые жирные остатки очереди и два крупнейших города области после Кирова.

| Код | Название группы | Короткий адрес | id | Пул |
|---|---|---|---|---|
| `yurya` | ЮРЬЯ - ИНФО \| Афиша, новости, события | `yurya_info43` | −240944863 | 40 |
| `slobodskoy` | СЛОБОДСКОЙ - ИНФО \| Афиша, новости, события | `slobodskoy_info43` | −240945121 | 28 |
| `chepetsk` | КИРОВО-ЧЕПЕЦК - ИНФО \| Афиша, новости, события | `chepetsk_info43` | −240945366 | 25 |

> У Кирово-Чепецка короткий адрес взят по городу без префикса «кирово-» (`chepetsk_info43`) —
> он совпадает с кодом района в БД.

### Порция 2026-08-22 — сшивка трёх кустов (сделана)

Выбраны **не по размеру пула, а по связности**: тройка замыкает разрыв между
северо-западным, северо-восточным и центральным кустами. Мураши граничат с
активными Лузой, Опарино и Юрьёй; Нагорск — с Белой Холуницей, Кирсом и Юрьёй;
Даровской — с Мурашами и Юрьёй. Соседский обмен получает перемычку, а не остров.

| Код | Название группы | Короткий адрес | id | Пул |
|---|---|---|---|---|
| `murashi` | МУРАШИ - ИНФО \| Афиша, новости, события | `murashi_info43` | −241004691 | 25 |
| `nagorsk` | НАГОРСК - ИНФО \| Афиша, новости, события | `nagorsk_info43` | −241004800 | 19 |
| `darovskoy` | ДАРОВСКОЙ - ИНФО \| Афиша, новости, события | ⚠️ нет, `club241004920` | −241004920 | 25 |

> ⚠️ **У Даровского короткий адрес не поставлен:** страница настроек именно этой
> группы стабильно отдаёт пустую оболочку (запрос содержимого не уходит вовсе, при
> 200 на всех ресурсах и без ошибок в консоли). Описание поставлено обходным путём —
> карточка «Заполните описание» в правой колонке страницы сообщества открывает
> отдельный диалог, не требующий страницы настроек. На работу это не влияет:
> программно используется только `vk_group_id`, а `activate_regions.py` принимает
> числовой id (`darovskoy=-241004920`).
>
> ✅ **Перепроверено 2026-08-25 механизмом из пункта 7 — вывод батча №5 устоял.**
> Найдя, что SPA не гидрируется без кадра, я предположил, что «пустая оболочка»
> Даровского была тем же самым, и проверил. **Не то же самое:** у трёх новых групп
> кадр поднимает `bodyLen` 497 → 1508 за секунды, а у `club241004920` страница
> остаётся на 497 после двух кадров и 24 с ожидания, `#group_edit_desc` не появляется
> вовсе. Отказ действительно привязан к этой конкретной группе, а не к пути и не к
> фокусу. Записано именно потому, что гипотеза была правдоподобной: следующий читатель
> наверняка захочет «починить» Даровского кадром — не сработает, не тратьте заход.

> 🔴 **Правка к граблинам батча №4 — прежняя запись описывала симптом, а не механизм,
> и потому не работала.** Проверено 2026-08-22 на трёх созданиях.
> 1. **Диалог создания — cross-origin iframe со `static.vk.ru`** (440×460), а не просто
>    «нет в дереве». Следствие жёсткое: `read_page`, `find`, `form_input` и любой JS в
>    родительской странице до него не достают в принципе. Только координатный клик.
> 2. **После таймаута CDP `Page.captureScreenshot` кадр не «съезжает на ~40px» — он
>    приходит неотмасштабированным** (1:1 с viewport 1920×N), тогда как обычный кадр
>    ужат до 1568 (коэффициент 0.8167), а клик всегда ждёт координаты в ужатом
>    пространстве. Поэтому старое правило «свежий скриншот перед финальным кликом»
>    не спасало: с такого кадра координаты читаются в чужой системе — так батч №4
>    потерял два создания, и так же потерялось первое создание Мурашей 22.08.
>    **Правило:** координаты кнопок диалога считать из рамки iframe через JS
>    (`getBoundingClientRect() * 1568/innerWidth`), а скриншот использовать **только**
>    для проверки, что поля заполнены, и никогда — как источник координат.
> 3. ~~**Первый клик по «Создать сообщество» после полной загрузки часто не открывает
>    диалог**: iframe грузится до 20 с (в центре спиннер). Не считать отказом —
>    подождать и кликнуть второй раз.~~ **Неверно — это снова был симптом. Механизм
>    найден 2026-08-25, см. пункт 7.** Верно осталось только одно: факт открытия
>    проверять по `w=groups_create_new` в URL, а факт создания — по
>    `groups.get(filter=admin)`, не по списку в UI.
> 4. **Синтетические события бесполезны:** VK слушает `pointerdown` и проверяет
>    `isTrusted`, поэтому `element.click()` и вручную собранная цепочка pointer/mouse
>    событий инертны.
> 5. **`groups.edit` user-токеном недоступен** («Unknown method passed») — управление
>    сообществом VK отдаёт только community-токену, которого у новой группы ещё нет.
>    Короткий адрес и описание ставятся только через интерфейс.
> 6. **Поле «Адрес» не пустое** — там лежит `club<id>`, ввод дописывается в хвост.
>    Через `form_input` по ref это не мешает (значение заменяется целиком); при ручном
>    вводе — очищать тройным кликом.
> 7. 🔑 **ГЛАВНОЕ (2026-08-25). SPA ВК не гидрируется, пока вкладка не композитится, —
>    и любой захват кадра её будит.** Это механизм, под которым лежали ТРИ разных
>    «симптома», описанных в этом файле по отдельности. Признак — `document.body.innerText.length`
>    ровно **497** (голая оболочка: только шапка и меню, никакого содержимого) при HTTP 200
>    на всех ресурсах и чистой консоли. Замер: клик по «Создать сообщество» **0 из 5** без
>    предшествующего кадра и **3 из 3** сразу после кадра; страница настроек трёх новых
>    групп — 497 → 1508 сразу после одного `zoom` 245×74.
>    **Правило:** перед первым взаимодействием со свежезагруженной страницей ВК сделать
>    один дешёвый `zoom` по любому мелкому региону (≈25 токенов) — он и разбудит SPA, и
>    подтвердит, что элемент на месте. Ждать дольше **бесполезно**: 30 с ожидания не
>    сдвигают `bodyLen` с 497 ни на символ, а один кадр сдвигает мгновенно.
>    Родня — [G49](../../../brain_matrica/cross-project-ideas/GOTCHAS.md) у мозга («композитор
>    не отдаёт кадр для перекрытого окна»), но **симметрично наоборот**: там не отдаётся
>    кадр, здесь не рендерится сама страница. И лекарство G49 («скриншоты нефатальны,
>    вердикт держат DOM-ассерты») здесь **не работает**: DOM-ассерт и есть то, что врёт.
> 8. **Название сообщества — максимум 48 символов** (счётчик `N / 48` в диалоге). Шаблон
>    `<РАЙОН> - ИНФО | Афиша, новости, события` даёт 38–42 символа, то есть запас есть,
>    но у длинных названий (например «БЕЛАЯ ХОЛУНИЦА») его почти нет — считать заранее.
> 9. ⚠️ **Чужие расширения браузера перехватывают контекст инструментов.** Симптом:
>    любой вызов (`javascript_tool`, `screenshot`, `left_click`) падает с
>    `Cannot access a chrome-extension:// URL of different extension`. Срабатывает на
>    фокусе поля ввода или сразу после ввода — похоже на автозаполнение. Перемежающееся,
>    случилось 2 раза из 5 заходов. **Лечение — `navigate` на ту же страницу** (сбрасывает
>    привязку фрейма); форма при этом теряется, поэтому заполнять её одним `browser_batch`,
>    а не по одному вызову. Ничего не создаётся, потери нет.
> Страница настроек (`vk.ru/club<id>?act=edit`) — классическая, **есть** в accessibility-
> дереве: поля «Описание»/«Адрес» и кнопка «Сохранить» берутся через `find` + `form_input`,
> без координат. ~~Грузится медленно (до 20–30 с) — `find` до загрузки даёт ложный отказ.~~
> **Поправка 2026-08-25:** она не «грузится медленно» — она не гидрируется без кадра (пункт 7).
> Порядок: `navigate` → `zoom` → `find` → `form_input` → клик «Сохранить». Факт сохранения
> виден по URL: `club<id>?act=edit` превращается в `<короткий_адрес>?act=edit`.

> ⚠️ **Очередь по пулу считать из БД, а не отсюда.** Список ниже уже дважды
> устаревал: массовый добор 2026-07-22 перетасовал порядок, а батч №4 снял с него
> три верхние строки. Любой список в этом файле — снимок, а не источник истины.
> ```sql
> SELECT r.code,
>        (SELECT count(*) FROM communities c WHERE c.region_id=r.id AND c.is_active) AS pool_active,
>        (SELECT count(*) FROM communities c WHERE c.region_id=r.id) AS pool_all,
>        r.neighbors
> FROM regions r
> WHERE r.is_active = false AND r.code <> 'test'
> ORDER BY pool_active DESC;
> ```
> Две поправки к прежней версии запроса (обе внесены 2026-08-25):
> **(а)** считать надо `pool_active` — допуск региона в волну требует
> `Community.is_active IS TRUE`, а `activate_regions.py` печатает пул без этого фильтра,
> то есть район с отключёнными сообществами покажет полный пул и войдёт «с нулём
> источников». На 25.08 расхождения нет (`pool_active = pool_all` у всех 11), но это
> совпадение, а не свойство. **(б)** без `code <> 'test'` в выдачу попадает служебный
> регион `test` (пул 9) — лишний кандидат на пустом месте.

### Порция 2026-08-25 — снятие головы очереди (сделана)

Выбор впервые решался **размером пула, а не изоляцией**: к этому моменту у всех 11
оставшихся районов уже есть хотя бы один активный сосед, и критерий «брать изолированных,
пока не застряли» (двигавший батчи №3 и №5) перестал к кому-либо применяться.

| Код | Название группы | Короткий адрес | id | Пул |
|---|---|---|---|---|
| `kotelnich` | КОТЕЛЬНИЧ - ИНФО \| Афиша, новости, события | `kotelnich_info43` | −241058223 | 32 |
| `shabalino` | ШАБАЛИНО - ИНФО \| Афиша, новости, события | `shabalino_info43` | −241058307 | 23 |
| `svecha` | СВЕЧА - ИНФО \| Афиша, новости, события | `svecha_info43` | −241058324 | 15 |

Замкнутый треугольник (все три пары симметричны в `neighbors`), у каждого есть активные
соседи: Котельнич — Арбаж, Даровской, Тужа, Верхошижемье; Шабалино — Даровской, Кикнур,
Тужа; Свеча — Даровской. Активация `activate_regions.py --apply`, все три ✅ без блокеров;
dry-run сразу после: Котельнич 44 поста / 10 сообществ / `would_publish=2`, Шабалино 15 / 5 / 1,
Свеча 5 / 4 / 1 — без публикации. Активных строк `regions` 32 → **35**. Капчи не было —
правило «3 за заход» подтверждено пятым замером.

> 🔴 **Рекомендация «Котельнич + Оричи + Орлов» из батча №5 была построена на неверном
> факте и здесь отменена.** Прежний текст гласил: «альтернатива по размеру пула — Суна (26),
> но она в другой части области и куста не образует». По `neighbors` из БД это неправда:
> `suna` содержит и `kumyony`, и `zuevka`, обратные рёбра на месте — **Суна + Кумёны +
> Зуевка образуют замкнутый треугольник с пулом 72**, то есть больше всех. Полный ранжир
> на 25.08: `suna+kumyony+zuevka` 72 > `kotelnich+shabalino+svecha` 70 >
> `kotelnich+orichi+orlov` 66. Хуже того, отменённая тройка — единственная из трёх, которая
> **разрушает** треугольник Котельнич–Шабалино–Свеча, забирая из него Котельнич.
> Урок тот же, что и с координатами: утверждение о связности («в другой части области»)
> писалось по памяти о карте, а проверяется одним `SELECT neighbors`.

**Дальше — `Суна + Кумёны + Зуевка` (пул 72).** Тройка не пересекается с только что
взятой, поэтому осталась целой. Остаток очереди после батча №6 (снимок 25.08, из БД):
Суна 26, Зуевка 23, Кумёны 23, Оричи 21, Уни 20, Фалёнки 14, Орлов 13, Богородское 10.
**Очередь и соседей всё равно пересчитать из БД** — любой список здесь снимок, он
устаревал уже трижды.

### Порция 2026-08-27 — верх очереди целиком (сделана)

Треугольник Суна + Кумёны + Зуевка из записи батча №6: пул 72 — больше всех
оставшихся, все три пары симметричны в `neighbors`, у каждого есть активные соседи
(Суна — Нема, Нолинск, Верхошижемье; Кумёны — Кирово-Чепецк, Слободской,
Верхошижемье; Зуевка — Белая Холуница, Кирово-Чепецк, Слободской). Очередь
пересчитана из БД перед заходом; преflight dry-run **до** создания дал у всех троих
штатное ⛔ «группа не найдена» — адреса были свободны.

| Код | Название группы | Короткий адрес | id | Пул |
|---|---|---|---|---|
| `suna` | СУНА - ИНФО \| Афиша, новости, события | `suna_info43` | −241117113 | 26 |
| `kumyony` | КУМЁНЫ - ИНФО \| Афиша, новости, события | `kumyony_info43` | −241117173 | 23 |
| `zuevka` | ЗУЕВКА - ИНФО \| Афиша, новости, события | `zuevka_info43` | −241117212 | 23 |

Активация `activate_regions.py --apply` под гейтом #025, все три ✅; dry-run сразу
после: Суна 11 постов / 6 сообществ / `would_publish=1`, Кумёны 8 / 6 / 1,
Зуевка 21 / 8 / 1 — без публикации. Активных строк `regions` 35 → **38**. Капчи не
было — правило «3 за заход» подтверждено шестым замером. Брендинг сверен по БД
**после** сохранения описаний — и снова поймал: у Суны, как у Свечи, **округ**
(`zagolovki.novost` = «Новости Сунского округа:»), описание группы переставлено.
Параметры всех трёх сверены `groups.getById` с эталоном `yaransk_info43` бит-в-бит.

> 🔴 **Главная находка порции: отказ инструмента ≠ отказ действия.** Клик по «Создать
> сообщество» для Зуевки упал ошибкой чужого расширения (пункт 9) — а группа при этом
> **создалась**: клик дошёл до страницы, оборвался только обратный путь инструмента.
> Слепой перезаход после такой ошибки создал бы дубль (спасла случайность — при
> повторе диалог не открылся, и весь ввод ушёл в подложку без последствий; проверено
> скриншотом и счётчиками). **Правило: после любого обрыва на финальном клике сначала
> `groups.get(filter=admin)` — не создалась ли группа на самом деле, — и только потом
> перезаход.** Это тот же класс, что «факт создания проверять по API, не по UI»
> (пункт 3), но жёстче: проверять надо даже когда инструмент уверенно сообщил об ошибке.
>
> ⚠️ Диалог создания открывается с задержкой в обе стороны: URL уже
> `w=groups_create_new`, а iframe ещё не смонтирован (rect null) — или URL меняется
> лишь через несколько секунд после клика. Перед заполнением проверять **rect iframe**,
> а не только URL; rect null → подождать и перемерить, не кликать вслепую.
>
> ℹ️ Масштаб кадра — не константа: в этом заходе окно владельца 946px давало 1:1
> (координаты со скриншота напрямую), прежние заходы — 0.8167 при 1920. Считать из
> фактического `innerWidth`, а не помнить число.

**Дальше.** Остаток очереди — 5 районов (снимок 27.08, из БД): Оричи 21, Уни 20,
Фалёнки 14, Орлов 13, Богородское 10. Замкнутый треугольник среди оставшихся один —
**Уни + Фалёнки + Богородское (пул 44)**; альтернатива — пара Оричи + Орлов (34),
оба уже с активными соседями. Очередь и соседей пересчитать из БД при заходе.

## Шаг 2. Владелец выпускает ключ сообщества

**Управление → Работа с API → Создать ключ** (права: `photos`, `docs`,
`messages`, `wall`, `manage`, `stories`, `market` — как у действующих).

Раздел «Работа с API» **виден только владельцу сообщества** — под МАМОЙ его нет
(проверено 2026-07-25). Ключ заносится через веб-интерфейс `/tokens` под именем
`COMM_<abs(group_id)>`.

Без ключа район всё равно заработает: публикация уйдёт каскадом
МАМА → VALSTAN ([дорожная карта токенов](../VK_TOKEN_ROADMAP.md) §2 —
VALSTAN последним, потому что его бан гасит вообще всё). Скрипт активации об
этом предупредит, но не заблокирует.

## Шаг 3. Резервный аккаунт в группе

МАМА подписывается на группу и назначается **редактором** — иначе резервное
плечо каскада публикации до неё не дотянется.

## Шаг 4. Соседи (один раз на всю область)

Миграция [`073_kirov_oblast_neighbors.sql`](../../database/migrations/073_kirov_oblast_neighbors.sql)
заполняет `regions.neighbors` для всех 24 скелетов сразу и симметрично. Она
**подготовлена, но не применена** — ждёт, чтобы владелец прочитал карту
соседства глазами (составлена ассистентом по географии). Для действующих
районов это no-op: соседи резолвятся с фильтром `is_active = TRUE AND
vk_group_id IS NOT NULL`, то есть неактивный сосед в сводку не попадает.

```bash
ssh sarafan "sudo -u postgres psql -d setka -f /home/valstan/SETKA/database/migrations/073_kirov_oblast_neighbors.sql"
```

## Шаг 5. Активация

[`scripts/activate_regions.py`](../../scripts/activate_regions.py) — сначала
отчёт готовности, запись только с `--apply`.

```bash
ssh sarafan "sudo bash -c 'set -a; . /etc/setka/setka.env; set +a; cd /home/valstan/SETKA && exec ./venv/bin/python scripts/activate_regions.py omutninsk belholunitsa verhnekame=kirs_info43 afanasyevo'"
```

> ⚠️ **Три части команды обязательны, иначе она не запустится.** `/etc/setka/setka.env` —
> `root:root 600`, поэтому без `sudo` + `set -a; . …` скрипт падает на
> `DATABASE_URL is not set` ещё до первого запроса. До 2026-08-25 в этом файле команда
> была записана без них (и в шаге 6 тоже) — ловушка ровно для того, кто пойдёт по
> чек-листу буквально.
>
> ⚠️ **`--dry-run` НЕ существует** — dry-run это поведение по умолчанию, а передача
> такого флага упадёт ошибкой argparse. Флагов ровно два: `--apply` и `--force`.

> ✅ **Преflight: прогонять dry-run ДО создания групп, а не после.** Хода не было ни в
> одном батче; введён 2026-08-25. Стоит ноль (без `--apply` скрипт не пишет), а ловит
> самый дорогой класс отказа: если короткий адрес `<code>_info43` занят чужой или
> сквоттерской группой, `resolveScreenName` вернёт ЕЁ, `groups.getById` даст название,
> пул непустой — и вердикт будет **✅ зелёный на чужое сообщество**. Единственная
> проверка, которая это ловит (`token_is_admin=False`), понижена в скрипте до ⚠️.
> Нормальный результат преflight — у всех ⛔ «группа не найдена в VK (искали `<code>_info43`)».
> Любая строка `группа: <чужое название> (id)` = адрес занят, туда не создавать.
> Тот же прогон заодно подтверждает, что READ-токен жив, и печатает пул.
>
> 🔴 **Смотреть на НАЗВАНИЕ в отчёте, а не на галочку.** При явном числовом id
> (`darovskoy=-241004920`) `_resolve_group` при падении `groups.getById` возвращает
> `(group_id, None)` — id не `None`, значит блокер «группа не найдена» **не сработает**,
> и `--apply` запишет мусорный `vk_group_id` в живой регион. Единственный признак —
> строка `группа: — (−241004920)` вместо `группа: <НАЗВАНИЕ> (−241004920)`.

Скрипт по каждому району находит группу в VK, считает пул, проверяет
community-токен, neighbors и права токена, и печатает вердикт. Различает:

- **⛔ блокеры** — активация даст неработающий район: группы нет в VK, региона
  нет в БД, пул пуст, регион уже активен с другой группой (перезапись — `--force`);
- **⚠️ предупреждения** — район заработает, но не всё сразу: нет
  community-токена (пойдёт каскадом), пусты neighbors (не будет соседского
  обмена), читающий токен не администрирует группу.

Когда все четыре зелёные — тот же вызов с `--apply` (это `UPDATE` прод-данных,
человеческий гейт [#025](../../AGENTS.md#автономия-под-гейтами-027) — выполняется
с подтверждением владельца в том же ходе).

## Шаг 6. Проверка живьём

```bash
ssh sarafan "sudo bash -c 'set -a; . /etc/setka/setka.env; set +a; cd /home/valstan/SETKA && exec ./venv/bin/python scripts/smoke_test.py --region omutninsk --theme novost --min-posts 1'"
```

> ⏱ **Окно «передумать» измеряется минутами, а не часами.** Ни один прежний батч этого
> не записал. Beat гоняет тематические волны на минутах 5/10/15/25/30/35/40/45/55 в часы
> 8–22, а планировщики выбирают регионы запросом к БД на каждом слоте — кэша активных
> регионов в процессе нет, **рестарт после `--apply` не нужен и отката «до следующего
> деплоя» не существует**. Включённый район уходит в ближайший слот и публикует
> по-настоящему. Если хвост владельца (ключ `COMM_<id>`) ещё не закрыт, первые сводки
> пойдут каскадом МАМА → VALSTAN. Откат — `UPDATE regions SET is_active=false,
> vk_group_id=NULL`, но уже ушедшую публикацию он не отменит.

Dry-run гоняет пайплайн без публикации: токены → VK → парсинг → фильтр → сборка
сводки. У батча №1 так и проверяли (Яранск 36 постов, Санчурск 1, Кикнур 17,
`would_publish=1` без реальной публикации).

После первой реальной сводки — записать район в
[`REGION_REFRESH_LOG.md`](../REGION_REFRESH_LOG.md) и снять его из списка
ожидающих в [`PENDING_FOLLOWUPS.md`](../PENDING_FOLLOWUPS.md).
