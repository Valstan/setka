# Ребрендинг сети ИНФО — журнал исполнения (август 2026)

> План утверждён владельцем 2026-08-29 (после разбора находимости,
> [`vk-findability-playbook.md`](vk-findability-playbook.md)). Здесь — что уже
> сделано, чем проверено и что осталось. Обновлять при каждом шаге.

## Сделано 2026-08-29 (одной сессией)

| Этап | Что | PR | Проверка |
|---|---|---|---|
| 0. Проба | Матрица «операция × токен» на полигоне; city/status оказались ловушкой #219 | #556 | [`group-setup-probe.md`](group-setup-probe.md) |
| 1. Графика | `modules/promotion/branding.py`: детерминированные аватары 1024² и обложки 2560×644, шрифт вендорен, Pillow в requirements | #557 | 10 тестов; контактный лист 25 аватаров принят владельцем |
| 2. Автооформление | `scripts/setup_groups.py`: dry-run/--apply/--repair/--rollback, идемпотентность через `promo_group_setup` | #558, #559 | **Порция 1 применена: 10 регионов** (afanasyevo…luza по алфавиту) — описание+аватар+обложка+визитка с закрепом; сверено `groups.getById`: `photo=1, cover=1, desc 243-260, pinned` у всех десяти |
| 3. Хедлайнер | Сильнейший пост волны — отдельной короткой публикацией (обоснование: замер Кирса 437k vs 167 на одном контенте) | #560 | 8 тестов; выкачен, воркер перезапущен, smoke `ur/kultura` OK |
| 4. Соседи | Футер «Ленты соседей» в сводках (ротация по дню) + гейт `#Новости` снят (канал публиковал 0 по всей сети — замер прод-логов) + CTA в витрине соседа | #561 | 3 теста; выкачен |
| Города | **Все 27 сообществ**: «Калинино»→свой райцентр (25 новых) + пустые у Арбажа и Пижанки | браузером (API не умеет — #219) | сверка всех 27 живым `groups.getById` |

Ключи `COMM_<id>` для uni/falenki/bogorodskoe владелец выпустил ещё до прогона —
описания и обложки у них встали community-ключами штатно.

### Аномалия Кирса — решена

`stats.get` работает под VALSTAN (интервалы, охваты, подписки по дням).
27–28.08 у Кирса: охват 213k+125k, +73 подписки, география — вся Россия.
Источник — пост id 593 (некролог СВО, 653 знака, 2 фото): **436 966 просмотров,
14 559 лайков, 823 комментария** через рекомендации. Контроль: тот же некролог
в той же волне внутри сводки-простыни (пост 592) — **167 просмотров**.
Разница 2600× на одном контенте → этап 3 (хедлайнер) построен на этом замере.

## Сделано 2026-08-31

### 🔴 Обложки обрезались у всех десяти — причина оказалась не там, где искали

Жалоба владельца: «обложки не вмещаются в окно обложки, обрезаются». На скриншоте
у Кикнура вместо «КИКНУР - ИНФО» — «КИКНУР - И».

**Две правки, и первая была верной, но недостаточной.** Это главное, что стоит
запомнить: правильно найденный баг не обязан быть единственным.

| # | Что нашли | PR | Помогло? |
|---|---|---|---|
| 1 | `COVER_CROP` просил у ВК прямоугольник **1590×400** из холста **2560×644** — ВК резал 38% ширины | #579 | нет, обрыв остался |
| 2 | Холст рисовался **3.975:1**, а ВК хранит обложку **2.5:1** — он ужимал картинку в свою пропорцию и резал бока при любом кропе | #581 | **да** |

**Замер, закрывший вопрос** — `groups.getById(group_id=<id>, fields="cover")` по
трём сообществам вернул один и тот же набор: `200×80`, `400×160`, `911×364`,
`1080×432`, `1920×768`. Все 2.5:1, ни одного 3.975. Холст переведён на
**1920×768**, безопасная зона — **1196×768** (мобильный клиент показывает не всю
ширину). Проверено глазами на живой странице Кикнура: заголовок и тэглайн видны
целиком.

> ⚠️ **Пропорцию холста не менять «на глаз».** Она обязана совпадать с той, в
> которой обложку хранит ВК. Проверка — одна команда выше; сторож
> `tests/test_promotion/test_cover_crop.py` держит 2.5:1 и объясняет, почему.

**Почему отказ дожил до жалобы:** со стороны кода всё было успешно — API вернул
`ok`, `groups.getById` показывал `cover=1`, в логах ни одной ошибки. Увидеть
обрезку можно было только глазами. Ровно pool #229.

**Новый режим `--force-cover`** (#580, #582): перезалить обложку там, где её
ставил этот скрипт. Обычный прогон такое не чинит — он считает режим по живому
снимку, и сообщество с кривой обложкой выглядит оформленным (`spot`). Бамп
`TEMPLATE_VERSION` тоже не помогает: версия решает, писать ли запись в журнал, а
режим считается раньше. Чужое оформление недоступно в принципе — цели берутся из
`promo_group_setup`. **User-бюджет VALSTAN не расходуется**: обложка ставится
community-ключом.

### Порция 2 автооформления — 10 из 22, дальше упёрлось в дневной лимит

`scripts/setup_groups.py --apply` прошёл 10 full-регионов и остановился сам:
бюджет VALSTAN 50 вызовов ≈ 10 регионов. Обложки у всех 13 наших сообществ
перезалиты в новой пропорции (`--force-cover`, 13/13).

## Осталось

- **Порция 2, продолжение** (9 регионов: `tuzha`, `uni`, `ur`, `verhnekame`,
  `verhoshizhem`, `vp`, `yaransk`, `yurya`, `zuevka`) — бюджет VALSTAN на
  2026-08-31 израсходован, запустить `scripts/setup_groups.py --apply` следующим
  днём.
- **Аватары семи регионов** (`nagorsk`, `oparino`, `podosinovets`, `sanchursk`,
  `shabalino`, `slobodskoy`, `svecha`) — упали на перемежающейся ошибке ВК
  `129 Invalid photo`, статус записи `error`. Добить `--repair` (тратит
  user-бюджет, поэтому вместе с пунктом выше). Обложки у них уже поправлены
  бесплатным `--force-cover`.
  ⚠️ **Ошибка 129 у ВК перемежающаяся и частая:** на обложках добивалась со
  2–4-го прохода. Ретраить, а не диагностировать.
- **UGC-действия в операторской ленте** (этап 5): сборщик предложки уже работает
  (beat :15, читается user-токеном — проверено), достроить в `/notifications`
  кнопки «одобрить→опубликовать» (`wall.post(post_id=…)` user-токеном) /
  «отклонить» (`wall.delete`) / «автор в ЧС». Публикация предложенного поста —
  только user-токен (community — err 27).
- **Верификация МАЛМЫЖ - ИНФО** (этап 6, owner-шаг): критерии пройдены
  (5316 подписчиков, оформление есть, хедлайнеры дадут оригинальный контент).
  Подача: страница `malmig_info` → Управление → «Отметки сообщества» →
  Верификация → «Подать заявку» (кнопка видна в act=edit).
- **Мониторинг отдачи** (этап 7): beat-таска `stats.get` VALSTAN-токеном
  (reach/reach_subscribers/subscribed по дням) в снимок + график в
  `/subscriber-growth`. Окно оценки ребрендинга — ≥2 недели с 2026-08-29.
- **Статусы сообществ**: в новом интерфейсе групп поля статуса нет ни в
  настройках, ни на странице (проверено DOM) — только у старых пабликов.
  Канал закрыт, не тратить время.

## Как мерить (напоминание из плейбука)

Не судить раньше 7–10 дней; смотреть источник переходов «Результаты поиска
ВКонтакте» и подписки в `region_member_snapshots`; бенчмарк рынка ER≈0,21%.
Точка «до» — 2026-08-29: суммарно у 25 новых групп ~90 подписчиков.
