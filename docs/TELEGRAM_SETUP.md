# Настройка Telegram уведомлений для SETKA

## 🤖 У вас уже есть боты!

Создавать нового **НЕ НУЖНО**. В проекте уже есть 2 бота:

### 1. VALSTANBOT
- **Токен:** `489021673:AAH7QDGmqzOMgT0W_wINvzWC1ihfljuFAKI`
- **Использование:** Основные уведомления системы

### 2. AFONYA (Малмыж Инфо Поддержка Бот)
- **Токен:** `5945194659:AAGIIXBSr3gSwyCSan_oY7l4p0D8LZ_UF4c`
- **Использование:** Поддержка пользователей

---

## 📋 Что нужно сделать:

### Шаг 1: Получить chat_id

**Вариант А - Через скрипт (рекомендуется):**

```bash
cd /home/valstan/SETKA
source venv/bin/activate
python scripts/get_telegram_chat_id.py
```

Скрипт попросит вас:
1. Отправить `/start` боту в Telegram
2. Нажать Enter
3. Покажет ваш `chat_id`

**Вариант Б - Вручную через браузер:**

1. Откройте Telegram, найдите бота (поиск по username)
2. Отправьте боту `/start`
3. Откройте в браузере:
   ```
   https://api.telegram.org/bot489021673:AAH7QDGmqzOMgT0W_wINvzWC1ihfljuFAKI/getUpdates
   ```
4. Найдите в JSON ответе: `"chat":{"id":123456789...}`
5. Скопируйте это число

**Вариант В - Через специального бота:**

1. Найдите в Telegram бота `@userinfobot`
2. Отправьте ему любое сообщение
3. Он пришлет вам ваш chat_id

---

### Шаг 2: Добавить chat_id в конфиг

Откройте файл `/home/valstan/SETKA/config/config.secure.py` и добавьте:

```python
# Telegram Monitoring
TELEGRAM_ALERT_CHAT_ID = "ваш_chat_id_здесь"  # Например: "123456789"
```

---

### Шаг 3: Протестировать

Запустите тест:

```bash
cd /home/valstan/SETKA
source venv/bin/activate

# Отредактируйте scripts/test_monitoring.py - добавьте ваш chat_id
# Затем запустите:
python scripts/test_monitoring.py
```

Вы получите тестовое сообщение в Telegram!

---

## 🔔 Как это будет работать:

После настройки, система будет автоматически отправлять вам:

### 1. Error Alerts (🚨)
Когда что-то сломается:
```
🚨 SETKA Error Alert

📍 Module: VKMonitor
⏰ Time: 2025-10-08 16:00:00
❌ Error: VK API rate limit exceeded
📝 Details: ...
```

### 2. Success Notifications (✅)
При успешных операциях:
```
✅ SETKA Notification

📍 Module: Publisher
⏰ Time: 2025-10-08 16:00:00
💬 Successfully published 5 posts to mi region
```

### 3. Daily Reports (📊)
Ежедневные отчеты:
```
📊 SETKA Daily Report

⏰ 2025-10-08 09:00:00

🌍 regions: 14
📡 communities: 50
📝 posts: 120
🆕 new_posts: 25
🤖 analyzed: 20
📤 published: 15
```

---

## 🎯 Пример использования в коде:

```python
from modules.monitoring.telegram_notifier import TelegramNotifier

# Создать notifier
notifier = TelegramNotifier(
    bot_token="489021673:AAH7QDGmqzOMgT0W_wINvzWC1ihfljuFAKI",
    chat_id="ваш_chat_id"
)

# Отправить сообщение
await notifier.send_message("Привет из SETKA!")

# Отправить error alert
await notifier.send_error_alert(
    "VK API error",
    module="VKMonitor",
    details="Rate limit exceeded"
)

# Отправить статистику
await notifier.send_stats_report({
    "regions": 14,
    "new_posts": 10,
    "published": 5
})
```

---

## ⚙️ Интеграция с мониторингом:

Health checker уже настроен для автоматической отправки alerts:

```python
from modules.monitoring.health_checker import HealthChecker
from modules.monitoring.telegram_notifier import TelegramNotifier

# Создать notifier
notifier = TelegramNotifier(bot_token="...", chat_id="...")

# Создать health checker с notifier
checker = HealthChecker(telegram_notifier=notifier)

# Запустить мониторинг
await checker.start_monitoring(interval_seconds=300)  # Каждые 5 минут
```

Теперь при любых проблемах вы получите уведомление в Telegram!

---

## 🔍 Troubleshooting:

### Бот не отвечает:
- Проверьте токен бота
- Убедитесь что бот не заблокирован
- Проверьте что отправили `/start`

### Не приходят сообщения:
- Проверьте правильность chat_id
- Проверьте что chat_id это строка: `"123456789"`
- Попробуйте отправить тестовое сообщение вручную

### 404 ошибка:
- Проверьте токен бота
- Возможно бот был удален

---

**После настройки у вас будет полноценная система мониторинга с оповещениями!** 🎉

