# 🔑 GATEWAY_KEY_DKMALMYZH — выпущен, привязан на 33, доставлен, grant'ы выданы (mandate brain 2026-09-01 ×3)

> Запись `P160` реестра [PENDING_FOLLOWUPS](../PENDING_FOLLOWUPS.md). Индекс грепается, запись читается целиком, реестр целиком не читается никогда ([D-097](../../AGENTS.md)).

`⏱ 2026-09-01 · snooze 0 · parked · условие расконсервации: первый живой вызов от DKMALMYZH в gateway_requests (не наш ход — ДК забирает ключ сам)`

**Вечер 01.09 — оба довеска мозга закрыты.** (1) Поправка «33, не 27»: `--owner-ids=<33>` без
`--rotate` → `action: rebound`, секрет не тронут; все шесть новых стен `200` ключом ДК,
чужой id `403`. Каксинвай-2 (`-218593948`) отдаёт `count=0` — открытая группа на 2 участника
с пустой стеной, не запрет. (2) **D-061**: grant'ы `setka→dkmalmyzh` на `GATEWAY_KEY_DKMALMYZH`
(19:36:00 UTC) и `ECOSYSTEM_KEY` (19:36:43 UTC) — «Действует», основание D-061 в аудите обеих
комнат. **Граница, названная мозгу:** у КАРМАНа grant выдаётся только server-action из GUI под
сессией владельца комнаты с `mfa:true` (`createGrant` → `ownership(user, …)`); room-токен и
provisioning-ключ права не имеют намеренно. Выдано из браузера владельца управляемой
вкладкой; в аудите КАРМАНа обе записи от `owner:1`. Чтобы D-061 стал буквальным, нужен
grant-endpoint у КАРМАНа под токеном комнаты-источника — предложено письмом
`2026-09-01-d061-done-two-grants-live-binding-33-…`. `ecosystem-self-serve.md` §0 переписан.

Заявка `DKMalmyzh/mailbox/to-brain/2026-08-31-vk-gateway-key-request.md`, мандат brain
01.09. Сайт ДК стал районным порталом культуры, разделы наполняются лентами 27 сельских
ДК; прямых VK-токенов проект не заводил и не станет — нет ключа, нет импорта, и это
видно в журнале, а не деградирует тихо.

**Исполнено в день получения.** Ключ `DKMALMYZH`, восьмой потребитель шлюза, привязка
D-047 на **27 owner_id** (24 сообщества + **3 личные страницы с положительным id**),
значение в комнате КАРМАНа `setka` под именем `GATEWAY_KEY_DKMALMYZH`, эхо приёмника
сошлось побайтово ([#110](../../../brain_matrica/cross-project-ideas/ideas/110-delivered-means-echo-from-receiver.md) — «отправлено ≠ доставлено»).
Отчёт: `mailbox/to-brain/2026-09-01-dkmalmyzh-key-issued-27-owners-frequency-fits.md`.

**Секрет не попал в вывод сессии ни разу.** Скрипт выпуска печатает его один раз — вывод
уехал в root-only файл, наружу отданы только длина и 12 знаков sha256; доставка в комнату
и приёмка читали файл сами. После приёмки обе копии на боксе `shred -u`. Живых носителей
два: хэш в `gateway_keys` и значение в комнате.

**Положительные id проверены ДО выдачи, обе половины отдельно** (brain предупредил, что
молчаливое предположение «owner_id отрицательный» уронило бы три записи):
`provisioning._validate_gateway_binding` пишет знаковое целое, все 27 доехали;
`gateway/scope` пропускает `wall.get`/`users.get` с положительным `owner_id`. Отрицательного
не требует никто, кроме `group_id`-параметров, где положительное `N` и означает `-N` —
это про форму вызова, а не про форму привязки.

- ⚠️ **`+420841463` привязка отвергает.** В письме id личных страниц записаны с плюсом,
  а регулярка `-?\d{1,20}` знака `+` не знает — выдача упала бы на `not an id`. Голые цифры.
- ⚠️ **`--owner-ids <список>` не работает**, когда список начинается с `-217788511`:
  argparse читает значение как ещё один флаг. Только форма `--owner-ids=<список>`.
- ⚠️ **`GATEWAY_KEY_*` в комнате наш bootstrap не принимает сознательно** — имя читается
  как ВХОДЯЩИЙ ключ шлюза, приняв его из комнаты, мы при недоступной БД открыли бы
  собственную дверь env-фолбэком (`modules/secrets_bootstrap`, `ACCEPTED_SUFFIXES`).
  Значение будет каждый старт попадать в `ignored` по имени — **ожидаемо, не тревога**.

**Приёмка на проде — 10 проверок, все зелёные**, четыре из них негативные: своя стена
`200`+3 поста; все три личные страницы `200`+3 поста; `utils.resolveScreenName` `200`
(глобальный метод, привязки не требует); неверный ключ `401`; чужой `owner_id` `403`;
чужой **положительный** `owner_id` `403`; свой id + чужой `domain` `403` (контрабанда);
`wall.post` `400`.

**Частота — раз в полчаса подтверждена замером**, а не оценкой. `gateway_requests` за
7 суток: пик **10 вызовов/мин**, p95 = 7, самые загруженные сутки за 30 дней — 66 вызовов.
Квоты: 30/мин и 5000/сутки на ключ, 120/мин суммарно. Их план 27 × 48 = **1296/сутки =
26% суточной квоты**; общий пик шлюза станет ~35–38/мин, меньше трети глобального бюджета.

- 🟡 **Первый прогон в минутное окно не влезет.** Окно — календарная минута (`epoch//60`),
  не скользящая. 27 вызовов за 57 с обычно лягут в два окна, но при неудачном старте — в
  одно: 27 из 30. Первый прогон несёт ещё 8 разовых `utils.resolveScreenName` → 35 → `429`.
  Не поломка (`Retry-After` отдаём, их обработчик на это рассчитан), но резолвинг лучше
  прогнать отдельным проходом. Сказано им в отчёте.
- 🟡 **`domain=` вместо `owner_id` получит 403** — в привязке 27 id, screen names нет.
  Их схема (`resolveScreenName` → числовой id → `wall.get`) совпадает, поэтому не блокер.
  Имена сразу не добавляли: потолок `MAX_BINDING_TARGETS = 50`, а 27 id + 27 имён = 54.
  Понадобятся — добавим выборочно (27 + 4 нечисловых = 31, место есть). Четыре будущих
  учреждения района (Малый Китяк, Нослы, Дерюшево, Савали) тоже влезут.
- 🖐 **ХОД ВЛАДЕЛЬЦА: grant `setka→dkmalmyzh` + `ECOSYSTEM_KEY`** — честная граница
  self-serve ([#116](../../../brain_matrica/cross-project-ideas/ideas/116-self-serve-needing-a-key-from-someone-elses-box.md)).
  В `/etc/dkmalmyzh/dkmalmyzh.env` не ходим — проект забирает сам.
- ✅ **Два спящих ключа погашены 01.09 по решению владельца.** `DK_MALMYZH` (через
  подчёркивание) и `SABANTUY_MALMYZH` были активны с 13.07 при **нуле реальных вызовов
  за всю историю** — единственные строки usage-лога у обоих датированы самим моментом
  `--import-env`. Заготовки, которые никогда не доехали до проектов: ДК Малмыжа в заявке
  прямо пишет, что ключей у него нет вовсе. Это был неиспользуемый живой доступ — у
  первого к стене, которая и так есть в новой привязке `DKMALMYZH`; у второго вообще без
  привязки (`НЕ ПРИВЯЗАН`, owner-scoped → 403), но в перечне он числился потребителем.
  `--disable` **обратим**: `--enable` вернёт тот же секрет, а выключенный в БД ключ
  env-фолбэком не воскресает. Сверено по производному перечню: у обоих `active = НЕТ`.
  Тот же класс, что мозг ловил 03.08 на `CDK_KALININO` — реестр говорил одно, БД другое;
  разница в том, что теперь перечень производный, и расхождение видно, а не выясняется
  письмом. Если `SABANTUY_MALMYZH` понадобится под свою OIDC-выдачу — включать вместе с
  привязкой, а не до неё.
