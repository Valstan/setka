# Прогон 01.09 — порция 2 выкачена, обложки закрыты целиком, аватары упёрлись в 129

> Запись `P106` реестра [PENDING_FOLLOWUPS](../PENDING_FOLLOWUPS.md). Индекс грепается, запись читается целиком, реестр целиком не читается никогда ([D-097](../../AGENTS.md)).

**Разведка исправила саму задачу — плана из handoff в природе не было:**

- **порция 2 — не 9 регионов, а 5.** `tuzha`, `ur`, `verhoshizhem`, `vp` пришли в режиме
  `spot` (авторское оформление, скрипт их не трогает). Правки нужны были только `uni`,
  `verhnekame`, `yaransk`, `yurya`, `zuevka` — **25 user-вызовов вместо 45**;
- **записей `error` было восемь, а не семь, и список другой.** У `nagorsk` аватар стоял —
  падала только обложка, а её добил `--force-cover` 31.08 (тот путь статус в журнале не
  переписывает, поэтому строка `error` осталась висеть враньём). Зато не хватало `orichi`:
  его починка 31.08 упала на аватаре;
- **аватар стоит 4 user-вызова за попытку**, до трёх попыток — то есть до 12 на регион,
  а не 4. Именно этого в счёте handoff и не было.

**Сделано (сверено живым снимком через `groups.getById`, не журналом):**

- **описания — 13 из 13**, все 243–256 знаков, шаблон v3;
- **обложки — 13 из 13.** Порция 2 добита `--force-cover` (community-ключ, **ноль**
  user-бюджета): 3 из 5 с первого прохода, `yurya` со второго, `verhnekame` с третьего;
- **визитки и закрепы — у всех пяти регионов порции 2**;
- **аватары — встали 4** (`yurya` в основном прогоне; `oparino` с третьей попытки,
  `sanchursk` со второй, `podosinovets` с первой — в `--repair`). `nagorsk` и `orichi`
  оказались уже целыми.

**Расход: ~61 user-вызов при потолке 50** — перебор осознанный и доложен владельцу по ходу.
Считается бюджет **per-run**, а не за сутки: `--apply` и `--repair` друг о друге не знают,
так что скрипт не остановит. Тормознул на 129, как и договаривались.

**~~🖐 Осталось: 7 аватаров~~ — ЗАКРЫТО 2026-09-06: их оказалось не 7, а 10, и дооформлены все.
См. «Прогон 06.09» ниже.**
- 🖐 **Город и статус** у пяти регионов порции 2 — только руками в веб-интерфейсе
  (`Уни`, `Кирс`, `Яранск`, `Юрья`, `Зуевка`), API их не правит (ловушка #219).
- ⚠️ **Ошибка 129 у ВК перемежающаяся и частая** — ретраить, а не диагностировать. За день
  01.09: обложки взялись с 1–3-го прохода, аватары с 1–3-го, `shabalino` не взялся за три.

**🟡 Техдолг закрыт тем же заходом: `--repair` докладывал провалом собственный успех.**
Вызывающий определял исход поиском слова «ошибки» в тексте сводки, поэтому регион, у
которого аватар встал со второго-третьего захода, писался в журнал как `error` — ровно то,
ради чего ретрай и написан. На живом прогоне так пострадали `oparino` и `sanchursk`:
аватары стоят, строки говорили «провал». Теперь `repair_region` сам возвращает `ok`,
считая его **по недостающему**, а не по тексту; след ретраев уезжает в колонку `after`,
чтобы частота 129 не терялась. Тот же класс, что гейт, называвший не тот счётчик (#588).
+7 тестов, включая гвоздь в прежнее правило.
