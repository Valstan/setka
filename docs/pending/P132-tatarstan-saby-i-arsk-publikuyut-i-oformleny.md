# ⏳ Татарстан: Сабы и Арск публикуют и оформлены — остались «Ссылки», «Разделы» и пересчёт ранга (заказ владельца 2026-09-10)

> Запись `P132` реестра [PENDING_FOLLOWUPS](../PENDING_FOLLOWUPS.md). Индекс грепается, запись читается целиком, реестр целиком не читается никогда ([D-097](../../AGENTS.md)).

`⏱ 2026-09-10 · snooze 0 · watch · 11.09 оформлены; ранг мест пересчитать ~25.09, когда наберутся посты`

Первая порция Татарстана за пределами Кировской области: `saby` (Сабинский район, −241411261,
`saby_info16`, пул 54) и `arsk` (Арский, −241411317, `arsk_info16`, пул 53). Скелет — миграция
101 (PR #688), активированы 10.09 под гейтом #025, smoke novost у обоих `would_publish=2`.
Подробности и грабли — [чек-лист активации](../ops/region-activation-checklist.md) (порция
2026-09-10) и [`REGION_REFRESH_LOG.md`](../REGION_REFRESH_LOG.md).

**11.09 — донастроены и оформлены** (заказ владельца «донастрой и дооформи»). Ключи
`COMM_241411261` / `COMM_241411317` владелец внёс 10.09 в 17:02–17:03, МАМА — администратор
обеих групп. `setup_groups.py --apply --regions saby,arsk`: описание-индекс, обложка, визитка и
закреп (`wall-241411261_30`, `wall-241411317_40`); аватар у обеих упал на `129 Invalid photo`
и дозалит `--repair --by-snapshot` с первой попытки. Независимый `--audit`: аватар ✓, обложка ✓,
журнал `applied`; публичные страницы проверены глазами. Город — «Богатые Сабы» и «Арск»
(сверено `groups.getById`). Места: `localities` заполнены (Сабы 65, Арск 121), ранг новой
формулой (PR #691) записан, строка мест в описаниях перечитана из ВК. Областная группа
`tatarstan_obl` получила «Балтаси, Кукмор, Арск, Сабы».

⚠️ **Длинные прогоны на проде — только через `nohup … &` с логом.** 11.09 SSH оборвался посреди
`--apply` (`Connection reset by peer`): процесс на боксе доработал, но вывод пропал, и состояние
пришлось восстанавливать аудитом и журналом `promo_group_setup`. Повторный `--apply` вслепую
задвоил бы визитку.

**Осталось:**
- 🖐 (владелец) блок «Ссылки» — API его не пишет; «Разделы» к эталону не сверялись;
- ранг мест ниже третьего места держится на 1–2 упоминаниях (посты копятся с 10.09) —
  **~25.09 пересчитать** `--rank-localities --apply` и `--refresh-desc --apply --regions
  saby,arsk`;
- 🟡 сверка города в `setup_groups.py` ловит литерал «Калинино» (город из профиля владельца на
  11.09), а не «город группы ≠ город района». Сменится город в профиле — следующая группа родится
  с чужим городом незамеченной. Записано мозгу как известный риск (письмо 11.09); до починки у
  каждой новой группы сверять `city` через `groups.getById` руками.

**Дальше по Татарстану** — соседи новых районов: Тюлячинский (граничит с Кукмором, Сабами и
Арском), Мамадышский (Кукмор), Атнинский и Высокогорский (Арск). Выбирать кустом по связности,
как в Кировской области.
