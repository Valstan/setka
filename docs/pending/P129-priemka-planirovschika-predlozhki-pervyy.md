# ✅ Приёмка планировщика предложки — первый автоматический выход прошёл (09.09 10:00 МСК)

> Запись `P129` реестра [PENDING_FOLLOWUPS](../PENDING_FOLLOWUPS.md). Индекс грепается, запись читается целиком, реестр целиком не читается никогда ([D-097](../../AGENTS.md)).

`⏱ 2026-09-09 · snooze 0 · ✅ ПРИНЯТО 2026-09-09 — 6 строк published, 6 ad_publications, 6 awaiting, 6 пингов владельцу доставлены`

Первый выход планировщика без участия человека. Проверено на живых данных, не по коду.

**Что вышло в 10:00:00–10:00:58 МСК** (лог воркера в МСК, БД пишет UTC — расхождение из
секции «`DEFAULT now()` отдаёт MSK» мешает читать оба источника рядом, учитывать при
разборах):

- строка 14, `kind=suggested` — реконсилер опознал выход из отложки ВК:
  «строка 14 — отложка 78294 вышла как `-158787639_78395` (двойник по подписи/тексту)»;
- строки 15–19, `kind=repost` — пять дублей в соседние сообщества
  (`-168172770`, `-180812597`, `-168170215`, `-166980909`, `-179203620`),
  все через `publish-token:MAMA`, по ~13 секунд между записями;
- 6 строк `ad_publications` (id 42–47), все `published`, по 200 ₽, `paid_status=unpaid`;
- 6 `ad_payments` в `awaiting` по 200 ₽ (id 19–24), суммарно 1200 ₽;
- строк, которым срок вышел, а статус не терминальный, — **ноль**.

**Пинги владельцу доставлены — проверено, а не предположено.** Успешную отправку
`owner_ping` не логирует вовсе, поэтому пустой греп по логу ничего не доказывал бы. Но
дедуп-ключ **снимается при неудаче отправки** (`release_dedup`), значит его наличие и есть
доказательство доставки: в Redis лежат все шесть `setka:cabinet_ping:awaiting:14…19` с TTL
≈ 62 900 из 86 400, то есть поставлены ≈ 6,5 ч назад — ровно в 10:00.

🟡 **Хвост: `owner_ping` молчит об успехе.** Из-за этого единственный способ убедиться в
доставке — окольный, через TTL ключа в Redis. Одна строка `logger.info` на успешную
отправку сделала бы приёмку прямой. Не блокер, но следующий разбор снова упрётся в это.

🟡 **Две мёртвые строки `suggested` висят рядом с живыми заменами.** id 26 (26.09) и 27
(30.09) — `failed` с `VK API error: [15] Access denied: cannot publish post`, обе созданы
04.09 23:30 и провалились **в момент создания** (`attempts=0`, `updated_at == created_at`):
их исходные посты 77681/77683 к тому времени исчезли из предложки. 05.09 в 11:41 владелец
перепланировал — появились id 28 и 34 на те же даты, они `scheduled` и живы, репосты
привязаны именно к ним. То есть сегодняшний выход это не задело.

Открытая часть: на 26.09 и 30.09 в матрице планировщика на один слот приходится по две
строки — `failed` и `scheduled`, — и отличить живую от мёртвой в интерфейсе нельзя. Сканер
`vanished` (PR 1.5) помечает исчезнувшие из предложки **заявки**, но в
`ad_scheduled_posts` не смотрит. Варианты: помечать такие строки отдельным статусом и
прятать из матрицы, либо чистить при успешной замене на тот же слот.
