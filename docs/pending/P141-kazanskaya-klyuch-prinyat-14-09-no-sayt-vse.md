# 🟡 Казанская: ключ принят 14.09, но сайт всё равно не в `CONVEYOR_SITES` — доставок ноль

> Запись `P141` реестра [PENDING_FOLLOWUPS](../PENDING_FOLLOWUPS.md). Индекс грепается, запись читается целиком, реестр целиком не читается никогда ([D-097](../../AGENTS.md)).

`⏱ 2026-09-14 · snooze 0 · fresh · половина причины снята, вторая осталась`

Найдено попутно при разведке D-091: `accept_secret_grants.py --list` показывал **пять**
выдач `KAZANSKAYA_INGEST_KEY` от `kazanskayamalmyzh`, все со решением `accept` — то есть
allowlist их пропускал, а скрипт после выдачи ни разу не запускался. В
`conveyor_deliveries` у сайта `kazanskaya` при этом **ноль строк любого статуса**.

**Снято 14.09:** прогон приёма (тот же, что принял ключ публикации портала) принял
новейшую выдачу id=13, и ключ доехал в процесс — `ingest=есть(48)`. Четыре старых дубля
той же ссылки отвалились по 500: имя уже занято. Это остаток на стороне Казанской,
чинить нам нечего — их сторона переподавала одну и ту же ссылку пять раз за сутки.

**Включено 14.09 по решению владельца:** `CONVEYOR_SITES=vmalmyzhe,kazanskaya`.

**И это ничего не изменит, пока не починят источник — замерено до включения.** Сухой прогон
по Казанской дал **0 отобранных** и в окне 7 дней, и в окне 30. Причина не в ключе и не в
allowlist'е, а в цепочке отбора: пост обязан (1) прийти из названного паблика, (2) пройти
фильтр, (3) войти в **опубликованную** сводку района `mi`. Замер по единственному источнику
`source_owner_ids=(217788511,)` — «ДК Малмыжа»:

| проверка | за 30 дней |
|---|---|
| сообщество живо | да: `active`, последний пост 12.09 |
| постов собрано | **1** |
| из них оставлено фильтром | **0** |
| вошло в опубликованные сводки `mi` | **0** |

Для сравнения, топ источников опубликованных сводок `mi` за то же окно — 78, 50, 38, 31…
постов. ДК в этот список не входит вовсе.
