# Рекламная рубрика ре-транслирует чужие агрегаторы объявлений → риск бана VK

> Запись `P023` реестра [PENDING_FOLLOWUPS](../PENDING_FOLLOWUPS.md). Индекс грепается, запись читается целиком, реестр целиком не читается никогда ([D-097](../../AGENTS.md)).

- 🟡 `⏱ 2026-07-08 · snooze 0 · parked · условие расконсервации: повторный бан аккаунта либо жалоба VK` (ре-триаж 2026-08-09: 32 дня; статус приведён в соответствие с решением владельца 07-08 «только код-фикс, пересмотреть при повторных банах» — это и есть явное условие, а `watch` заставлял пункт всплывать месяц подряд без новой информации. За 32 дня после `is_hard_spam` повторных банов не было) (ре-триаж 2026-07-27: 19 дней) **Системный риск.** Инцидент Уржум 2026-07-08: рекламная сводка
  (`theme=reklama`, расписание 10:00) ре-транслировала пост источника-агрегатора «ОУ» (Объявления Уржум,
  community id 328, `category=reklama`) со **скам-объявлением** «удалённая работа / рассылка рекламы / по
  готовой системе» → VK-антиспам забанил аккаунт владельца на 4 дня. Публикация шла community-токеном
  `COMM_168170215`, но VK банит **аккаунт-админа за контент**, токен не защищает. **Surgical-фикс задеплоен:**
  `is_hard_spam` (`utils/text_utils.py`) режет скам/увод-в-обход-VK **до публикации для всех тем, включая
  reklama** (шаг 5a в `advanced_parser._filter_post`; аудит-причина `hard_spam`). Легальные объявления
  остаются. **Осталось (deferred, решение владельца от 2026-07-08 «только код-фикс»):** такой reklama-источник
  «объявления/барахолка/работа» есть в **~каждом из 15 регионов** — рассмотреть отказ от ре-трансляции чужих
  агрегаторов целиком (оставить в reklama только свои объявления через ad_cabinet). Не блокер, пока держит
  `is_hard_spam`; пересмотреть при повторных банах.
- ✅ **Вторая дыра того же риска закрыта 2026-08-20** — и она была шире записанной. `is_advertisement`
  при `theme='reklama'` возвращает False **первой же строкой**, то есть в рубрику проезжала и
  коммерческая реклама с легальной маркировкой (`erid:`, `#реклама`, «на правах рекламы») — ровно то,
  за ре-трансляцию чего ВК банит аккаунт админа. **А официальная метка ВК `marked_as_ads` не
  проверялась ВООБЩЕ НИ В ОДНОЙ теме** (в PENDING этого не было; найдено красным прогоном:
  `is_advertisement` смотрит только текст, а флаг ВК в `_filter_post` не доходил).
  Новый шаг 5c режет размеченную рекламу во всех темах, включая `reklama`; определение одно на три
  вызова — `ads_filter.is_marked_advertisement` (копия жила в `krugozor_broadcast._is_promo`, а на
  пути парсинга не жила вовсе), зеркало в `collection_audit` по ADR-0004, причина дропа `marked_ad`.
  Рубрика при этом не обнуляется: маркеры узкие, их носит рекламодатель, а «продам мёд, 500 руб,
  тел …» не носит — это стережёт отдельный тест. 6 тестов, показаны красными.
- 🟢 **Найдено попутно, НЕ чинил — продуктовое решение владельца.** Fallback «нет доноров темы →
  берём все communities» (`tasks/parsing_scheduler_tasks.py:229`) распространяется и на `reklama`:
  район без своей доски объявлений строит рекламную сводку из **всего** пула источников — в рубрике,
  где рекламный фильтр выключен. Риск шире «доноров-агрегаторов»: он про любой паблик района.
  Исключить `reklama` из fallback — значит, часть из 15 регионов останется без рекламной сводки
  вовсе. **Вопрос владельцу:** принимаем такое обеднение ленты или оставляем fallback?
- ~~🟡 **`black_id` конструктивно не ловит агрегаторы**~~ **✅ ЗАКРЫТО 2026-09-01 как
  теоретическое — правкой кода быть не должно.** Механизм описан верно (проверка после
  `clear_copy_history`, по автору оригинала), но **замер на проде снял вопрос целиком**:
  `black_id` пуст у **всех 44** региональных конфигов, и `posts_filtered_black_id` за 30 дней
  равен **0** при 6 707 848 просмотренных постах. Рычага, который «не работает», в проде
  просто нет — ни в нынешнем виде, ни в исправленном.

  Разбор показал, что писать эту правку было бы **вредно**, и это стоит помнить:
  1. **Формулировка преувеличена.** Собственные посты агрегатора отсекаются уже сегодня
     (у не-репоста `owner_id` = сам агрегатор). Не отсекается только то, что он
     **пересылает**, — а это по определению чужой материал, и его автор проверяется той же
     строкой. Новое правило срабатывало бы ровно там, где автор оригинала **чист**.
  2. **Рычаг уже есть и точнее.** В боевом пути ретранслятор — всегда активный донор пула,
     а для него на `/communities` есть кнопка «Деактивировать»: мгновенно и обратимо. Правка
     сделала бы «в black_id» ≡ `is_active=False`, то есть завела бы вторую, неявную точку для
     того же решения.
  3. **Агрегатора в ленте и так не видно:** атрибуция строится по развёрнутому оригиналу,
     имя ретранслятора в сводке не появляется. Исчезла бы не «видимость агрегатора», а новость.
  4. 🔴 **Правка не только вычитает — она подменяет победителя.** Отсев на шаге 4 идёт **до**
     регистрации отпечатков, поэтому переписанный near-dup, который сегодня проигрывает
     ретранслированному оригиналу, выжил бы и вышел **вместо** него. Это лобовое столкновение
     с решением владельца 2026-08-30 «первоисточник — тот, кто вышел раньше», и счётчик
     дропов такую подмену не показал бы в принципе: он считает вычитание, а не замещение.
  5. **Отравление корпуса.** `collection_audit._derive_drop_reason` вывел бы такому посту
     чужую причину, и она уехала бы в `collected_post_audit`, которым кормится классификатор.

  **Если тема когда-нибудь вернётся:** сначала продуктовое решение владельца, а не код.
  Дёшево и полезно независимо — поправить текст в `/filtration`: «блокирует автора поста;
  чтобы выключить ретранслятора — деактивируйте сообщество в `/communities`».

- 🟢 **Мёртвый код вокруг репостов** (найдено при разборе выше, 2026-09-01, ничего не ломает):
  `repost_from_owner_id` и `is_repost` (`utils/post_utils.py`) пишутся и **не читаются ни одним
  модулем**; `modules/filters/structural.py:106-177` (`BlacklistIDFilter`) — параллельная
  реализация того же отсева по таблице `filters`, в боевой Celery-путь не входит (только
  `scripts/run_production_workflow.py` и `scripts/test_filter_pipeline.py`). Кандидаты в
  ближайший `/deadcode`.

- 🟡 **`community_vk_id` в пути парсинга не выставляется никогда** (`advanced_parser.py:604`,
  найдено 2026-09-01): для репоста групповой гейт `filter_group_by_region_words` ищется по id
  автора оригинала и молча не применяется. Отдельная запись сознательно — это второе
  расширение отсева со своей ценой, мешать его с предыдущим нельзя.
