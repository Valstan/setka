# ~~PWA Радара: манифест и выход из сессии остались от старого адреса~~ ЗАКРЫТО 2026-09-01

> Запись `P026` реестра [PENDING_FOLLOWUPS](../PENDING_FOLLOWUPS.md). Индекс грепается, запись читается целиком, реестр целиком не читается никогда ([D-097](../../AGENTS.md)).

- ✅ `⏱ 2026-08-02 · закрыто 2026-09-01` Манифест отдаётся маршрутом по хосту
  (`main.radar_manifest`: `/manifest.webmanifest` на радар-хосте со `start_url`/`scope` = `/`,
  `/radar/manifest.webmanifest` на прочих), оба в `PUBLIC_EXACT` — браузер тянет манифест без
  cookie. **`id` = `/radar` в обоих** — иначе Chrome счёл бы переехавший `start_url` другим
  приложением и не обновил бы установленные. Статический файл удалён (два источника правды).
  `login_url` в шаблон → `radar.js` (401 и выход) ведёт на ЕСА с `next` на корень своего хоста.
  `sw.js` v7: пуш/клик по уведомлению — относительно scope, серверный `/radar` на корневом
  scope открывается как `/`. Проверка владельцем на телефоне: переустановленный Радар открывается
  standalone с корня — ⏳ ждёт.
  <details><summary>История</summary>
  Хвосты того же захода, что и https. Не блокеры,
  но всплывут, как только кто-то поставит Радар как приложение.
  - `web/static/radar/manifest.webmanifest`: `start_url` и `scope` = `/radar`, а на
    `радар.вмалмыже.рф` кабинет живёт на корне → установленное приложение стартует за пределами
    своего scope и открывается не в standalone. Правильное лечение — отдавать манифест роутом,
    зависящим от хоста (как уже сделано для самого интерфейса), а не статикой.
  - `web/static/js/radar.js`: после выхода и при 401 ведёт на **относительный** `/login`, то есть на
    `радар.вмалмыже.рф/login`, а не на единый вход. Работает (там свой `/login`), но мимо ЕСА;
    адрес issuer'а уже приезжает в шаблон как `services_url` — рядом можно отдать `login_url`.
  </details>
