# ~~🔑 D-074 — отозвать `GATEWAY_KEY_CDK_KALININO` (mandate brain 2026-09-05)~~ ЗАКРЫТО 2026-09-06 в день получения

> Запись `P119` реестра [PENDING_FOLLOWUPS](../PENDING_FOLLOWUPS.md). Индекс грепается, запись читается целиком, реестр целиком не читается никогда ([D-097](../../AGENTS.md)).

`⏱ 2026-09-05 · snooze 0 · ✅ ЗАКРЫТО 2026-09-06 — ключ id 1 деактивирован, отчёт мозгу ушёл`

ДК доложил приёмку раздела Калинино (44/44, редирект стоит) — по плану D-074 отзыв ключа шлюза
за нами. Исполнено `scripts/issue_gateway_key.py CDK_KALININO --disable`: `action: disabled`,
**id 1, `is_active = f`, 2026-09-06**. Обратимо — `--enable` вернёт тот же секрет; в БД
выключенный ключ env-фолбэком не воскресает (`modules/gateway/keys.py:84`, источник у ключа
и так был `db` без env-копии).

**Ключ был живой, а не спящий** — это стоит помнить, если решение будут пересматривать:
45 `wall.get` + 2 `video.get` за 30 дней, **ровно один вызов в сутки в ~03:50 UTC**, ни одного
отказа, последний 06.09 03:57. Это ночной автоимпорт Калинино с Бокса 1; его падение с `401` —
ожидаемо и по мандату не наш инцидент (юнит и таймер — отдельный мандат Калинино).

**Лента не осиротела:** группа Калинино `-218991929` входит в привязку живого `DKMALMYZH`
(3448 вызовов, последний 06.09 11:36) — контент в портал культуры тянет ДК своим ключом.
Проверено до отзыва, а не после.

Подтверждено двумя независимыми чтениями (не выводом самой команды): строка `gateway_keys`
и производный перечень `list_gateway_keys.py`.
