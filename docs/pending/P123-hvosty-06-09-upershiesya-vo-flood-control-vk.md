# ⏸ Хвосты 06.09, упершиеся во flood control ВК

> Запись `P123` реестра [PENDING_FOLLOWUPS](../PENDING_FOLLOWUPS.md). Индекс грепается, запись читается целиком, реестр целиком не читается никогда ([D-097](../../AGENTS.md)).

`⏱ 2026-09-07 · snooze 0 · fresh · троттлинг снят 07.09, описания дописаны; остались «Ссылки» (41 запись) и 14 переименований`

За день сделано 40 записей `groups.edit`; после этого ВК закрыл канал. Остаток:

- ✅ **9 описаний — закрыто 07.09.** Троттлинг проверен **пробоем, а не по времени**: одна
  запись на `zuevka` прошла и подтверждена независимым перечитыванием. Остальные восемь
  дописаны **четырьмя порциями по две группы с паузами ~50 с** между порциями. Итог сверен
  полным срезом: 43 из 43 «уже шаблонное». Подробности — в секции 📝 ниже;
- **14 переименований** короткого адреса — **через браузер тоже не вышло**. Проба на `arbazh`:
  поле заполнено, «Сохранить» нажато, форма приняла клик — адрес **не изменился**, ссылка
  «Управление» осталась на `arbazh_info`. Ошибки не показано. Дальше страница настроек перестала
  прогружаться совсем. Ничего не сломано, `arbazh_info` на месте. Имена утверждены владельцем
  (основа сохраняется, меняется суффикс; `malmig_info` → **`malmyzh_info43`**, `vp_inform` →
  `vp_info43`, татарстанские → `_info16`). Привязка `VMALMYZHE` уже держит оба имени Малмыжа,
  так что переименование безопасно в любой момент;
- **блок «Ссылки»** в 41 группе — решение владельца: соседи + областная лента + витрина сети.
  Не начинали, тот же барьер.

**Урок для следующего массового прогона:** 40 записей подряд исчерпывают квоту, и следующая
операция того же аккаунта — включая ручную правку в браузере — начинает молча не срабатывать.
Массовые правки делать порциями с паузами.
