# ~~🎯 Кировская область: осталось два района~~ ✅ ЗАКРЫТО 2026-08-31 — область закрыта целиком

> Запись `P104` реестра [PENDING_FOLLOWUPS](../PENDING_FOLLOWUPS.md). Индекс грепается, запись читается целиком, реестр целиком не читается никогда ([D-097](../../AGENTS.md)).

`⏱ 2026-08-31 · snooze 0 · ✅ ЗАКРЫТО 2026-08-31 — Оричи и Орлов заведены, оформлены, активированы`

Замер прода после работы: **41 район активен, неактивных 0.** Свободных скелетов в
Кировской области не осталось.

- `orichi` → `orichi_info43`, `vk_group_id = -241197723`, пул 21;
- `orlov` → `orlov_info43`, `vk_group_id = -241198096`, пул 13.

Группы созданы владельцем-агентом через браузер (по поручению владельца 2026-08-31 —
прежнее ограничение «только руками владельца» снято им же в этом заходе), приведены к
эталону `yaransk_info43` бит-в-бит, активированы под гейтом #025, оформлены
`setup_groups.py --apply` в режиме `full` + `--repair` по перемежающейся ошибке ВК 129.
Обложки **проверены глазами**, а не только по API.

**Три находки уехали в [`region-activation-checklist.md`](../ops/region-activation-checklist.md)
(батч №9), потому что чек-лист в этих местах врал:**

1. **Тематика из диалога создания больше не сохраняется надёжно** — и её отсутствие
   заставляло страницу настроек молча не сохранять НИЧЕГО, включая короткий адрес и
   описание (`/groupsedit.php?act=save` → `["Укажите тематику сообщества.", false]`).
   Ставится теперь двумя уровнями в «Дополнительной информации», выбор — только
   клавиатурой.
2. **Дефолты новой группы разошлись с эталоном:** `wall` приходит `1` (стена открытая).
   Запись «в „Разделы“ заходить не нужно» отменена; правки там требуют явного
   «Сохранить», иначе откатываются при перезагрузке молча.
3. **`spot` в отчёте `setup_groups.py` не означает «оформление стоит»** — только
   «описание длиннее 240». Я на этом ошибся: увидел `spot` и счёл дело сделанным.
   Проверять `groups.getById(fields=cover)` и глазами.

Плюс: **сообщения сообщества** у новой группы могут быть выключены, и тогда COMM-ключ
не проходит валидацию (`[15] group messages are disabled`) — у Оричей так и было,
включено.
