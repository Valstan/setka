# 🔐 ЕСА — три обещания мозгу от 04.09: уведомление о передаче `name`/`email`, RP-initiated logout, ответ на аудит инструкций

> Запись `P110` реестра [PENDING_FOLLOWUPS](../PENDING_FOLLOWUPS.md). Индекс грепается, запись читается целиком, реестр целиком не читается никогда ([D-097](../../AGENTS.md)).

`⏱ 2026-09-04 · snooze 0 · ✅ ЗАКРЫТО 2026-09-04 — все три обещания исполнены в тот же день (PR #614, #615)`

1. ~~**Уведомление на странице входа**~~ ✅ 04.09: строка «Входя, вы разрешаете передавать
   сервисам экосистемы ваше имя и e-mail из этого аккаунта» в `login.html`.
2. ~~**`end_session_endpoint`**~~ ✅ 04.09: discovery отдаёт `end_session_endpoint`;
   `GET|POST /oidc/logout` (`post_logout_redirect_uri`, `client_id` | `id_token_hint`, `state`)
   гасит куку `.вмалмыже.рф`, возврат — только на origin зарегистрированного `redirect_uri`
   клиента, иначе `/login?logged_out=1`; на `/login` уже вошедший видит «Выйти из всех
   сервисов». Реестра `post_logout_redirect_uris` нет сознательно (миграция ради того же
   origin). Письмо мозгу `2026-09-04-end-session-live-…` — клиентам одна строка редиректа.
   🖐 **Приёмка владельцем:** войти в ПОЗВОНИ → на `вход.вмалмыже.рф` нажать «Выйти из всех
   сервисов» → ПОЗВОНИ при следующем входе должен спросить ЕСА заново.
3. ~~**Аудит инструкций под Fable 5.1**~~ ✅ 04.09 (PR #615): все High и Medium применены,
   тег `pre-fable-freeze-2026-09-02` на `d79b286`, приёмочный grep пуст; оставлен hot-fix-вопрос
   в `reliz.md` (развилка ADR-0002 §8, не happy-path); vendor-зеркала `.agents/` удалены;
   SessionStart-хук `scripts/session_start.sh` печатает handoff. Письмо
   `2026-09-04-instruction-audit-applied-setka.md`. 🟢 Остаток: укоротить `/start` до
   «почта + задача», когда хук проверен на второй машине.
