# 🎛 Программа «Кабинет рекламодателя под ключ» — Этап 0: планировщик предложки (заказ владельца 2026-09-05)

> Запись `P117` реестра [PENDING_FOLLOWUPS](../PENDING_FOLLOWUPS.md). Индекс грепается, запись читается целиком, реестр целиком не читается никогда ([D-097](../../AGENTS.md)).

`⏱ 2026-09-05 · snooze 0 · fresh · Этап 0 выкачен 05.09 (#627 бэкенд + #628 UI, миграция 095, прод 67d9186); probe на полигоне ждёт предложенного поста от не-админа; Этапы 1–4 на проде (#630–#648, миграции до 100); Этап 5 выкачен 06.09: PR-1 фото (#649), PR-2 liveness демона (#650), PR-3 тексты бота (#651), прод 0bd7176; программа «Кабинет под ключ» — вся на проде, остались приёмки владельца`

**Заказ (шесть этапов, план `~/.claude/plans/partitioned-tickling-token.md`):** аудит кабинета и ВК-бота
как продукта (заказ → одобрение → выход → оплата → счётчики), гибкое расписание предложки с дублированием
в соседей (срочно — 5 постов Анны Валиевой в МАЛМЫЖ-ИНФО), скидки с полом 200 ₽ + пакет «безлимит 30 дней»
+ закреп, токен-роутер (личность + способности), рассылка предложения 503 + 143 адресатам сообществ,
уведомления владельцу. Решения владельца 05.09: предложка публикуется САМА с подписью автора, соседям —
репост; личку VALSTAN/MAMA читать по API нельзя (у токенов нет scope `messages`, замер 05.09 — err15 у
всех трёх, включая VITA); кабинеты адресатам заводить заранее; скидка 5 %/3 поста, потолок 30 %, сгорает
1-го; постоянным +10 %; безлимит 5000 ₽/30 дней с суточным лимитом на сообщество.

**Этап 0 — сделано (PR `feat/suggested-planner`):** миграция 095 (`kind`/`source_post_id`/
`next_attempt_at`/`attempts` + уникумы), `VKPublisher.publish_suggested` / `pin_post` / `unpin_post`,
`publish_reconciler.record_published`, `modules/ad_cabinet/suggested_planner.py`,
`modules/ad_cabinet/repost_dispatcher.py` (beat `ad-repost-dispatch` каждую минуту + watchdog),
API `suggested-plan/options` / `suggested-plan` / матрица, `cancel_scheduled` по виду строки,
`scripts/probe_suggested_wall_post.py`.

- ⏳ **Probe на полигоне `-137760500`** (ход владельца: положить в предложку полигона пост с
  не-админского аккаунта, затем `SETKA_PROBE_CONFIRM=yes ./venv/bin/python
  scripts/probe_suggested_wall_post.py --group -137760500 --post <id> --apply --minutes-ahead 5 --wait
  --revert`). Итог — сюда raw-строками (post_type/signer_id до и после выхода, возвращённый post_id) и в
  env: подпись сохраняется → `AD_SUGGESTED_VK_POSTPONE=1` (дефолт); теряется → `=0` (режим queue).
- ⏳ **PR-3 UI**: блок «Предложка → размещение» во вкладке «Планировщик» `/ad` + матрица «пост × дата ×
  дублёры» + кнопка «В планировщик» у карточки инбокса.
- ~~⏳ **Деплой** (`/reliz`)~~ ✅ 05.09: миграция 095 применена раннером, три сервиса active, health 200,
  smoke OK, `dispatch_ad_reposts` тикает раз в минуту (0 строк). Остались: один план на полигон после
  probe, затем владелец сам ставит даты постам Анны (`ad_clients.id=10`, 16 предложенных постов по сети).
- ⏳ **Этап 1 (аудит):** ✅ PR 1.1 (#629) — реконсилер считает «сейчас» по МСК (`publish_reconciler.now_msk`),
  beat каждые 15 мин 8–23:45. ✅ PR 1.2 — миграция 096: уникумы «одна публикация / один awaiting на
  отложку», «один активный пост клиента в сообщество в день»; `FOR UPDATE` в реконсилере и обеих
  отменах; операторская раскладка коммитит строки до VK и идемпотентна по `client_ref`; клиент
  **архивируется** (`ad_clients.is_archived`, `POST /clients/{id}/unarchive`) вместо DELETE с каскадом
  по оплатам; очередь модерации видит pending-сирот (OUTER JOIN); хотфикс #631 — дублёры по `abs()`
  в SQL (на проде `vk_group_id` отрицательный), `refresh(region)` после отката. ✅ PR 1.3 — сторожа денег:
  долговой гейт trusted (`AD_TRUST_DEBT_LIMIT`, дефолт 2000 ₽, или awaiting старше `AD_DEBTOR_DAYS` →
  заказ на одобрение), одобрение с прошедшей датой требует `publish_at` (409 → UI спрашивает дату),
  реконсилер пингует о зависшей отложке (2 ч без подтверждения, дедуп 12 ч), сторож pending с прошедшей
  датой (beat `ad-pending-watch` :05, 8–23: пометка + пинг + одно уведомление клиенту), сборщик фото
  падает в `failed` вместо поста без картинок, удалить фото активного поста нельзя (409). ✅ PR 1.4 —
  деньги пакетов в `ad_payments` (`provider=package`, миграция 097 бэкфилл), «заказано» без
  отказов, свежесть по подтверждению оплаты/выходу/заявке (не по ответу владельца и визиту), клиенты
  из бота в списке по умолчанию, перерасход без пакетных публикаций, деньги и выход в ленте.
  ✅ PR 1.5 — очередь модерации: заказ целиком, полный текст, фото (owner-роут), названия районов,
  обязательная причина отказа, автообновление, `/moderation/count`; сканер помечает исчезнувшие из
  предложки заявки (`vanished`).
  ✅ PR 1.6 — пинги владельцу (`feat/ad-owner-pings`): заказ trusted-клиента пингует всегда
  (текст «ждёт одобрения / долг / уже в отложке»), бот различает `order`/`order_direct`,
  «вышел пост — ждёт оплаты» из `record_published` (дедуп сутки), онбординг per-client вместо
  бюджета 5/час, дедуп-ключ возвращается при провале обоих каналов, чат-пинг 10 мин с превью,
  суточные алёрты экранируют имена и помечают перерасход только после 200 от Telegram.
  Тесты кабинета больше не ходят в Redis/Telegram/ВК (autouse `_owner_pings_offline`).
  ✅ PR 1.7 — «Я оплатил» (`feat/ad-payment-claims`, миграция 098 `ad_payments.claimed_at`):
  кнопка в кабинете (по счёту и «всё») и «✅ Оплатил» в боте → `claimed_at` + пинг владельцу;
  владелец подтверждает заказ (`/orders/{ref}/confirm-paid`) или все счета клиента одной
  кнопкой «Всё оплачено», клиенту уходит «оплата получена» (и при правке одной строки);
  «заявил оплату» в списке кабинетов и в бейдже очереди; свободный текст бота вне шага —
  в чат, а не в приветствие; доверие — по разным заказам (`AD_TRUST_AFTER_ORDERS`), не по постам.
  ✅ PR 1.8 — фото/инфраструктура (`feat/ad-photos-infra`): `AD_UPLOAD_DIR` (прод —
  `/var/lib/setka/ad_uploads`), пол свободного места при загрузке (507), `web/uploads/` в
  `.gitignore`, ретенция фото раз в неделю (`ad_photo_retention`), архив фото в бэкапе,
  чекбоксы «в этот пост» в кабинете, `client_reply` через `to_thread`.
  Этап 1 закрыт.
  ✅ PR 2A — скидки и пол цены (`feat/ad-pricing-discounts`): `pricing.quote_for_client`
  (прайс → 5 %/3 оплаченных поста в месяце ≤30 % + 10 % постоянным → пол 200 ₽/размещение,
  не выше прайса → закреп), одна котировка для заказа, кабинета и бота; оплаченные посты по
  `ad_payments` (размещение = 1, пакет = units_paid); правка цены в CRM ниже 200 → 400.
  ✅ PR 2B — безлимит 30 дней (`feat/ad-unlimited-package`, без миграции): `kind=unlimited`,
  `posts_total=0`, открывается галочкой «оплачен» (период 30 дней с даты), списание без квоты
  (использованное считается), порядок промо → безлимит → платный, потолок незавершённых
  постов 500 (`AD_CLIENT_MAX_ACTIVE_POSTS_UNLIMITED`), продление встык, кнопка в CRM,
  «в счёт безлимита до …» в кабинете и боте.
  ✅ PR 2C — закреп на сутки (`feat/ad-pin`, миграция 099): чекбокс в кабинете, +200 ₽ за
  сообщество без скидки и пакета, окно закрепа ±сутки на сообщество (`pinning.pin_conflicts`),
  `wall.pin` после фиксации выхода в `record_published`, `unpin-ad-posts` раз в час снимает
  через сутки; неудача закрепа — событие `pin_failed`. Не сделано: закреп из бота и из
  операторского планировщика (галочка `ScheduleCreateIn.pinned`) — 🟢 следующий шаг.
  Этап 2 закрыт.
  ✅ Этап 3 — токен-роутер (`feat/token-router-identity`): `TokenOp.COMMUNITY_DM` (каскад
  сообщество → user, личность = сообщество) и `TokenOp.USER_DM(account)` (ровно этот аккаунт,
  без каскада и резерва; deny-list действует); фильтр кандидатов по снапшоту способностей
  (`token_capabilities`, только явные err7/err15, выключатель `VK_CAPABILITY_FILTER=0`);
  `messages.send`/лайки/precheck идут через общий лимитер и учёт расхода по токену
  (`enforce_token_rate_limit`) — раньше мимо, в UNKNOWN; VK 9/14 ставит паузу на DM-КАНАЛ
  сообщества (`modules/ad_cabinet/dm_channel.py`: 9 → сутки, 14 → 6 ч, Redis + запас в
  процессе, алёрт владельцу), токен не выключается. Потребители pick(COMMUNITY_DM/USER_DM)
  подключаются в Этапе 4 (рассылка); существующие пути ЛС уже уважают паузу.
  По разведке Этапа 3 также: `load_community_routing()` (бот/Радар/приветствие не гаснут при
  мёртвых user-токенах), ЛС без каскада на user-токен (у них нет scope messages), sync-ЛС из
  async-хендлеров web — в поток, два сырых httpx-пути ЛС в Радаре сведены в один транспорт,
  починен молчавший алёрт автоотключения токенов (импорт несуществующей функции).
- 🟡 Снапшот способностей: замеряются 2 community-токена из ~19 и только read-пробы; нужен полный
  прогон по COMM_* + тело `groups.getTokenPermissions` (scope) и ручной перезамер
  `POST /api/tokens/capabilities/refresh` — иначе переизданный токен до 1-го числа сидит с err15.
- 🟢 Пауза DM-канала хранится в Redis (+ запас в процессе); если понадобится строгая гарантия
  между web и воркерами при лежащем Redis — перенести в БД по образцу `promo_settings.paused_until`.
  ✅ Этап 4 — рассылка оффера (`feat/ad-outreach`, миграция 100): кампании
  `ad_outreach_campaigns` / адресаты `ad_outreach_recipients` (уникум по человеку, «один контакт
  навсегда») / стоп-лист; аудитория за N месяцев из `ad_requests` (ЛС → авто, предложка с
  `can_message` → авто, остальные → ручной список с deeplink), всем — кабинет и промо-пакет
  3 поста; тик раз в 5 минут 9–20 МСК: лиз строки, 30/сутки на сообщество и 150/сутки всего,
  тихие часы 21–9, dry-run по умолчанию, 901/902 → в ручной список, 900 → стоп-лист, 9/14 (в том
  числе капча отдельным исключением vk_api) → пауза DM-канала сообщества и стоп тика; шаблон — категория `ad_offer` с плейсхолдерами цен/ссылок; вкладка «Рассылка»
  в /ad. Не сделано: картинки в оффере (attachment_builder есть, сборщик из web-слоя не
  перенесён), Telegram-алёрт тика — через `_send_debtor_alert`.
  🖐 Владелец: положить шаблон категории `ad_offer` в /templates (плейсхолдеры
  {author_name} {price_single} {price_5} {price_10} {price_all} {pin_price} {promo_posts}
  {cabinet_link} {bot_link}), создать кампанию, «Набрать адресатов», сухой прогон, пилот.
  Далее: Этап 5 (доводка бота) — PR-1 фото из вложений и картинки от бота (`feat/vk-bot-photos`);
  ✅ PR-2 liveness демона `setka-vk-bot` (#650): heartbeat `setka:vkbot:heartbeat`
  после каждого ответа Long Poll + сторож `vk-bot-watchdog` каждые 10 мин (порог 15 мин, cooldown
  6 ч, «нет ключа» — WARNING без алёрта, бот выключен конфигом — skip), `/check`, `/logs`, доки
  со списком сервисов, `TimeoutStopSec=120` в шаблоне юнита (на проде юнит переустановлен
  `scripts/install_vk_bot_service.sh`). Негативная проба сторожа (остановка демона >15 мин) не
  гонялась — только `celery call … check_vk_bot_heartbeat` → `fresh`; PR-3 «📋 Мои посты» (группировка по order_ref, имена районов, ссылка на
  вышедший пост, строки предложки/репостов с пометкой) — ✅ сделано в #651
  вместе с прайсом и балансом бота (пакет, в т.ч. безлимит, накопительная скидка,
  `SINGLE_SERVICES`) и котировкой на шаге «когда», зеркалящей `/api/advertiser/quote`:
  долг отвечает «⛔» до «Подтвердить», выбор сверх остатка пакета возвращает к районам,
  дата вне периода — к выбору даты, цена «в счёт пакета/безлимита (0 ₽)».
  🖐 Владельцу — живая приёмка бота: «📋 Цены», «💰 Баланс», «📋 Мои посты» у клиента с
  пакетом и без; заказ с фото из ЛС САРАФАНа (текст+фото, второе фото отдельным сообщением)
  и картинка в «📣 Ваш пост вышел» — оба фото-пути живьём ещё не гонялись.
- 🟢 Хвосты Этапа 5 (не блокируют): картинка в подтверждении заказа из самого диалога
  (нужен 3-кортеж Reply), «оплатил <сумма>» текстом → awaiting-платёж, ретенция брошенных
  фото бота старше 2 суток и атомарная запись `<name>.part` → `os.replace`, негативная проба
  сторожа демона (остановка `setka-vk-bot` > 15 мин).
- 🟡 Черновик заказа в боте теряется, если `submit_order` всё же откажет на «Подтвердить»
  (`state=None`, текст поста набирать заново). Основные отказы бот теперь ловит раньше — на
  шаге даты зеркалятся долг, остаток и период пакета, окно планирования и анти-спам
  `busy_days`, — но остаточные (гонка с параллельным заказом, `pin_conflicts`) остаются;
  вариант — возвращать состояние на шаг даты, а не сбрасывать.
  🔴→✅ Инцидент 05.09 11:45 (сторож «планировщик встал»): оригинал Анны вышел в 10:00 как
  НОВЫЙ пост 78299 (VK меняет id отложке из предложки), а проверка `wall.getById` шла
  community-токеном (ошибка 27) — диспетчер ждал дедлайна. Руками: id в строке 8 → 78299,
  диспетчер запущен с user-токеном, 5 репостов вышли 11:49. Фикс кода —
  `fix/ad-suggested-published-id`: user-токен для проверки + поиск двойника по подписи/тексту.
- ✅ 05.09 13:00: планировщик не показывал два поста Анны из предложки Малмыж-ИНФО — сканер
  видел их, но классификатор дал 2 балла при пороге 3 (только «номер телефона»), заявки не
  создавались. Руками заведены (ids 1114731/1114732); фикс `fix/ad-planner-live-suggests`:
  планировщик сам подтягивает живую предложку в заявки независимо от вердикта классификатора.
- 🟢 Классификатор предложки: тексты про банкротство/списание долгов/кредиты без ссылки и цены
  набирают 2 балла (примеры — посты 78274, 78278 Малмыж-ИНФО, автор Анна Валиева, 04.09).
  Кандидат в правило для следующего `/distill`: финансовые услуги (банкротство, кредит, долг,
  МФО, юрист) как сигнал +2.
- 🟡 Тики диспетчера репостов (раз в минуту, `expires 55`) протухают, пока однопроцессный
  воркер гонит долгую тему (`copy/setka` ~40 мин): 25 попыток до 11:20, потом тишина до
  сторожа. Предложение: отдельная очередь `ad` и второй systemd-воркер `setka-celery-worker-ad`
  (`-Q ad -c 1`), маршрутизация `dispatch_ad_reposts`/`reconcile`/`watch_ad_pending` в неё.
- 🖐 **Владелец:** сумма за размещения Анны (операторская цена ≥ 200 ₽ × размещений).
- Этапы 1–5 — по плану, отдельными PR; хвосты аудита (реконсилер MSK/UTC, гонки, деньги пакетов,
  очередь модерации без фото, клиенты из бота вне списка, «оплатил» в боте) — Этап 1.
