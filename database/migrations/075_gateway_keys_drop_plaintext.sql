-- 075: gateway_keys — занулить открытые секреты, шаг 2 из 3
-- (мандат brain 2026-08-01, 🔴). Применять ТОЛЬКО после 074 и ТОЛЬКО после
-- рестарта приложения с кодом, проверяющим ключ по хэшу.
--
-- ⚠️ DESTRUCTIVE, необратимо: значения ключей после этого UPDATE нигде у нас
-- не хранятся. Это и есть цель — но она означает, что человеческий гейт #025
-- обязателен: подтверждение владельца в том же ходе, до применения.
--
-- Что НЕ ломается: потребители продолжают ходить со своими ключами (мы
-- проверяем хэш). Что становится невозможным: посмотреть чужой ключ у себя —
-- потерявший ключ потребитель получает новый через ротацию, а не «подсмотреть».
--
-- Предохранитель: зануляем только те строки, у которых хэш уже посчитан.
-- Если 074 почему-то не отработала, эта миграция не тронет ничего.

UPDATE gateway_keys
   SET secret = NULL
 WHERE secret IS NOT NULL
   AND secret_sha256 IS NOT NULL;

ALTER TABLE gateway_keys ALTER COLUMN secret DROP NOT NULL;

-- Проверка после применения (должно быть 0 строк):
--   SELECT name FROM gateway_keys WHERE secret IS NOT NULL;
--   SELECT name FROM gateway_keys WHERE is_active AND secret_sha256 IS NULL;
--
-- Отката нет: значения не восстановимы. Возврат на plaintext = ре-выдача
-- ключей всем потребителям.
--
-- Колонка ``secret`` остаётся занулённой на один цикл (чтобы откат кода на
-- предыдущий релиз не упирался в отсутствующую колонку) и удаляется
-- миграцией 076 следующим релизом.
