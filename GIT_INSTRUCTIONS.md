# 📤 Инструкция по пушу на GitHub

## ✅ Подготовка завершена!

Git репозиторий инициализирован, initial commit создан.

**Коммит:** `ba89171` - Initial commit: SETKA v1.0.0-alpha  
**Файлов:** 53  
**Строк кода:** 6,840+

---

## 🚀 Как запушить на GitHub (2 шага):

### ШАГ 1: Создать репозиторий на GitHub

1. Откройте: **https://github.com/new**
2. Заполните:
   - **Repository name:** `setka`
   - **Description:** `SETKA - Multimedia Management System for News Resources`
   - **Visibility:** ✅ **Private** (рекомендуется!)
   - **НЕ добавляйте:** README, .gitignore, license (у нас уже есть)
3. Нажмите **"Create repository"**

---

### ШАГ 2: Запушить код

**Вариант А (простой) - через скрипт:**

```bash
~/push_to_github.sh
```

Скрипт попросит:
- Username: `Valstan`
- Password: **Personal Access Token** (не обычный пароль!)

**Вариант Б (вручную):**

```bash
cd /home/valstan/SETKA
git push -u origin main
```

---

## 🔑 Получение Personal Access Token:

1. Откройте: https://github.com/settings/tokens
2. Нажмите **"Generate new token (classic)"**
3. Name: `SETKA Project`
4. Выберите scopes:
   - ✅ **repo** (полный доступ)
5. Нажмите **"Generate token"**
6. **СКОПИРУЙТЕ ТОКЕН** (показывается только один раз!)
7. Используйте этот токен вместо пароля при пуше

---

## ✅ После успешного пуша:

Ваш проект будет доступен:
- **URL:** https://github.com/Valstan/setka
- **Clone:** `git clone https://github.com/Valstan/setka.git`

---

## 🔒 Что НЕ попадёт на GitHub (защищено):

✅ **config/config.secure.py** - все токены и пароли  
✅ **venv/** - виртуальное окружение  
✅ **logs/** - логи приложения  
✅ **__pycache__/** - Python cache  

Эти файлы останутся только на сервере!

---

## 📝 Будущие изменения:

После разработки новых функций:

```bash
cd /home/valstan/SETKA
git add .
git commit -m "Описание изменений"
git push
```

---

## ⚠️ Важно:

- **НИКОГДА** не коммитьте `config/config.secure.py`
- **НИКОГДА** не удаляйте `.gitignore`
- **ВСЕГДА** проверяйте `git status` перед коммитом

---

**Готово к пушу!** 🚀

