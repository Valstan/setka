"""Модуль «Раскрутка» — продвижение молодых сообществ сети САРАФАН.

Раскладка: чистая логика (``hashtags``, ``settings``, ``enrollment``, ``pairing``,
``copy``, ``vk_errors``, ``scoring``) отделена от IO (``enrollment_service``,
``dispatcher``, ``group_setup``, ``outreach``) — арифметику и правила можно
прогнать без Postgres, VK и FastAPI.

Границы модуля описаны в ``config/promo.py``: на чужие стены не пишем, личных
сообщений не рассылаем, в сообщества не приглашаем, дополнительных аккаунтов не
заводим. Кандидатов для аутрича модуль только готовит — отправляет владелец сам.
"""
