"""HITL-классификатор контента (ADR-0003).

Shadow-фаза, этап B: классификацию делает облачная рутина через HTTP-интерфейс;
этот пакет — доменное ядро (схема вердикта + операции над БД), общее для рутины
и будущего Claude-API-пути.
"""
